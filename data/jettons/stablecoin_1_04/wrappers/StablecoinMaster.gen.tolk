/// Auto-generated wrapper for contract 'StablecoinMaster'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stablecoin_master.types"

/// Storage `StablecoinMasterStorage` is defined in `../types/stablecoin_master.types`
struct StablecoinMaster {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StablecoinMaster.fromStorage(
    storage: StablecoinMasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StablecoinMaster {
    val stateInit = ContractState {
        code: build("StablecoinMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StablecoinMaster { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StablecoinMaster.fromAddress(address: address): StablecoinMaster {
    return StablecoinMaster { address };
}

/// Deploys the contract to the blockchain
fun StablecoinMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterMint(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: StablecoinMasterJettonInternalTransfer,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterMint {
            queryId,
            toAddress,
            tonAmount,
            masterMsg: masterMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterClaimAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterClaimAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterCallTo(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: StablecoinMasterWalletAdminAction,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterCallTo {
            queryId,
            toAddress,
            tonAmount,
            masterMsg: masterMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterUpgrade(
    self,
    from: address,
    queryId: uint64,
    newData: cell,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterUpgrade { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterChangeMetadataUri(
    self,
    from: address,
    queryId: uint64,
    metadataUri: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterChangeMetadataUri { queryId, metadataUri },
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.sendStablecoinMasterTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinMasterTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StablecoinMaster.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StablecoinMaster.getJettonData(self): GetStablecoinJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun StablecoinMaster.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun StablecoinMaster.getNextAdminAddress(self): any_address {
    return net.runGetMethod(self.address, "get_next_admin_address");
}
