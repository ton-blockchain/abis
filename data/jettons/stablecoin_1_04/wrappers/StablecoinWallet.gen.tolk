/// Auto-generated wrapper for contract 'StablecoinWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stablecoin_wallet.types"

/// Storage `StablecoinWalletStorage` is defined in `../types/stablecoin_wallet.types`
struct StablecoinWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StablecoinWallet.fromStorage(
    storage: StablecoinWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StablecoinWallet {
    val stateInit = ContractState {
        code: build("StablecoinWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StablecoinWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StablecoinWallet.fromAddress(address: address): StablecoinWallet {
    return StablecoinWallet { address };
}

/// Deploys the contract to the blockchain
fun StablecoinWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StablecoinWallet.sendStablecoinWalletTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinWalletTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StablecoinWallet.sendStablecoinWalletInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    from_: address?,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinWalletInternalTransfer {
            queryId,
            amount,
            from: from_,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StablecoinWallet.sendStablecoinWalletBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinWalletBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun StablecoinWallet.sendStablecoinWalletSetStatus(
    self,
    from: address,
    queryId: uint64,
    status: uint4,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinWalletSetStatus { queryId, status },
    });
    return net.send(from, genericMsg);
}

fun StablecoinWallet.sendStablecoinWalletTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StablecoinWalletTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StablecoinWallet.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StablecoinWallet.getWalletData(self): GetStablecoinWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}

fun StablecoinWallet.getStatus(self): uint4 {
    return net.runGetMethod(self.address, "get_status");
}
