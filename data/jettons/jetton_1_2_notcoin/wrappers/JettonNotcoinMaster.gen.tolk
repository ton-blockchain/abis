/// Auto-generated wrapper for contract 'JettonNotcoinMaster'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/jetton_notcoin_master.types"

/// Storage `JettonNotcoinMasterStorage` is defined in `../types/jetton_notcoin_master.types`
struct JettonNotcoinMaster {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun JettonNotcoinMaster.fromStorage(
    storage: JettonNotcoinMasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): JettonNotcoinMaster {
    val stateInit = ContractState {
        code: build("JettonNotcoinMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return JettonNotcoinMaster { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun JettonNotcoinMaster.fromAddress(address: address): JettonNotcoinMaster {
    return JettonNotcoinMaster { address };
}

/// Deploys the contract to the blockchain
fun JettonNotcoinMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterMint(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: JettonNotcoinMasterInternalTransfer,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterMint {
            queryId,
            toAddress,
            tonAmount,
            masterMsg: masterMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterClaimAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterClaimAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterDropAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterDropAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterChangeMetadataUri(
    self,
    from: address,
    queryId: uint64,
    metadataUri: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterChangeMetadataUri { queryId, metadataUri },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterUpgrade(
    self,
    from: address,
    queryId: uint64,
    newData: cell,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterUpgrade { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.sendJettonNotcoinMasterTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonNotcoinMasterTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun JettonNotcoinMaster.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun JettonNotcoinMaster.getJettonData(self): GetJettonNotcoinDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun JettonNotcoinMaster.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun JettonNotcoinMaster.getNextAdminAddress(self): any_address {
    return net.runGetMethod(self.address, "get_next_admin_address");
}
