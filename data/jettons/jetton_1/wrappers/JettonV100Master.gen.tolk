/// Auto-generated wrapper for contract 'JettonV100Master'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/jetton_v1_00_master.types"

/// Storage `JettonV100MasterStorage` is defined in `../types/jetton_v1_00_master.types`
struct JettonV100Master {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun JettonV100Master.fromStorage(
    storage: JettonV100MasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): JettonV100Master {
    val stateInit = ContractState {
        code: build("JettonV100Master"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return JettonV100Master { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun JettonV100Master.fromAddress(address: address): JettonV100Master {
    return JettonV100Master { address };
}

/// Deploys the contract to the blockchain
fun JettonV100Master.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonV100Master.sendJettonV100Mint(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonV100Mint { queryId, toAddress, tonAmount, masterMsg },
    });
    return net.send(from, genericMsg);
}

fun JettonV100Master.sendJettonBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun JettonV100Master.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun JettonV100Master.getJettonData(self): GetJettonV100DataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun JettonV100Master.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}
