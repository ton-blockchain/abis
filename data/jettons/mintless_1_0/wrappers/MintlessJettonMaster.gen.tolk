/// Auto-generated wrapper for contract 'MintlessJettonMaster'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/mintless_master.types"

/// Storage `MintlessJettonMasterStorage` is defined in `../types/mintless_master.types`
struct MintlessJettonMaster {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MintlessJettonMaster.fromStorage(
    storage: MintlessJettonMasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MintlessJettonMaster {
    val stateInit = ContractState {
        code: build("MintlessJettonMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MintlessJettonMaster { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MintlessJettonMaster.fromAddress(address: address): MintlessJettonMaster {
    return MintlessJettonMaster { address };
}

/// Deploys the contract to the blockchain
fun MintlessJettonMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterMint(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: MintlessJettonMasterInternalTransfer,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterMint {
            queryId,
            toAddress,
            tonAmount,
            masterMsg: masterMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterClaimAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterClaimAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterDropAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterDropAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterChangeMetadataUri(
    self,
    from: address,
    queryId: uint64,
    metadataUri: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterChangeMetadataUri { queryId, metadataUri },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterUpgrade(
    self,
    from: address,
    queryId: uint64,
    newData: cell,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterUpgrade { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.sendMintlessJettonMasterTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonMasterTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MintlessJettonMaster.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonMaster.getJettonData(self): GetMintlessJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun MintlessJettonMaster.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun MintlessJettonMaster.getWalletStateInitAndSalt(
    self,
    ownerAddress: address,
): GetMintlessWalletStateInitAndSaltReply {
    return net.runGetMethod(self.address, "get_wallet_state_init_and_salt", [ownerAddress]);
}

fun MintlessJettonMaster.getNextAdminAddress(self): any_address {
    return net.runGetMethod(self.address, "get_next_admin_address");
}

fun MintlessJettonMaster.getMintlessAirdropHashmapRoot(self): uint256 {
    return net.runGetMethod(self.address, "get_mintless_airdrop_hashmap_root");
}
