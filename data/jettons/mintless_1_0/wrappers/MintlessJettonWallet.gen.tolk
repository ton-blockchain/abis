/// Auto-generated wrapper for contract 'MintlessJettonWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/mintless_wallet.types"

/// Storage `MintlessJettonWalletStorage` is defined in `../types/mintless_wallet.types`
struct MintlessJettonWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MintlessJettonWallet.fromStorage(
    storage: MintlessJettonWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MintlessJettonWallet {
    val stateInit = ContractState {
        code: build("MintlessJettonWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MintlessJettonWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MintlessJettonWallet.fromAddress(address: address): MintlessJettonWallet {
    return MintlessJettonWallet { address };
}

/// Deploys the contract to the blockchain
fun MintlessJettonWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonWallet.sendMintlessJettonWalletTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: Cell<MintlessAirdropClaim>?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonWalletTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonWallet.sendMintlessJettonWalletInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    from_: address?,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonWalletInternalTransfer {
            queryId,
            amount,
            from: from_,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonWallet.sendMintlessJettonWalletBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonWalletBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonWallet.sendMintlessJettonWalletTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintlessJettonWalletTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MintlessJettonWallet.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MintlessJettonWallet.getWalletData(self): GetMintlessJettonWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}

fun MintlessJettonWallet.getStatus(self): uint4 {
    return net.runGetMethod(self.address, "get_status");
}

fun MintlessJettonWallet.isClaimed(self): bool {
    return net.runGetMethod(self.address, "is_claimed");
}
