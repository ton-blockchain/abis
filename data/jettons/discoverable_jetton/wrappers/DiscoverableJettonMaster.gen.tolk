/// Auto-generated wrapper for contract 'DiscoverableJettonMaster'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/discoverable_jetton_master.types"

/// Storage `DiscoverableJettonMasterStorage` is defined in `../types/discoverable_jetton_master.types`
struct DiscoverableJettonMaster {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DiscoverableJettonMaster.fromStorage(
    storage: DiscoverableJettonMasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DiscoverableJettonMaster {
    val stateInit = ContractState {
        code: build("DiscoverableJettonMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DiscoverableJettonMaster { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DiscoverableJettonMaster.fromAddress(address: address): DiscoverableJettonMaster {
    return DiscoverableJettonMaster { address };
}

/// Deploys the contract to the blockchain
fun DiscoverableJettonMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DiscoverableJettonMaster.sendJettonMint(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonMint { queryId, toAddress, tonAmount, masterMsg },
    });
    return net.send(from, genericMsg);
}

fun DiscoverableJettonMaster.sendJettonBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun DiscoverableJettonMaster.sendProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun DiscoverableJettonMaster.sendJettonChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun DiscoverableJettonMaster.sendJettonChangeContent(
    self,
    from: address,
    queryId: uint64,
    jettonContent: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonChangeContent { queryId, jettonContent },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DiscoverableJettonMaster.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DiscoverableJettonMaster.getJettonData(self): GetDiscoverableJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun DiscoverableJettonMaster.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}
