/// Auto-generated wrapper for contract 'Jetton2Master'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/jetton_2_master.types"

/// Storage `Jetton2MasterStorage` is defined in `../types/jetton_2_master.types`
struct Jetton2Master {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Jetton2Master.fromStorage(
    storage: Jetton2MasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Jetton2Master {
    val stateInit = ContractState {
        code: build("Jetton2Master"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Jetton2Master { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Jetton2Master.fromAddress(address: address): Jetton2Master {
    return Jetton2Master { address };
}

/// Deploys the contract to the blockchain
fun Jetton2Master.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterMint(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    tonAmount: coins,
    masterMsg: Jetton2MasterInternalTransfer,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterMint {
            queryId,
            toAddress,
            tonAmount,
            masterMsg: masterMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterClaimAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterClaimAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterDropAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterDropAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterChangeMetadataUri(
    self,
    from: address,
    queryId: uint64,
    metadataUri: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterChangeMetadataUri { queryId, metadataUri },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterUpgrade(
    self,
    from: address,
    queryId: uint64,
    newData: cell,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterUpgrade { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.sendJetton2MasterTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Jetton2MasterTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Jetton2Master.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Jetton2Master.getJettonData(self): GetJetton2DataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun Jetton2Master.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun Jetton2Master.getNextAdminAddress(self): any_address {
    return net.runGetMethod(self.address, "get_next_admin_address");
}
