/// Auto-generated wrapper for contract 'StonksPumpCustomJettonWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonkspump_custom_jetton_wallet.types"

/// Storage `StonksPumpCustomJettonWalletStorage` is defined in `../types/stonkspump_custom_jetton_wallet.types`
struct StonksPumpCustomJettonWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonksPumpCustomJettonWallet.fromStorage(
    storage: StonksPumpCustomJettonWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonksPumpCustomJettonWallet {
    val stateInit = ContractState {
        code: build("StonksPumpCustomJettonWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonksPumpCustomJettonWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonksPumpCustomJettonWallet.fromAddress(address: address): StonksPumpCustomJettonWallet {
    return StonksPumpCustomJettonWallet { address };
}

/// Deploys the contract to the blockchain
fun StonksPumpCustomJettonWallet.deploy(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpCustomJettonWallet.sendStonksPumpJettonTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpJettonTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpCustomJettonWallet.sendStonksPumpJettonInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderOwner: address?,
    transferContext: StonksPumpJettonTransferContext,
    responseDestination: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpJettonInternalTransfer {
            queryId,
            amount,
            senderOwner,
            transferContext: transferContext.toCell(),
            responseDestination,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpCustomJettonWallet.sendStonksPumpJettonBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpJettonBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpCustomJettonWallet.sendStonksPumpJettonTaxTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderOwner: address,
    contextAddress0: address,
    contextAddress1: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpJettonTaxTransfer {
            queryId,
            amount,
            senderOwner,
            contextAddress0,
            contextAddress1,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonksPumpCustomJettonWallet.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonksPumpCustomJettonWallet.getTax(self): StonksPumpJettonTaxRates {
    return net.runGetMethod(self.address, "get_tax");
}

fun StonksPumpCustomJettonWallet.getWalletData(self): StonksPumpCustomJettonWalletData {
    return net.runGetMethod(self.address, "get_wallet_data");
}
