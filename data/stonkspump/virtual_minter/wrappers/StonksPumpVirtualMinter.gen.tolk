/// Auto-generated wrapper for contract 'StonksPumpVirtualMinterStonfi'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonkspump_virtual_minter.types"

/// Storage `StonksPumpVirtualMinterStorage` is defined in `../types/stonkspump_virtual_minter.types`
struct StonksPumpVirtualMinterStonfi {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonksPumpVirtualMinterStonfi.fromStorage(
    storage: StonksPumpVirtualMinterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonksPumpVirtualMinterStonfi {
    val stateInit = ContractState {
        code: build("StonksPumpVirtualMinter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonksPumpVirtualMinterStonfi { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonksPumpVirtualMinterStonfi.fromAddress(address: address): StonksPumpVirtualMinterStonfi {
    return StonksPumpVirtualMinterStonfi { address };
}

/// Deploys the contract to the blockchain
fun StonksPumpVirtualMinterStonfi.deploy(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpJettonMint(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    forwardTonAmount: coins,
    internalTransfer: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpJettonMint { queryId, recipient, forwardTonAmount, internalTransfer },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpJettonBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    owner: address,
    responseAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpJettonBurnNotification { queryId, amount, owner, responseAddress },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpChangeContent(
    self,
    from: address,
    queryId: uint64,
    jettonContent: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpChangeContent { queryId, jettonContent },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpVirtualMinterInit(
    self,
    from: address,
    queryId: uint64,
    presaleOpen: bool,
    migrationTonTarget: coins,
    tonsCollected: coins,
    baseJettonReserve: coins,
    additionalTonReserve: coins,
    additionalJettonReserve: coins,
    maxBuyPercent: uint16,
    migrationConfig: cell,
    devBuy: StonksPumpVirtualDevBuy,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpVirtualMinterInit {
            queryId,
            presaleOpen,
            migrationTonTarget,
            tonsCollected,
            baseJettonReserve,
            additionalTonReserve,
            additionalJettonReserve,
            maxBuyPercent,
            migrationConfig,
            devBuy: devBuy.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpBuyFromPresale(
    self,
    from: address,
    queryId: uint64,
    tonAmount: coins,
    minJettonsOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpBuyFromPresale { queryId, tonAmount, minJettonsOut },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.sendStonksPumpPresaleSellNotificationForMinter(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    sellInitiator: address,
    tonsRecipient: address?,
    minTonOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpPresaleSellNotificationForMinter {
            queryId,
            jettonAmount,
            sellInitiator,
            tonsRecipient,
            minTonOut,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonksPumpVirtualMinterStonfi.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualMinterStonfi.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun StonksPumpVirtualMinterStonfi.getJettonData(self): StonksPumpJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun StonksPumpVirtualMinterStonfi.getVersion(self): int {
    return net.runGetMethod(self.address, "get_version");
}

fun StonksPumpVirtualMinterStonfi.getBondingData(self): StonksPumpBondingData {
    return net.runGetMethod(self.address, "get_bonding_data");
}
