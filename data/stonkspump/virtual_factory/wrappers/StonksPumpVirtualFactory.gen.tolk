/// Auto-generated wrapper for contract 'StonksPumpVirtualFactoryStonfi'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonkspump_virtual_factory.types"

/// Storage `StonksPumpVirtualFactoryStonfiStorage` is defined in `../types/stonkspump_virtual_factory.types`
struct StonksPumpVirtualFactoryStonfi {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonksPumpVirtualFactoryStonfi.fromStorage(
    storage: StonksPumpVirtualFactoryStonfiStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonksPumpVirtualFactoryStonfi {
    val stateInit = ContractState {
        code: build("StonksPumpVirtualFactory"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonksPumpVirtualFactoryStonfi { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonksPumpVirtualFactoryStonfi.fromAddress(address: address): StonksPumpVirtualFactoryStonfi {
    return StonksPumpVirtualFactoryStonfi { address };
}

/// Deploys the contract to the blockchain
fun StonksPumpVirtualFactoryStonfi.deploy(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualFactoryStonfi.sendStonksPumpCreateVirtualLiquidityJetton(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    devBuyTonAmount: coins,
    maxBuyPercent: uint16,
    devWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpCreateVirtualLiquidityJetton {
            queryId,
            content,
            devBuyTonAmount,
            maxBuyPercent,
            devWallet,
        },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualFactoryStonfi.sendStonksPumpVirtualFactoryWithdrawTon(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpVirtualFactoryWithdrawTon { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualFactoryStonfi.sendStonksPumpVirtualFactorySetDisabled(
    self,
    from: address,
    queryId: uint64,
    disabled: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonksPumpVirtualFactorySetDisabled { queryId, disabled },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonksPumpVirtualFactoryStonfi.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonksPumpVirtualFactoryStonfi.getFactoryData(self): StonksPumpVirtualFactoryStonfiData {
    return net.runGetMethod(self.address, "get_factory_data");
}
