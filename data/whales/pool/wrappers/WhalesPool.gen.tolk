/// Auto-generated wrapper for contract 'WhalesPool'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/whales_pool.types"

/// Storage `WhalesPoolStorage` is defined in `../types/whales_pool.types`
struct WhalesPool {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WhalesPool.fromStorage(
    storage: WhalesPoolStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WhalesPool {
    val stateInit = ContractState {
        code: build("WhalesPool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WhalesPool { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WhalesPool.fromAddress(address: address): WhalesPool {
    return WhalesPool { address };
}

/// Deploys the contract to the blockchain
fun WhalesPool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolTextComment(
    self,
    from: address,
    text: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolTextComment { text },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolStakeDeposit(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolStakeDeposit { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolStakeWithdraw(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolStakeWithdraw { queryId, gasLimit, amount },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolStakeRecover(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolStakeRecover { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolDonate(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolDonate { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolStakeSend(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    stake: coins,
    validatorPubkey: bits256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddr: bits256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolStakeSend {
            queryId,
            gasLimit,
            stake,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddr,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolStakeLockFinalize(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolStakeLockFinalize { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolWithdrawUnowned(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolWithdrawUnowned { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolAcceptStakes(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    members: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolAcceptStakes { queryId, gasLimit, members },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolAcceptWithdraws(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    members: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolAcceptWithdraws { queryId, gasLimit, members },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolForceKick(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    members: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolForceKick { queryId, gasLimit, members },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolUpgrade(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolUpgrade { queryId, gasLimit, code },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolUpdate(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    extras: WhalesPoolExtras,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolUpdate {
            queryId,
            gasLimit,
            extras: extras.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolChangeAddress(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    addressId: uint8,
    newAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolChangeAddress { queryId, gasLimit, addressId, newAddress },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolElectorNewStakeConfirmation(
    self,
    from: address,
    queryId: uint64,
    comment: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolElectorNewStakeConfirmation { queryId, comment },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolElectorNewStakeError(
    self,
    from: address,
    queryId: uint64,
    reason: uint32,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolElectorNewStakeError { queryId, reason, tail },
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.sendWhalesPoolElectorRecoverStakeResponse(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesPoolElectorRecoverStakeResponse { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WhalesPool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WhalesPool.getProxy(self): address {
    return net.runGetMethod(self.address, "get_proxy");
}

fun WhalesPool.getOwner(self): address {
    return net.runGetMethod(self.address, "get_owner");
}

fun WhalesPool.getController(self): address {
    return net.runGetMethod(self.address, "get_controller");
}

fun WhalesPool.getUnowned(self): coins {
    return net.runGetMethod(self.address, "get_unowned");
}

fun WhalesPool.getAvailable(self): coins {
    return net.runGetMethod(self.address, "get_available");
}

fun WhalesPool.getStakingStatus(self): WhalesPoolStakingStatusReply {
    return net.runGetMethod(self.address, "get_staking_status");
}

fun WhalesPool.getPoolStatus(self): WhalesPoolStatusReply {
    return net.runGetMethod(self.address, "get_pool_status");
}

fun WhalesPool.getParams(self): WhalesPoolParamsReply {
    return net.runGetMethod(self.address, "get_params");
}

fun WhalesPool.getMemberBalance(self, memberAddress: address): WhalesPoolMemberBalanceReply {
    return net.runGetMethod(self.address, "get_member_balance", [memberAddress]);
}

fun WhalesPool.getMembersRaw(self): map<uint256, WhalesPoolMember> {
    return net.runGetMethod(self.address, "get_members_raw");
}

fun WhalesPool.getMembers(self): lisp_list<WhalesPoolMemberListEntry> {
    return net.runGetMethod(self.address, "get_members");
}

fun WhalesPool.getMember(self, memberAddress: address): WhalesPoolMemberBalanceReply {
    return net.runGetMethod(self.address, "get_member", [memberAddress]);
}

fun WhalesPool.supportedInterfaces(self): WhalesPoolSupportedInterfacesReply {
    return net.runGetMethod(self.address, "supported_interfaces");
}
