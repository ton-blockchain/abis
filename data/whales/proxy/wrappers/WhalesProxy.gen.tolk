/// Auto-generated wrapper for contract 'WhalesProxy'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/whales_proxy.types"

/// Storage `WhalesProxyStorage` is defined in `../types/whales_proxy.types`
struct WhalesProxy {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WhalesProxy.fromStorage(
    storage: WhalesProxyStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WhalesProxy {
    val stateInit = ContractState {
        code: build("WhalesProxy"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WhalesProxy { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WhalesProxy.fromAddress(address: address): WhalesProxy {
    return WhalesProxy { address };
}

/// Deploys the contract to the blockchain
fun WhalesProxy.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesProxy.sendWhalesProxyElectorNewStake(
    self,
    from: address,
    queryId: uint64,
    validatorPubkey: bits256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddr: bits256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesProxyElectorNewStake {
            queryId,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddr,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesProxy.sendWhalesProxyElectorRecoverStakeRequest(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesProxyElectorRecoverStakeRequest { queryId },
    });
    return net.send(from, genericMsg);
}

fun WhalesProxy.sendWhalesProxyElectorNewStakeConfirmation(
    self,
    from: address,
    queryId: uint64,
    comment: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesProxyElectorNewStakeConfirmation { queryId, comment },
    });
    return net.send(from, genericMsg);
}

fun WhalesProxy.sendWhalesProxyElectorRecoverStakeResponse(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesProxyElectorRecoverStakeResponse { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WhalesProxy.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
