/// Auto-generated wrapper for contract 'WhalesLiquidStaking'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/whales_liquid_staking.types"

/// Storage `WhalesLiquidStakingStorage` is defined in `../types/whales_liquid_staking.types`
struct WhalesLiquidStaking {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WhalesLiquidStaking.fromStorage(
    storage: WhalesLiquidStakingStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WhalesLiquidStaking {
    val stateInit = ContractState {
        code: build("WhalesLiquidStaking"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WhalesLiquidStaking { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WhalesLiquidStaking.fromAddress(address: address): WhalesLiquidStaking {
    return WhalesLiquidStaking { address };
}

/// Deploys the contract to the blockchain
fun WhalesLiquidStaking.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingTextCommand(
    self,
    from: address,
    text: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingTextCommand { text },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingDeposit(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingDeposit {
            queryId,
            amount,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingWithdraw(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    responseAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingWithdraw { queryId, jettonAmount, responseAddress },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingCollect(
    self,
    from: address,
    queryId: uint64,
    responseAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingCollect { queryId, responseAddress },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingWalletWithdrawal(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    roundId: uint32,
    responseAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingWalletWithdrawal { queryId, amount, roundId, responseAddress },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingBurnNotification(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    ownerAddress: address,
    responseAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingBurnNotification {
            queryId,
            jettonAmount,
            ownerAddress,
            responseAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeOwnerAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingProvideWalletAddress {
            queryId,
            ownerAddress,
            includeOwnerAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingProvideCurrentQuote(
    self,
    from: address,
    queryId: uint64,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingProvideCurrentQuote { queryId, customPayload },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingStakeSend(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    proxyId: bool,
    stake: coins,
    validatorPubkey: bits256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddress: bits256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingStakeSend {
            queryId,
            gasLimit,
            proxyId,
            stake,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddress,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingStakeRecover(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    proxyId: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingStakeRecover { queryId, gasLimit, proxyId },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingStakeLockFinalize(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    proxyId: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingStakeLockFinalize { queryId, gasLimit, proxyId },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingWithdrawUnowned(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingWithdrawUnowned { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingDeploy(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingDeploy { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingUpgrade(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingUpgrade { queryId, gasLimit, code },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingUpdateParams(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    params: WhalesLiquidStakingParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingUpdateParams {
            queryId,
            gasLimit,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingAirdrop(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingAirdrop { queryId, gasLimit },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingChangeAddress(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    addressId: uint8,
    newAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingChangeAddress { queryId, gasLimit, addressId, newAddress },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingSetContent(
    self,
    from: address,
    queryId: uint64,
    gasLimit: coins,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingSetContent { queryId, gasLimit, content },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingElectorNewStakeConfirmation(
    self,
    from: address,
    queryId: uint64,
    comment: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingElectorNewStakeConfirmation { queryId, comment },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingElectorNewStakeError(
    self,
    from: address,
    queryId: uint64,
    reason: uint32,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingElectorNewStakeError { queryId, reason, tail },
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.sendWhalesLiquidStakingElectorRecoverStakeResponse(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WhalesLiquidStakingElectorRecoverStakeResponse { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WhalesLiquidStaking.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WhalesLiquidStaking.getProxies(self): WhalesLiquidStakingProxiesReply {
    return net.runGetMethod(self.address, "get_proxies");
}

fun WhalesLiquidStaking.getOwner(self): address {
    return net.runGetMethod(self.address, "get_owner");
}

fun WhalesLiquidStaking.getAvailable(self): coins {
    return net.runGetMethod(self.address, "get_available");
}

fun WhalesLiquidStaking.getUnowned(self): coins {
    return net.runGetMethod(self.address, "get_unowned");
}

fun WhalesLiquidStaking.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun WhalesLiquidStaking.getTonBalance(self, jettonAmount: coins): coins {
    return net.runGetMethod(self.address, "get_ton_balance", [jettonAmount]);
}

fun WhalesLiquidStaking.getJettonData(self): WhalesLiquidStakingJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun WhalesLiquidStaking.getRates(self): WhalesLiquidStakingRatesReply {
    return net.runGetMethod(self.address, "get_rates");
}

fun WhalesLiquidStaking.supportedInterfaces(self): WhalesLiquidStakingSupportedInterfacesReply {
    return net.runGetMethod(self.address, "supported_interfaces");
}

fun WhalesLiquidStaking.getParams(self): WhalesLiquidStakingParamsReply {
    return net.runGetMethod(self.address, "get_params");
}

fun WhalesLiquidStaking.getController(self): address {
    return net.runGetMethod(self.address, "get_controller");
}

fun WhalesLiquidStaking.getPoolStatus(self): WhalesLiquidStakingPoolStatusReply {
    return net.runGetMethod(self.address, "get_pool_status");
}

fun WhalesLiquidStaking.getStakingStatus(self, proxyId: int): WhalesLiquidStakingStatusReply {
    return net.runGetMethod(self.address, "get_staking_status", [proxyId]);
}
