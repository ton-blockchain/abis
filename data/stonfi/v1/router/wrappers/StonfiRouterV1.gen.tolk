/// Auto-generated wrapper for contract 'StonfiRouterV1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonfi_router_v1.types"

/// Storage `StonfiRouterV1Storage` is defined in `../types/stonfi_router_v1.types`
struct StonfiRouterV1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonfiRouterV1.fromStorage(
    storage: StonfiRouterV1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonfiRouterV1 {
    val stateInit = ContractState {
        code: build("StonfiRouterV1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonfiRouterV1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonfiRouterV1.fromAddress(address: address): StonfiRouterV1 {
    return StonfiRouterV1 { address };
}

/// Deploys the contract to the blockchain
fun StonfiRouterV1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1PayTo(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    exitCode: uint32,
    paramsAsRef: uint1,
    params: StonfiRouterV1PayToParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1PayTo {
            queryId,
            owner,
            exitCode,
            paramsAsRef,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1JettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    fromAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1JettonTransferNotification {
            queryId,
            jettonAmount,
            fromAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1SetFees(
    self,
    from: address,
    queryId: uint64,
    newLpFee: uint8,
    newProtocolFee: uint8,
    newRefFee: uint8,
    newProtocolFeeAddress: address,
    poolInfo: StonfiRouterV1SetFeesPoolInfo,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1SetFees {
            queryId,
            newLpFee,
            newProtocolFee,
            newRefFee,
            newProtocolFeeAddress,
            poolInfo: poolInfo.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1CollectFees(
    self,
    from: address,
    queryId: uint64,
    jettonWallet0: address,
    jettonWallet1: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1CollectFees { queryId, jettonWallet0, jettonWallet1 },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1Lock(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1Lock { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1Unlock(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1Unlock { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1InitCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1InitCodeUpgrade { queryId, newCode },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1InitAdminUpgrade(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1InitAdminUpgrade { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1CancelCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1CancelCodeUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1CancelAdminUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1CancelAdminUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1FinalizeUpgrades(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1FinalizeUpgrades { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1ResetPoolGas(
    self,
    from: address,
    queryId: uint64,
    jettonWallet0: address,
    jettonWallet1: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1ResetPoolGas { queryId, jettonWallet0, jettonWallet1 },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.sendStonfiRouterV1GetterPoolAddressRequest(
    self,
    from: address,
    queryId: uint64,
    token0: address,
    token1: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV1GetterPoolAddressRequest { queryId, token0, token1 },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonfiRouterV1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV1.getPoolAddress(self, token0: address, token1: address): address {
    return net.runGetMethod(self.address, "get_pool_address", [token0, token1]);
}

fun StonfiRouterV1.getRouterData(self): StonfiRouterV1DataReply {
    return net.runGetMethod(self.address, "get_router_data");
}
