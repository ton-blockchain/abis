/// Auto-generated wrapper for contract 'StonfiLpAccountV1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonfi_lp_account_v1.types"

/// Storage `StonfiLpAccountV1Storage` is defined in `../types/stonfi_lp_account_v1.types`
struct StonfiLpAccountV1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonfiLpAccountV1.fromStorage(
    storage: StonfiLpAccountV1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonfiLpAccountV1 {
    val stateInit = ContractState {
        code: build("StonfiLpAccountV1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonfiLpAccountV1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonfiLpAccountV1.fromAddress(address: address): StonfiLpAccountV1 {
    return StonfiLpAccountV1 { address };
}

/// Deploys the contract to the blockchain
fun StonfiLpAccountV1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV1.sendStonfiLpAccountV1AddLiquidity(
    self,
    from: address,
    queryId: uint64,
    newAmount0: coins,
    newAmount1: coins,
    minLpOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV1AddLiquidity { queryId, newAmount0, newAmount1, minLpOut },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV1.sendStonfiLpAccountV1RefundMe(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV1RefundMe { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV1.sendStonfiLpAccountV1DirectAddLiquidity(
    self,
    from: address,
    queryId: uint64,
    amount0: coins,
    amount1: coins,
    minLpOut: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV1DirectAddLiquidity { queryId, amount0, amount1, minLpOut },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV1.sendStonfiLpAccountV1ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV1ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV1.sendStonfiLpAccountV1GetterLpAccountDataRequest(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV1GetterLpAccountDataRequest { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonfiLpAccountV1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV1.getLpAccountData(self): StonfiLpAccountV1DataReply {
    return net.runGetMethod(self.address, "get_lp_account_data");
}
