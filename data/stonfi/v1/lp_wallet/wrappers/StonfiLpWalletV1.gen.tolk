/// Auto-generated wrapper for contract 'StonfiLpWalletV1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonfi_lp_wallet_v1.types"

/// Storage `StonfiLpWalletV1Storage` is defined in `../types/stonfi_lp_wallet_v1.types`
struct StonfiLpWalletV1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonfiLpWalletV1.fromStorage(
    storage: StonfiLpWalletV1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonfiLpWalletV1 {
    val stateInit = ContractState {
        code: build("StonfiLpWalletV1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonfiLpWalletV1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonfiLpWalletV1.fromAddress(address: address): StonfiLpWalletV1 {
    return StonfiLpWalletV1 { address };
}

/// Deploys the contract to the blockchain
fun StonfiLpWalletV1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpWalletV1.sendStonfiLpWalletV1Transfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseAddress: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpWalletV1Transfer {
            queryId,
            amount,
            destination,
            responseAddress,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpWalletV1.sendStonfiLpWalletV1InternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    fromAddress: address,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpWalletV1InternalTransfer {
            queryId,
            amount,
            fromAddress,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpWalletV1.sendStonfiLpWalletV1Burn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpWalletV1Burn { queryId, amount, responseAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonfiLpWalletV1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonfiLpWalletV1.getWalletData(self): GetStonfiLpWalletV1DataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}
