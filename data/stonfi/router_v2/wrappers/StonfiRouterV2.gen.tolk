/// Auto-generated wrapper for contract 'StonfiRouterV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonfi_router_v2.types"

/// Storage `StonfiRouterV2Storage` is defined in `../types/stonfi_router_v2.types`
struct StonfiRouterV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonfiRouterV2.fromStorage(
    storage: StonfiRouterV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonfiRouterV2 {
    val stateInit = ContractState {
        code: build("StonfiRouterV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonfiRouterV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonfiRouterV2.fromAddress(address: address): StonfiRouterV2 {
    return StonfiRouterV2 { address };
}

/// Deploys the contract to the blockchain
fun StonfiRouterV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2JettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    fromAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2JettonTransferNotification {
            queryId,
            jettonAmount,
            fromAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendPtonResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2UpdateStatus(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2UpdateStatus { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2SetFees(
    self,
    from: address,
    queryId: uint64,
    newLpFee: uint16,
    newProtocolFee: uint16,
    newProtocolFeeAddress: address,
    jettonInfo: StonfiRouterV2SetFeesJettonInfo,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2SetFees {
            queryId,
            newLpFee,
            newProtocolFee,
            newProtocolFeeAddress,
            jettonInfo: jettonInfo.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2ResetPoolGas(
    self,
    from: address,
    queryId: uint64,
    jettonWallet0: address,
    jettonWallet1: address,
    excessesRecipient: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2ResetPoolGas {
            queryId,
            jettonWallet0,
            jettonWallet1,
            excessesRecipient,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2UpdatePoolStatus(
    self,
    from: address,
    queryId: uint64,
    jettonWallet0: address,
    jettonWallet1: address,
    excessesRecipient: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2UpdatePoolStatus {
            queryId,
            jettonWallet0,
            jettonWallet1,
            excessesRecipient,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2UpdatePoolCode(
    self,
    from: address,
    queryId: uint64,
    jettonWallet0: address,
    jettonWallet1: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2UpdatePoolCode { queryId, jettonWallet0, jettonWallet1 },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2InitCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2InitCodeUpgrade { queryId, newCode },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2InitAdminUpgrade(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2InitAdminUpgrade { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2InitPoolCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    newPoolCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2InitPoolCodeUpgrade { queryId, newPoolCode },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2CancelCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2CancelCodeUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2CancelAdminUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2CancelAdminUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2CancelPoolCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2CancelPoolCodeUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2FinalizeUpgrades(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2FinalizeUpgrades { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2SetParamsRaw(
    self,
    from: address,
    queryId: uint64,
    params: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2SetParamsRaw { queryId, params },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiPayToV2(
    self,
    from: address,
    queryId: uint64,
    toAddress: address,
    excessesAddress: address,
    originalCaller: address,
    exitCode: uint32,
    customPayload: cell?,
    additionalInfo: StonfiPayToV2AdditionalInfo,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiPayToV2 {
            queryId,
            toAddress,
            excessesAddress,
            originalCaller,
            exitCode,
            customPayload,
            additionalInfo: additionalInfo.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiPayVaultV2(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    excessesAddress: address,
    additionalInfo: StonfiPayVaultV2AdditionalInfo,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiPayVaultV2 {
            queryId,
            owner,
            excessesAddress,
            additionalInfo: additionalInfo.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiVaultPayToV2(
    self,
    from: address,
    queryId: uint64,
    amountOut: coins,
    tokenAddress: address,
    toAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiVaultPayToV2 { queryId, amountOut, tokenAddress, toAddress },
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.sendStonfiRouterV2GetterPoolAddressRequest(
    self,
    from: address,
    queryId: uint64,
    token0: address,
    token1: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiRouterV2GetterPoolAddressRequest { queryId, token0, token1 },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonfiRouterV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonfiRouterV2.getPoolAddress(self, token0: address, token1: address): address {
    return net.runGetMethod(self.address, "get_pool_address", [token0, token1]);
}

fun StonfiRouterV2.getVaultAddress(self, user: address, token: address): address {
    return net.runGetMethod(self.address, "get_vault_address", [user, token]);
}

fun StonfiRouterV2.getRouterData(self): GetRouterDataReply {
    return net.runGetMethod(self.address, "get_router_data");
}

fun StonfiRouterV2.getUpgradedPoolCode(self): cell {
    return net.runGetMethod(self.address, "get_upgraded_pool_code");
}

fun StonfiRouterV2.getRouterVersion(self): GetRouterVersionReply {
    return net.runGetMethod(self.address, "get_router_version");
}
