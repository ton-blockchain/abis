/// Auto-generated wrapper for contract 'StonfiLpAccountV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonfi_lp_account_v2.types"

/// Storage `StonfiLpAccountV2Storage` is defined in `../types/stonfi_lp_account_v2.types`
struct StonfiLpAccountV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonfiLpAccountV2.fromStorage(
    storage: StonfiLpAccountV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonfiLpAccountV2 {
    val stateInit = ContractState {
        code: build("StonfiLpAccountV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonfiLpAccountV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonfiLpAccountV2.fromAddress(address: address): StonfiLpAccountV2 {
    return StonfiLpAccountV2 { address };
}

/// Deploys the contract to the blockchain
fun StonfiLpAccountV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV2.sendStonfiLpAccountV2AddLiquidity(
    self,
    from: address,
    queryId: uint64,
    newAmount0: coins,
    newAmount1: coins,
    minLpOut: coins,
    fwdAmount: coins,
    bothPositive: uint1,
    toUser: address,
    customPayloadCs: cell?,
    additionalFields: StonfiLpAccountV2RefundExcessFields,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV2AddLiquidity {
            queryId,
            newAmount0,
            newAmount1,
            minLpOut,
            fwdAmount,
            bothPositive,
            toUser,
            customPayloadCs,
            additionalFields: additionalFields.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV2.sendStonfiLpAccountV2RefundMe(
    self,
    from: address,
    queryId: uint64,
    leftMaybePayload: cell?,
    rightMaybePayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV2RefundMe { queryId, leftMaybePayload, rightMaybePayload },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV2.sendStonfiLpAccountV2DirectAddLiquidity(
    self,
    from: address,
    queryId: uint64,
    am0: coins,
    am1: coins,
    minLpOut: coins,
    fwdAmount: coins,
    toUser: address,
    customPayloadCs: cell?,
    additionalFields: StonfiLpAccountV2RefundExcessFields,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV2DirectAddLiquidity {
            queryId,
            am0,
            am1,
            minLpOut,
            fwdAmount,
            toUser,
            customPayloadCs,
            additionalFields: additionalFields.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV2.sendStonfiLpAccountV2ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV2ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV2.sendStonfiLpAccountV2GetterLpAccountDataRequest(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiLpAccountV2GetterLpAccountDataRequest { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonfiLpAccountV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonfiLpAccountV2.getLpAccountData(self): StonfiLpAccountV2DataReply {
    return net.runGetMethod(self.address, "get_lp_account_data");
}
