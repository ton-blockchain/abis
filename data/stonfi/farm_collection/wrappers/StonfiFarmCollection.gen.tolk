/// Auto-generated wrapper for contract 'StonfiFarmCollection'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stonfi_farm_collection.types"

/// Storage `StonfiFarmCollectionStorage` is defined in `../types/stonfi_farm_collection.types`
struct StonfiFarmCollection {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StonfiFarmCollection.fromStorage(
    storage: StonfiFarmCollectionStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StonfiFarmCollection {
    val stateInit = ContractState {
        code: build("StonfiFarmCollection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StonfiFarmCollection { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StonfiFarmCollection.fromAddress(address: address): StonfiFarmCollection {
    return StonfiFarmCollection { address };
}

/// Deploys the contract to the blockchain
fun StonfiFarmCollection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionUnstakeRequest(
    self,
    from: address,
    queryId: uint64,
    itemIndex: uint64,
    stakeDate: uint64,
    stakedTokens: coins,
    ownerAddress: address,
    claimedPerUnitRoot: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionUnstakeRequest {
            queryId,
            itemIndex,
            stakeDate,
            stakedTokens,
            ownerAddress,
            claimedPerUnitRoot,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionClaimRequest(
    self,
    from: address,
    queryId: uint64,
    itemIndex: uint64,
    stakedTokens: coins,
    ownerAddress: address,
    claimAll: bool,
    poolIndex: uint8,
    claimedPerUnitRoot: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionClaimRequest {
            queryId,
            itemIndex,
            stakedTokens,
            ownerAddress,
            claimAll,
            poolIndex,
            claimedPerUnitRoot,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionJettonTransferNotification {
            queryId,
            amount,
            senderAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionWithdrawRewards(
    self,
    from: address,
    queryId: uint64,
    poolIndex: uint8,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionWithdrawRewards { queryId, poolIndex, amount },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionInitialize(
    self,
    from: address,
    queryId: uint64,
    stakingTokenWallet: address,
    canChangeCustodian: bool,
    canSendRawMsg: bool,
    minStakeTime: uint64,
    custodianAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionInitialize {
            queryId,
            stakingTokenWallet,
            canChangeCustodian,
            canSendRawMsg,
            minStakeTime,
            custodianAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionCreatePool(
    self,
    from: address,
    queryId: uint64,
    rewardTokenWallet: address,
    rewardsPer24h: coins,
    unrestrictedDepositRewards: bool,
    canChangeFee: bool,
    adminFee: uint16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionCreatePool {
            queryId,
            rewardTokenWallet,
            rewardsPer24h,
            unrestrictedDepositRewards,
            canChangeFee,
            adminFee,
        },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionUpdateStakeTime(
    self,
    from: address,
    queryId: uint64,
    minStakeTime: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionUpdateStakeTime { queryId, minStakeTime },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionUpdateAccruedValues(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionUpdateAccruedValues { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionChangeStatus(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionChangeStatus { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionChangeRate(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionChangeRate { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionChangePoolStatus(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionChangePoolStatus { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionChangeFee(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionChangeFee { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionClaimFee(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionClaimFee { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionChangeCustodian(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionChangeCustodian { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionCodeUpgrade { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionScheduleRawMessage(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionScheduleRawMessage { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionConfirmChangeCustodian(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionConfirmChangeCustodian { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionRejectChangeCustodian(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionRejectChangeCustodian { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionConfirmRawMessage(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionConfirmRawMessage { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionRejectRawMessage(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionRejectRawMessage { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionConfirmCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionConfirmCodeUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionRejectCodeUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionRejectCodeUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.sendStonfiFarmCollectionResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StonfiFarmCollectionResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StonfiFarmCollection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StonfiFarmCollection.getVersion(self): StonfiFarmCollectionVersionReply {
    return net.runGetMethod(self.address, "get_version");
}

fun StonfiFarmCollection.getFarmingMinterData(self): StonfiFarmCollectionFarmingDataReply {
    return net.runGetMethod(self.address, "get_farming_minter_data");
}

fun StonfiFarmCollection.getNftContent(self, itemIndex: int, individualNftContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [itemIndex, individualNftContent]);
}

fun StonfiFarmCollection.getNftAddressByIndex(self, itemIndex: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [itemIndex]);
}

fun StonfiFarmCollection.getNftItemCode(self): cell {
    return net.runGetMethod(self.address, "get_nft_item_code");
}

fun StonfiFarmCollection.getCollectionData(self): StonfiFarmCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun StonfiFarmCollection.getPendingData(self): StonfiFarmCollectionPendingDataReply {
    return net.runGetMethod(self.address, "get_pending_data");
}
