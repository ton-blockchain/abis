/// Auto-generated wrapper for contract 'MyTonWallet2fa'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/mytonwallet_2fa.types"

/// Storage `MyTonWallet2faStorage` is defined in `../types/mytonwallet_2fa.types`
struct MyTonWallet2fa {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MyTonWallet2fa.fromStorage(
    storage: MyTonWallet2faStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MyTonWallet2fa {
    val stateInit = ContractState {
        code: build("MyTonWallet2fa"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MyTonWallet2fa { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MyTonWallet2fa.fromAddress(address: address): MyTonWallet2fa {
    return MyTonWallet2fa { address };
}

/// Deploys the contract to the blockchain
fun MyTonWallet2fa.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MyTonWallet2fa.sendMyTonWallet2faInstall(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MyTonWallet2faInstall {},
    });
    return net.send(from, genericMsg);
}

fun MyTonWallet2fa.sendMyTonWallet2faInternalSigned(
    self,
    from: address,
    seedSignature: MyTonWallet2faSeedSignature,
    authDate: MyTonWallet2faAuthDate,
    payload: MyTonWallet2faSignedPayload,
    telegramSignature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MyTonWallet2faInternalSigned {
            seedSignature: seedSignature.toCell(),
            authDate: authDate.toCell(),
            payload,
            telegramSignature,
        },
    });
    return net.send(from, genericMsg);
}

fun MyTonWallet2fa.sendMyTonWallet2faConfirmRemoval(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MyTonWallet2faConfirmRemoval {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MyTonWallet2fa.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MyTonWallet2fa.sendExternalMyTonWallet2faSignedExternal(
    self,
    seedSignature: MyTonWallet2faSeedSignature,
    authDate: MyTonWallet2faAuthDate,
    payload: MyTonWallet2faSignedPayload,
    telegramSignature: bits512,
): ExternalSendResult {
    val body = MyTonWallet2faSignedExternal {
        seedSignature: seedSignature.toCell(),
        authDate: authDate.toCell(),
        payload,
        telegramSignature,
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun MyTonWallet2fa.sendExternalMyTonWallet2faSeedExternal(
    self,
    payload: MyTonWallet2faSeedPayload,
    signature: bits512,
): ExternalSendResult {
    val body = MyTonWallet2faSeedExternal { payload, signature };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun MyTonWallet2fa.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun MyTonWallet2fa.getRecoveryState(self): MyTonWallet2faRecoveryStateReply {
    return net.runGetMethod(self.address, "get_recovery_state");
}

fun MyTonWallet2fa.getSeqno(self): uint32 {
    return net.runGetMethod(self.address, "get_seqno");
}

fun MyTonWallet2fa.getTelegramId(self): int {
    return net.runGetMethod(self.address, "get_telegram_id");
}
