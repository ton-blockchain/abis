/// Auto-generated wrapper for contract 'Tonkeeper2fa'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonkeeper_2fa.types"

/// Storage `Tonkeeper2faDeploymentStorage` is defined in `../types/tonkeeper_2fa.types`
struct Tonkeeper2fa {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Tonkeeper2fa.fromStorage(
    storage: Tonkeeper2faDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Tonkeeper2fa {
    val stateInit = ContractState {
        code: build("Tonkeeper2fa"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Tonkeeper2fa { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Tonkeeper2fa.fromAddress(address: address): Tonkeeper2fa {
    return Tonkeeper2fa { address };
}

/// Deploys the contract to the blockchain
fun Tonkeeper2fa.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Tonkeeper2fa.sendTonkeeper2faInstall(
    self,
    from: address,
    rootCaPubkey: uint256,
    seedPubkey: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Tonkeeper2faInstall { rootCaPubkey, seedPubkey },
    });
    return net.send(from, genericMsg);
}

fun Tonkeeper2fa.sendTonkeeper2faInternalSigned(
    self,
    from: address,
    certificate: Tonkeeper2faCertificate,
    seedSignature: Tonkeeper2faRefWithSeedSignature,
    payload: Tonkeeper2faSigned2faPayload,
    certificateSignature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Tonkeeper2faInternalSigned {
            certificate: certificate.toCell(),
            seedSignature: seedSignature.toCell(),
            payload,
            certificateSignature,
        },
    });
    return net.send(from, genericMsg);
}

fun Tonkeeper2fa.sendTonkeeper2faConfirmRemoval(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Tonkeeper2faConfirmRemoval {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Tonkeeper2fa.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Tonkeeper2fa.sendExternalTonkeeper2faSigned2faExternal(
    self,
    certificate: Tonkeeper2faCertificate,
    seedSignature: Tonkeeper2faRefWithSeedSignature,
    payload: Tonkeeper2faSigned2faPayload,
    certificateSignature: bits512,
): ExternalSendResult {
    val body = Tonkeeper2faSigned2faExternal {
        certificate: certificate.toCell(),
        seedSignature: seedSignature.toCell(),
        payload,
        certificateSignature,
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun Tonkeeper2fa.sendExternalTonkeeper2faSignedSeedExternal(
    self,
    payload: Tonkeeper2faSeedPayload,
    seedSignature: bits512,
): ExternalSendResult {
    val body = Tonkeeper2faSignedSeedExternal { payload, seedSignature };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun Tonkeeper2fa.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun Tonkeeper2fa.getSeqno(self): uint32 {
    return net.runGetMethod(self.address, "get_seqno");
}

fun Tonkeeper2fa.getWalletAddr(self): address {
    return net.runGetMethod(self.address, "get_wallet_addr");
}

fun Tonkeeper2fa.getRootPubkey(self): uint256 {
    return net.runGetMethod(self.address, "get_root_pubkey");
}

fun Tonkeeper2fa.getSeedPubkey(self): uint256 {
    return net.runGetMethod(self.address, "get_seed_pubkey");
}

fun Tonkeeper2fa.getDelegationState(self): Tonkeeper2faDelegationStateReply {
    return net.runGetMethod(self.address, "get_delegation_state");
}

fun Tonkeeper2fa.getEstimatedAttachedValue(
    self,
    forwardMsg: cell,
    msgActions: int,
    extendedActions: int,
): coins {
    return net.runGetMethod(
        self.address,
        "get_estimated_attached_value",
        [forwardMsg, msgActions, extendedActions],
    );
}
