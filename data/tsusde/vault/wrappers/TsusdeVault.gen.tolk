/// Auto-generated wrapper for contract 'TsusdeVault'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tsusde_vault.types"

/// Storage `TsusdeVaultStorage` is defined in `../types/tsusde_vault.types`
struct TsusdeVault {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TsusdeVault.fromStorage(
    storage: TsusdeVaultStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TsusdeVault {
    val stateInit = ContractState {
        code: build("TsusdeVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TsusdeVault { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TsusdeVault.fromAddress(address: address): TsusdeVault {
    return TsusdeVault { address };
}

/// Deploys the contract to the blockchain
fun TsusdeVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultInitialize(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultInitializationPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultInitialize {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultSetMintNanos(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultCoinsPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultSetMintNanos {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultSetRewarderAddress(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultAddressPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultSetRewarderAddress {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultClaimOwnership(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultClaimOwnership { queryId, donationNanos, origin, payload },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultTransferOwnership(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultAddressPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultTransferOwnership {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultUpgradeCode(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultMdObject,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultUpgradeCode {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultSetAssetWalletAddress(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultAddressPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultSetAssetWalletAddress {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultSetShareMinterAddress(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultAddressPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultSetShareMinterAddress {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultSetCooldownDuration(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultCooldownPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultSetCooldownDuration {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultSetVestingDuration(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    payload: TsusdeVaultVestingPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultSetVestingDuration {
            queryId,
            donationNanos,
            origin,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.sendTsusdeVaultEventMessage(
    self,
    from: address,
    queryId: uint64,
    donationNanos: coins,
    origin: address,
    event: TsusdeVaultEvent,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsusdeVaultEventMessage {
            queryId,
            donationNanos,
            origin,
            event: event.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TsusdeVault.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TsusdeVault.getMintNanos(self): int {
    return net.runGetMethod(self.address, "getMintNanos");
}

fun TsusdeVault.getUnvestedAmount(self): int {
    return net.runGetMethod(self.address, "getUnvestedAmount");
}

fun TsusdeVault.getTotalAssets(self): int {
    return net.runGetMethod(self.address, "getTotalAssets");
}

fun TsusdeVault.getTotalShares(self): int {
    return net.runGetMethod(self.address, "getTotalShares");
}

fun TsusdeVault.convertToShares(self, amountAssets: int): int {
    return net.runGetMethod(self.address, "convertToShares", [amountAssets]);
}

fun TsusdeVault.convertToAssets(self, amountShares: int): int {
    return net.runGetMethod(self.address, "convertToAssets", [amountShares]);
}
