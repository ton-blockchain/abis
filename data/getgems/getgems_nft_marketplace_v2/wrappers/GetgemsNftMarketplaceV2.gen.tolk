/// Auto-generated wrapper for contract 'GetgemsNftMarketplaceV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/getgems_nft_marketplace_v2.types"

/// Storage `GetgemsNftMarketplaceV2Storage` is defined in `../types/getgems_nft_marketplace_v2.types`
struct GetgemsNftMarketplaceV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GetgemsNftMarketplaceV2.fromStorage(
    storage: GetgemsNftMarketplaceV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GetgemsNftMarketplaceV2 {
    val stateInit = ContractState {
        code: build("GetgemsNftMarketplaceV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GetgemsNftMarketplaceV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GetgemsNftMarketplaceV2.fromAddress(address: address): GetgemsNftMarketplaceV2 {
    return GetgemsNftMarketplaceV2 { address };
}

/// Deploys the contract to the blockchain
fun GetgemsNftMarketplaceV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftMarketplaceV2.sendGetgemsNftMarketplaceV2DeploySignedSale(
    self,
    from: address,
    signature: bits512,
    stateInit: StateInit,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftMarketplaceV2DeploySignedSale {
            signature,
            stateInit: stateInit.toCell(),
            body,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GetgemsNftMarketplaceV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftMarketplaceV2.sendExternalGetgemsNftMarketplaceV2SignedExternal(
    self,
    signature: bits512,
    subwalletId: uint32,
    validUntil: uint32,
    seqno: uint32,
    messages: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = GetgemsNftMarketplaceV2SignedExternal {
        signature,
        subwalletId,
        validUntil,
        seqno,
        messages,
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun GetgemsNftMarketplaceV2.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun GetgemsNftMarketplaceV2.getPublicKey(self): int {
    return net.runGetMethod(self.address, "get_public_key");
}

fun GetgemsNftMarketplaceV2.seqno(self): uint32 {
    return net.runGetMethod(self.address, "seqno");
}
