/// Auto-generated wrapper for contract 'GetgemsNftFixpriceSaleV3R2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/getgems_nft_fixprice_sale_v3r2.types"

/// Storage `GetgemsNftFixpriceSaleV3R2Storage` is defined in `../types/getgems_nft_fixprice_sale_v3r2.types`
struct GetgemsNftFixpriceSaleV3R2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GetgemsNftFixpriceSaleV3R2.fromStorage(
    storage: GetgemsNftFixpriceSaleV3R2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GetgemsNftFixpriceSaleV3R2 {
    val stateInit = ContractState {
        code: build("GetgemsNftFixpriceSaleV3R2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GetgemsNftFixpriceSaleV3R2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GetgemsNftFixpriceSaleV3R2.fromAddress(address: address): GetgemsNftFixpriceSaleV3R2 {
    return GetgemsNftFixpriceSaleV3R2 { address };
}

/// Deploys the contract to the blockchain
fun GetgemsNftFixpriceSaleV3R2.deploy(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendGetgemsNftFixpriceSaleV3R2ZeroOpcodeBody(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3R2ZeroOpcodeBody { tail },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendGetgemsNftFixpriceSaleV3R2OwnershipAssigned(
    self,
    from: address,
    queryId: uint64,
    previousOwnerAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3R2OwnershipAssigned {
            queryId,
            previousOwnerAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendGetgemsNftFixpriceSaleV3R2AcceptCoins(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3R2AcceptCoins { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendGetgemsNftFixpriceSaleV3R2Buy(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3R2Buy { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendGetgemsNftFixpriceSaleV3R2CancelSale(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3R2CancelSale { queryId },
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendGetgemsNftFixpriceSaleV3R2ResendRawMessage(
    self,
    from: address,
    queryId: uint64,
    resendEnvelope: GetgemsNftFixpriceSaleV3R2ResendEnvelope,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GetgemsNftFixpriceSaleV3R2ResendRawMessage {
            queryId,
            resendEnvelope: resendEnvelope.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GetgemsNftFixpriceSaleV3R2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GetgemsNftFixpriceSaleV3R2.sendExternalGetgemsNftFixpriceSaleV3R2ExternalDeploy(
    self,
    ignoredBody: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = GetgemsNftFixpriceSaleV3R2ExternalDeploy { ignoredBody };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun GetgemsNftFixpriceSaleV3R2.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun GetgemsNftFixpriceSaleV3R2.getSaleData(self): GetgemsNftFixpriceSaleV3R2SaleDataReply {
    return net.runGetMethod(self.address, "get_sale_data");
}
