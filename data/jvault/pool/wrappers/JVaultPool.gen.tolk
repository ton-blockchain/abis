/// Auto-generated wrapper for contract 'JVaultPool'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/jvault_pool.types"

/// Storage `JVaultPoolStorage` is defined in `../types/jvault_pool.types`
struct JVaultPool {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun JVaultPool.fromStorage(
    storage: JVaultPoolStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): JVaultPool {
    val stateInit = ContractState {
        code: build("JVaultPool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return JVaultPool { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun JVaultPool.fromAddress(address: address): JVaultPool {
    return JVaultPool { address };
}

/// Deploys the contract to the blockchain
fun JVaultPool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolAddRewardJettons(
    self,
    from: address,
    queryId: uint64,
    wallets: map<address, bool>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolAddRewardJettons { queryId, wallets },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolClaimCommissions(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolClaimCommissions { queryId },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolChangeContent(
    self,
    from: address,
    queryId: uint64,
    content: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolChangeContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolTakeWalletAddress(
    self,
    from: address,
    queryId: uint64,
    walletAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolTakeWalletAddress { queryId, walletAddress },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolGetStorageData(
    self,
    from: address,
    queryId: uint64,
    responseAddress: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolGetStorageData { queryId, responseAddress, tail },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolWithdrawTon(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolWithdrawTon { queryId },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolWithdrawJetton(
    self,
    from: address,
    queryId: uint64,
    jettonWalletAddress: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolWithdrawJetton { queryId, jettonWalletAddress, amount },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolDeactivateWallet(
    self,
    from: address,
    queryId: uint64,
    walletAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolDeactivateWallet { queryId, walletAddress },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolSendAnyMessage(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    payload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolSendAnyMessage { queryId, destination, payload },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolSetCode(
    self,
    from: address,
    queryId: uint64,
    code: cell,
    optionalData: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolSetCode { queryId, code, optionalData },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolSendReferrerRewards(
    self,
    from: address,
    queryId: uint64,
    referrerAddress: address,
    inviteesBalance: coins,
    userRewards: map<address, JVaultPoolUserRewardInfo>,
    jettonsToClaim: map<address, bool>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolSendReferrerRewards {
            queryId,
            referrerAddress,
            inviteesBalance,
            userRewards,
            jettonsToClaim,
        },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolRequestUpdateReferrer(
    self,
    from: address,
    queryId: uint64,
    referrerAddress: address,
    inviteesBalance: coins,
    balanceChange: int121,
    userRewards: map<address, JVaultPoolUserRewardInfo>,
    responseAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolRequestUpdateReferrer {
            queryId,
            referrerAddress,
            inviteesBalance,
            balanceChange,
            userRewards,
            responseAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolCancelStake(
    self,
    from: address,
    queryId: uint64,
    stakeOwnerAddress: address,
    lockPeriod: uint32,
    stakedAmount: coins,
    depositCommission: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolCancelStake {
            queryId,
            stakeOwnerAddress,
            lockPeriod,
            stakedAmount,
            depositCommission,
        },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolRequestUpdateRewards(
    self,
    from: address,
    queryId: uint64,
    stakeOwnerAddress: address,
    lockPeriod: uint32,
    userJettonBalance: coins,
    tvlChange: int121,
    userRewards: map<address, JVaultPoolUserRewardInfo>,
    jettonsToUnstake: coins,
    commission: coins,
    forwardedTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolRequestUpdateRewards {
            queryId,
            stakeOwnerAddress,
            lockPeriod,
            userJettonBalance,
            tvlChange,
            userRewards,
            jettonsToUnstake,
            commission,
            forwardedTail,
        },
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.sendJVaultPoolSendClaimedRewards(
    self,
    from: address,
    queryId: uint64,
    stakeOwnerAddress: address,
    lockPeriod: uint32,
    userJettonBalance: coins,
    userRewards: map<address, JVaultPoolUserRewardInfo>,
    jettonsToClaim: map<address, bool>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JVaultPoolSendClaimedRewards {
            queryId,
            stakeOwnerAddress,
            lockPeriod,
            userJettonBalance,
            userRewards,
            jettonsToClaim,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun JVaultPool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun JVaultPool.getNftData(self): JVaultPoolNftData {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun JVaultPool.getAuthorityAddress(self): any_address {
    return net.runGetMethod(self.address, "get_authority_address");
}

fun JVaultPool.getRevokedTime(self): int {
    return net.runGetMethod(self.address, "get_revoked_time");
}

fun JVaultPool.getStorageData(self): JVaultPoolStorageData {
    return net.runGetMethod(self.address, "get_storage_data");
}

fun JVaultPool.getStakeWalletAddress(self, ownerAddress: address, lockPeriod: int): any_address {
    return net.runGetMethod(self.address, "get_stake_wallet_address", [ownerAddress, lockPeriod]);
}
