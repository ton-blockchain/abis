/// Auto-generated wrapper for contract 'BemoFinancial'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/bemo_financial.types"

/// Storage `BemoFinancialStorage` is defined in `../types/bemo_financial.types`
struct BemoFinancial {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun BemoFinancial.fromStorage(
    storage: BemoFinancialStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): BemoFinancial {
    val stateInit = ContractState {
        code: build("BemoFinancial"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return BemoFinancial { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun BemoFinancial.fromAddress(address: address): BemoFinancial {
    return BemoFinancial { address };
}

/// Deploys the contract to the blockchain
fun BemoFinancial.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialStake(
    self,
    from: address,
    queryId: uint64,
    forwardTonAmount: coins,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialStake { queryId, forwardTonAmount, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialDepositWithCommission(
    self,
    from: address,
    tonAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialDepositWithCommission { tonAmount },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    ownerAddress: address,
    receiverAddress: address,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialBurnNotification {
            queryId,
            amount,
            ownerAddress,
            receiverAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialUnstake(
    self,
    from: address,
    index: uint64,
    owner: address,
    tonAmount: coins,
    jettonAmount: coins,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialUnstake { index, owner, tonAmount, jettonAmount, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialProvideLegacyQuote(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialProvideLegacyQuote { queryId },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialProvideCurrentQuote(
    self,
    from: address,
    queryId: uint64,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialProvideCurrentQuote { queryId, customPayload },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialSetAdminAddress(
    self,
    from: address,
    adminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialSetAdminAddress { adminAddress },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialSetTransactionAddress(
    self,
    from: address,
    transactionAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialSetTransactionAddress { transactionAddress },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialSetContent(
    self,
    from: address,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialSetContent { content },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialSetCommissionFactor(
    self,
    from: address,
    commissionFactor: uint16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialSetCommissionFactor { commissionFactor },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialSetCommissionAddress(
    self,
    from: address,
    commissionAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialSetCommissionAddress { commissionAddress },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialOperationalTransfer(
    self,
    from: address,
    destination: address,
    amount: coins,
    body: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialOperationalTransfer { destination, amount, body },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialWithdrawCommission(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialWithdrawCommission {},
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialNoop(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialNoop {},
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialRefreshLockupEpoch(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialRefreshLockupEpoch {},
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialSetCode(
    self,
    from: address,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialSetCode { code },
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.sendBemoFinancialAdminJettonTransferCommand(
    self,
    from: address,
    walletAddress: address,
    destination: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoFinancialAdminJettonTransferCommand { walletAddress, destination, amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun BemoFinancial.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun BemoFinancial.getFullData(self): BemoFinancialFullDataReply {
    return net.runGetMethod(self.address, "get_full_data");
}

fun BemoFinancial.getJettonData(self): BemoFinancialJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun BemoFinancial.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun BemoFinancial.getUnstakeRequestAddress(self, index: uint64): address {
    return net.runGetMethod(self.address, "get_unstake_request_address", [index]);
}

fun BemoFinancial.getLockupPeriod(self): uint32 {
    return net.runGetMethod(self.address, "get_lockup_period");
}
