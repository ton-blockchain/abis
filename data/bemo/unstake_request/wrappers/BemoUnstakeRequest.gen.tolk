/// Auto-generated wrapper for contract 'BemoUnstakeRequest'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/bemo_unstake_request.types"

/// Storage `BemoUnstakeRequestStorage` is defined in `../types/bemo_unstake_request.types`
struct BemoUnstakeRequest {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun BemoUnstakeRequest.fromStorage(
    storage: BemoUnstakeRequestStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): BemoUnstakeRequest {
    val stateInit = ContractState {
        code: build("BemoUnstakeRequest"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return BemoUnstakeRequest { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun BemoUnstakeRequest.fromAddress(address: address): BemoUnstakeRequest {
    return BemoUnstakeRequest { address };
}

/// Deploys the contract to the blockchain
fun BemoUnstakeRequest.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun BemoUnstakeRequest.sendBemoUnstakeRequestDeploy(
    self,
    from: address,
    ownerAddress: address,
    withdrawTonAmount: coins,
    withdrawJettonAmount: coins,
    forwardPayload: cell?,
    lockupTimestamp: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoUnstakeRequestDeploy {
            ownerAddress,
            withdrawTonAmount,
            withdrawJettonAmount,
            forwardPayload,
            lockupTimestamp,
        },
    });
    return net.send(from, genericMsg);
}

fun BemoUnstakeRequest.sendBemoUnstakeRequestReturn(
    self,
    from: address,
    lockupTimestamp: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: BemoUnstakeRequestReturn { lockupTimestamp },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun BemoUnstakeRequest.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun BemoUnstakeRequest.getUnstakeData(self): BemoUnstakeRequestDataReply {
    return net.runGetMethod(self.address, "get_unstake_data");
}
