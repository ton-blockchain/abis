/// Auto-generated wrapper for contract 'SignedCrossDexExecutor'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/signed_cross_dex_executor.types"

/// Storage `SignedCrossDexExecutorStorage` is defined in `../types/signed_cross_dex_executor.types`
struct SignedCrossDexExecutor {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun SignedCrossDexExecutor.fromStorage(
    storage: SignedCrossDexExecutorStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): SignedCrossDexExecutor {
    val stateInit = ContractState {
        code: build("SignedCrossDexExecutor"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return SignedCrossDexExecutor { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun SignedCrossDexExecutor.fromAddress(address: address): SignedCrossDexExecutor {
    return SignedCrossDexExecutor { address };
}

/// Deploys the contract to the blockchain
fun SignedCrossDexExecutor.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun SignedCrossDexExecutor.sendSignedCrossDexJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SignedCrossDexJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun SignedCrossDexExecutor.sendSignedCrossDexExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SignedCrossDexExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun SignedCrossDexExecutor.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun SignedCrossDexExecutor.sendExternalSignedCrossDexExecutorSignedRequest(
    self,
    signature: bits512,
    validUntil: uint32,
    queryId: uint64,
    sendMode: uint8,
    replaceContinuation: bool,
    payload: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = SignedCrossDexExecutorSignedRequest {
        signature,
        validUntil,
        queryId,
        sendMode,
        replaceContinuation,
        payload,
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun SignedCrossDexExecutor.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun SignedCrossDexExecutor.seqno(self): int {
    return net.runGetMethod(self.address, "seqno");
}

fun SignedCrossDexExecutor.getMethod995190ybc(self): int {
    return net.runGetMethod(self.address, "get_method_99519_0ybc");
}

fun SignedCrossDexExecutor.processed(self, queryId: uint64): int {
    return net.runGetMethod(self.address, "processed", [queryId]);
}
