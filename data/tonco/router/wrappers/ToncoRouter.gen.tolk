/// Auto-generated wrapper for contract 'ToncoRouter'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonco_router.types"

/// Storage `ToncoRouterStorage` is defined in `../types/tonco_router.types`
struct ToncoRouter {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun ToncoRouter.fromStorage(
    storage: ToncoRouterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): ToncoRouter {
    val stateInit = ContractState {
        code: build("ToncoRouter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return ToncoRouter { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun ToncoRouter.fromAddress(address: address): ToncoRouter {
    return ToncoRouter { address };
}

/// Deploys the contract to the blockchain
fun ToncoRouter.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendRouterv3CreatePool(
    self,
    from: address,
    queryId: uint64,
    jettonWallet0: address,
    jettonWallet1: address,
    tickSpacing: int24,
    initialPriceX96: uint160,
    activatePool: bool,
    protocolFee: uint16,
    lpFeeBase: uint16,
    lpFeeCurrent: uint16,
    nftv3Content: cell,
    nftv3itemContent: cell,
    minterCell: Routerv3CreatePoolMinterCell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Routerv3CreatePool {
            queryId,
            jettonWallet0,
            jettonWallet1,
            tickSpacing,
            initialPriceX96,
            activatePool,
            protocolFee,
            lpFeeBase,
            lpFeeCurrent,
            nftv3Content,
            nftv3itemContent,
            minterCell: minterCell.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    fromUser: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonTransferNotification { queryId, jettonAmount, fromUser, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendRouterv3PayTo(
    self,
    from: address,
    queryId: uint64,
    receiver0: address,
    receiver1: address,
    payTo: ToncoPayToPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Routerv3PayTo { queryId, receiver0, receiver1, payTo },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendRouterv3ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Routerv3ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendRouterv3ChangeAdminStart(
    self,
    from: address,
    queryId: uint64,
    hasNewAdmin: bool,
    newAdmin: address,
    hasNewFlags: bool,
    newFlags: uint64,
    newCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Routerv3ChangeAdminStart {
            queryId,
            hasNewAdmin,
            newAdmin,
            hasNewFlags,
            newFlags,
            newCode,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendRouterv3ChangeAdminCommit(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Routerv3ChangeAdminCommit { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.sendRouterv3ChangeParams(
    self,
    from: address,
    queryId: uint64,
    hasNewPoolFactory: bool,
    newPoolFactory: address,
    hasNewPoolAdmin: bool,
    newPoolAdmin: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Routerv3ChangeParams {
            queryId,
            hasNewPoolFactory,
            newPoolFactory,
            hasNewPoolAdmin,
            newPoolAdmin,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun ToncoRouter.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun ToncoRouter.getRouterState(self): GetRouterStateReply {
    return net.runGetMethod(self.address, "getRouterState");
}

fun ToncoRouter.getPoolAddress(self, jettonWallet0: address, jettonWallet1: address): address {
    return net.runGetMethod(self.address, "getPoolAddress", [jettonWallet0, jettonWallet1]);
}

fun ToncoRouter.getChildContracts(self): GetChildContractsReply {
    return net.runGetMethod(self.address, "getChildContracts");
}

fun ToncoRouter.getPoolInitialData(self, jettonWallet0: address, jettonWallet1: address): cell {
    return net.runGetMethod(self.address, "getPoolInitialData", [jettonWallet0, jettonWallet1]);
}

fun ToncoRouter.getPoolStateInit(self, jettonWallet0: address, jettonWallet1: address): cell {
    return net.runGetMethod(self.address, "getPoolStateInit", [jettonWallet0, jettonWallet1]);
}
