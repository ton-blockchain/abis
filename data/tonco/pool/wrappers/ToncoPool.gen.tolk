/// Auto-generated wrapper for contract 'ToncoPool'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonco_pool.types"

/// Storage `ToncoPoolStorage` is defined in `../types/tonco_pool.types`
struct ToncoPool {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun ToncoPool.fromStorage(
    storage: ToncoPoolStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): ToncoPool {
    val stateInit = ContractState {
        code: build("ToncoPool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return ToncoPool { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun ToncoPool.fromAddress(address: address): ToncoPool {
    return ToncoPool { address };
}

/// Deploys the contract to the blockchain
fun ToncoPool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3Init(
    self,
    from: address,
    queryId: uint64,
    fromAdmin: bool,
    hasAdmin: bool,
    adminAddr: address,
    hasController: bool,
    controllerAddr: address,
    setSpacing: bool,
    tickSpacing: int24,
    setPrice: bool,
    initialPriceX96: uint160,
    setActive: bool,
    poolActive: bool,
    protocolFee: uint16,
    lpFeeBase: uint16,
    lpFeeCurrent: uint16,
    nftv3Content: cell,
    nftv3itemContent: cell,
    minterCell: Cell<Poolv3InitMinterCell>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3Init {
            queryId,
            fromAdmin,
            hasAdmin,
            adminAddr,
            hasController,
            controllerAddr,
            setSpacing,
            tickSpacing,
            setPrice,
            initialPriceX96,
            setActive,
            poolActive,
            protocolFee,
            lpFeeBase,
            lpFeeCurrent,
            nftv3Content,
            nftv3itemContent,
            minterCell,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3Lock(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3Lock { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3Unlock(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3Unlock { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3SetFee(
    self,
    from: address,
    queryId: uint64,
    protocolFee: uint16,
    lpFeeBase: uint16,
    lpFeeCurrent: uint16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3SetFee { queryId, protocolFee, lpFeeBase, lpFeeCurrent },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3CollectProtocol(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3CollectProtocol { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3StartBurn(
    self,
    from: address,
    queryId: uint64,
    burnedIndex: uint64,
    liquidity2Burn: uint128,
    tickLower: int24,
    tickUpper: int24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3StartBurn { queryId, burnedIndex, liquidity2Burn, tickLower, tickUpper },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3Burn(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    burnedIndex: uint64,
    liquidity: uint128,
    tickLower: int24,
    tickUpper: int24,
    liquidity2Burn: uint128,
    oldFeeCell: Poolv3BurnOldFeeCell,
    newFeeCell: Poolv3BurnNewFeeCell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3Burn {
            queryId,
            recipient,
            burnedIndex,
            liquidity,
            tickLower,
            tickUpper,
            liquidity2Burn,
            oldFeeCell: oldFeeCell.toCell(),
            newFeeCell: newFeeCell.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3Swap(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    sourceWallet: address,
    paramsCell: Poolv3SwapParamsCell,
    payloadsCell: Poolv3SwapPayloadsCell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3Swap {
            queryId,
            ownerAddress,
            sourceWallet,
            paramsCell: paramsCell.toCell(),
            payloadsCell: payloadsCell.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3FundAccount(
    self,
    from: address,
    queryId: uint64,
    ownerAddr: address,
    amount0: coins,
    amount1: coins,
    enough0: coins,
    enough1: coins,
    liquidity: uint128,
    tickLower: int24,
    tickUpper: int24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3FundAccount {
            queryId,
            ownerAddr,
            amount0,
            amount1,
            enough0,
            enough1,
            liquidity,
            tickLower,
            tickUpper,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.sendPoolv3Mint(
    self,
    from: address,
    queryId: uint64,
    amount0Funded: coins,
    amount1Funded: coins,
    recipient: address,
    liquidity: uint128,
    tickLower: int24,
    tickUpper: int24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Poolv3Mint {
            queryId,
            amount0Funded,
            amount1Funded,
            recipient,
            liquidity,
            tickLower,
            tickUpper,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun ToncoPool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun ToncoPool.getIsActive(self): bool {
    return net.runGetMethod(self.address, "getIsActive");
}

fun ToncoPool.getPoolStateAndConfiguration(self): GetPoolStateAndConfigurationReply {
    return net.runGetMethod(self.address, "getPoolStateAndConfiguration");
}

fun ToncoPool.getChildContracts(self): GetChildContractsReply {
    return net.runGetMethod(self.address, "getChildContracts");
}

fun ToncoPool.getAllTickInfos(self): cell {
    return net.runGetMethod(self.address, "getAllTickInfos");
}

fun ToncoPool.getTickInfosFrom(self, tickNumber: int, amount: int, back: int, full: int): tuple {
    return net.runGetMethod(self.address, "getTickInfosFrom", [tickNumber, amount, back, full]);
}

fun ToncoPool.getMaxLiquidityPerTick(self): int {
    return net.runGetMethod(self.address, "getMaxLiquidityPerTick");
}

fun ToncoPool.getCollectedFees(
    self,
    tickLower: int,
    tickUpper: int,
    posLiquidityDelta: int,
    posFeeGrowthInside0X128: int,
    posFeeGrowthInside1X128: int,
): GetCollectedFeesReply {
    return net.runGetMethod(
        self.address,
        "getCollectedFees",
        [tickLower, tickUpper, posLiquidityDelta, posFeeGrowthInside0X128, posFeeGrowthInside1X128],
    );
}

fun ToncoPool.getMintEstimate(
    self,
    tickLower: int,
    tickUpper: int,
    liquidity: int,
): GetMintEstimateReply {
    return net.runGetMethod(self.address, "getMintEstimate", [tickLower, tickUpper, liquidity]);
}

fun ToncoPool.getSwapEstimate(
    self,
    zeroForOne: int,
    amount: int,
    sqrtPriceLimitX96: int,
): GetSwapEstimateReply {
    return net.runGetMethod(
        self.address,
        "getSwapEstimate",
        [zeroForOne, amount, sqrtPriceLimitX96],
    );
}

fun ToncoPool.getSwapEstimateGas(
    self,
    zeroForOne: int,
    amount: int,
    sqrtPriceLimitX96: int,
    minOutAmount: int,
    gasLimit: int,
): GetSwapEstimateReply {
    return net.runGetMethod(
        self.address,
        "getSwapEstimateGas",
        [zeroForOne, amount, sqrtPriceLimitX96, minOutAmount, gasLimit],
    );
}

fun ToncoPool.getFeeGrowthInside(
    self,
    tickLower: int,
    tickUpper: int,
    tickCurrent: int,
    feeGrowthGlobal0X128: int,
    feeGrowthGlobal1X128: int,
): GetFeeGrowthInsideReply {
    return net.runGetMethod(
        self.address,
        "getFeeGrowthInside",
        [tickLower, tickUpper, tickCurrent, feeGrowthGlobal0X128, feeGrowthGlobal1X128],
    );
}

fun ToncoPool.getUserAccountAddress(self, userAddress: address): address {
    return net.runGetMethod(self.address, "getUserAccountAddress", [userAddress]);
}

fun ToncoPool.getCollectionData(self): GetCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun ToncoPool.getNftAddressByIndex(self, index: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [index]);
}

fun ToncoPool.getNftContent(self, index: int, nftv3itemContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [index, nftv3itemContent]);
}
