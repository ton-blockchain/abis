/// Auto-generated wrapper for contract 'ToncoAccount'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonco_account.types"

/// Storage `ToncoAccountStorage` is defined in `../types/tonco_account.types`
struct ToncoAccount {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun ToncoAccount.fromStorage(
    storage: ToncoAccountStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): ToncoAccount {
    val stateInit = ContractState {
        code: build("ToncoAccount"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return ToncoAccount { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun ToncoAccount.fromAddress(address: address): ToncoAccount {
    return ToncoAccount { address };
}

/// Deploys the contract to the blockchain
fun ToncoAccount.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoAccount.sendAccountv3AddLiquidity(
    self,
    from: address,
    queryId: uint64,
    newAmount0: coins,
    newAmount1: coins,
    newEnough0: coins,
    newEnough1: coins,
    liquidity: uint128,
    tickLower: int24,
    tickUpper: int24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Accountv3AddLiquidity {
            queryId,
            newAmount0,
            newAmount1,
            newEnough0,
            newEnough1,
            liquidity,
            tickLower,
            tickUpper,
        },
    });
    return net.send(from, genericMsg);
}

fun ToncoAccount.sendAccountv3ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Accountv3ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun ToncoAccount.sendAccountv3RefundMe(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Accountv3RefundMe { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun ToncoAccount.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun ToncoAccount.getAccountData(self): GetAccountDataReply {
    return net.runGetMethod(self.address, "get_account_data");
}
