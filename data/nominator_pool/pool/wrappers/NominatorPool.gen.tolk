/// Auto-generated wrapper for contract 'NominatorPool'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/nominator_pool.types"

/// Storage `NominatorPoolStorage` is defined in `../types/nominator_pool.types`
struct NominatorPool {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun NominatorPool.fromStorage(
    storage: NominatorPoolStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): NominatorPool {
    val stateInit = ContractState {
        code: build("NominatorPool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return NominatorPool { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun NominatorPool.fromAddress(address: address): NominatorPool {
    return NominatorPool { address };
}

/// Deploys the contract to the blockchain
fun NominatorPool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolTextCommand(
    self,
    from: address,
    action: uint8,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolTextCommand { action, tail },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolAcceptCoins(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolAcceptCoins { queryId },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolProcessWithdrawRequests(
    self,
    from: address,
    queryId: uint64,
    limit: uint8,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolProcessWithdrawRequests { queryId, limit, tail },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolEmergencyProcessWithdrawRequest(
    self,
    from: address,
    queryId: uint64,
    requestAddress: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolEmergencyProcessWithdrawRequest { queryId, requestAddress },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolValidatorDeposit(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolValidatorDeposit { queryId },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolValidatorWithdraw(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolValidatorWithdraw { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolUpdateValidatorSetHash(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolUpdateValidatorSetHash { queryId },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolCleanupVotings(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolCleanupVotings { queryId },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolRecoverStake(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolRecoverStake { queryId },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolNewStake(
    self,
    from: address,
    queryId: uint64,
    stakeValue: coins,
    validatorPubkey: bits256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddr: bits256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolNewStake {
            queryId,
            stakeValue,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddr,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolNewStakeError(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolNewStakeError { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolNewStakeOk(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolNewStakeOk { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolRecoverStakeOk(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolRecoverStakeOk { queryId },
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.sendNominatorPoolRecoverStakeError(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NominatorPoolRecoverStakeError { queryId, tail },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun NominatorPool.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun NominatorPool.getPoolData(self): NominatorPoolDataReply {
    return net.runGetMethod(self.address, "get_pool_data");
}

fun NominatorPool.hasWithdrawRequests(self): bool {
    return net.runGetMethod(self.address, "has_withdraw_requests");
}

fun NominatorPool.getNominatorData(
    self,
    nominatorAddress: uint256,
): NominatorPoolNominatorDataReply {
    return net.runGetMethod(self.address, "get_nominator_data", [nominatorAddress]);
}

fun NominatorPool.getMaxPunishment(self, stake: int): int {
    return net.runGetMethod(self.address, "get_max_punishment", [stake]);
}

fun NominatorPool.listNominators(self): lisp_list<NominatorPoolListNominatorsEntry> {
    return net.runGetMethod(self.address, "list_nominators");
}

fun NominatorPool.listVotes(self): lisp_list<NominatorPoolListVotesEntry> {
    return net.runGetMethod(self.address, "list_votes");
}

fun NominatorPool.listVoters(self, proposalHash: uint256): lisp_list<NominatorPoolListVotersEntry> {
    return net.runGetMethod(self.address, "list_voters", [proposalHash]);
}
