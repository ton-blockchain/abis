/// Auto-generated wrapper for contract 'NftV2Collection'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/nft_v2_collection.types"

/// Storage `NftV2CollectionStorage` is defined in `../types/nft_v2_collection.types`
struct NftV2Collection {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun NftV2Collection.fromStorage(
    storage: NftV2CollectionStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): NftV2Collection {
    val stateInit = ContractState {
        code: build("NftV2Collection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return NftV2Collection { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun NftV2Collection.fromAddress(address: address): NftV2Collection {
    return NftV2Collection { address };
}

/// Deploys the contract to the blockchain
fun NftV2Collection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV2Collection.sendNftV2GetRoyaltyParams(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2GetRoyaltyParams { queryId },
    });
    return net.send(from, genericMsg);
}

fun NftV2Collection.sendNftV2DeployItem(
    self,
    from: address,
    queryId: uint64,
    itemIndex: uint64,
    attachTonAmount: coins,
    initParams: NftV2CollectionItemInit,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2DeployItem {
            queryId,
            itemIndex,
            attachTonAmount,
            initParams: initParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun NftV2Collection.sendNftV2BatchDeployItems(
    self,
    from: address,
    queryId: uint64,
    deployList: map<uint64, NftV2CollectionBatchDeployItem>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2BatchDeployItems { queryId, deployList },
    });
    return net.send(from, genericMsg);
}

fun NftV2Collection.sendNftV2ChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2ChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun NftV2Collection.sendNftV2ChangeContent(
    self,
    from: address,
    queryId: uint64,
    content: NftV2CollectionContent,
    royaltyParams: NftV2RoyaltyParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2ChangeContent {
            queryId,
            content: content.toCell(),
            royaltyParams: royaltyParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun NftV2Collection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun NftV2Collection.getCollectionData(self): GetNftV2CollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun NftV2Collection.getNftAddressByIndex(self, itemIndex: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [itemIndex]);
}

fun NftV2Collection.royaltyParams(self): NftV2RoyaltyParams {
    return net.runGetMethod(self.address, "royalty_params");
}

fun NftV2Collection.getNftContent(self, itemIndex: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [itemIndex, individualContent]);
}
