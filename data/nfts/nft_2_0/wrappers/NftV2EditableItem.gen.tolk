/// Auto-generated wrapper for contract 'NftV2EditableItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/nft_v2_editable_item.types"

/// Storage `NftV2EditableItemDeploymentStorage` is defined in `../types/nft_v2_editable_item.types`
struct NftV2EditableItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun NftV2EditableItem.fromStorage(
    storage: NftV2EditableItemDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): NftV2EditableItem {
    val stateInit = ContractState {
        code: build("NftV2EditableItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return NftV2EditableItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun NftV2EditableItem.fromAddress(address: address): NftV2EditableItem {
    return NftV2EditableItem { address };
}

/// Deploys the contract to the blockchain
fun NftV2EditableItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV2EditableItem.sendNftV2EditableTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2EditableTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV2EditableItem.sendNftV2EditableGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2EditableGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

fun NftV2EditableItem.sendNftV2EditableTransferEditorship(
    self,
    from: address,
    queryId: uint64,
    newEditorAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2EditableTransferEditorship {
            queryId,
            newEditorAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV2EditableItem.sendNftV2EditableEditContent(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV2EditableEditContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun NftV2EditableItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun NftV2EditableItem.getNftData(self): GetNftV2EditableDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun NftV2EditableItem.getEditor(self): address {
    return net.runGetMethod(self.address, "get_editor");
}
