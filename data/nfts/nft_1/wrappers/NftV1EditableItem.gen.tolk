/// Auto-generated wrapper for contract 'NftV1EditableItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/nft_v1_editable_item.types"

/// Storage `NftV1EditableItemDeploymentStorage` is defined in `../types/nft_v1_editable_item.types`
struct NftV1EditableItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun NftV1EditableItem.fromStorage(
    storage: NftV1EditableItemDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): NftV1EditableItem {
    val stateInit = ContractState {
        code: build("NftV1EditableItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return NftV1EditableItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun NftV1EditableItem.fromAddress(address: address): NftV1EditableItem {
    return NftV1EditableItem { address };
}

/// Deploys the contract to the blockchain
fun NftV1EditableItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV1EditableItem.sendNftV1EditableTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1EditableTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV1EditableItem.sendNftV1EditableGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1EditableGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

fun NftV1EditableItem.sendNftV1EditableTransferEditorship(
    self,
    from: address,
    queryId: uint64,
    newEditorAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1EditableTransferEditorship {
            queryId,
            newEditorAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun NftV1EditableItem.sendNftV1EditableEditContent(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NftV1EditableEditContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun NftV1EditableItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun NftV1EditableItem.getNftData(self): GetNftV1EditableDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun NftV1EditableItem.getEditor(self): address {
    return net.runGetMethod(self.address, "get_editor");
}
