/// Auto-generated wrapper for contract 'TonstakersPool'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonstakers_pool.types"

/// Storage `TonstakersPoolStorage` is defined in `../types/tonstakers_pool.types`
struct TonstakersPool {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TonstakersPool.fromStorage(
    storage: TonstakersPoolStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TonstakersPool {
    val stateInit = ContractState {
        code: build("TonstakersPool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TonstakersPool { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TonstakersPool.fromAddress(address: address): TonstakersPool {
    return TonstakersPool { address };
}

/// Deploys the contract to the blockchain
fun TonstakersPool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolDeposit(
    self,
    from: address,
    queryId: uint64,
    partnerCode: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolDeposit { queryId, partnerCode },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolWithdraw(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    fromAddress: address,
    responseAddress: address,
    waitTillRoundEnd: bool,
    fillOrKill: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolWithdraw {
            queryId,
            jettonAmount,
            fromAddress,
            responseAddress,
            waitTillRoundEnd,
            fillOrKill,
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolRequestLoan(
    self,
    from: address,
    queryId: uint64,
    minLoan: coins,
    maxLoan: coins,
    maxInterest: uint24,
    controllerData: TonstakersPoolControllerData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolRequestLoan {
            queryId,
            minLoan,
            maxLoan,
            maxInterest,
            controllerData: controllerData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolLoanRepayment(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolLoanRepayment { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolTouch(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolTouch { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolDonate(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolDonate { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolDeployController(
    self,
    from: address,
    queryId: uint64,
    controllerId: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolDeployController { queryId, controllerId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolHalt(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolHalt { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSudoSendMessage(
    self,
    from: address,
    queryId: uint64,
    mode: uint8,
    message: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSudoSendMessage { queryId, mode, message },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSudoUpgrade(
    self,
    from: address,
    queryId: uint64,
    data: cell?,
    code: cell?,
    afterUpgrade: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSudoUpgrade { queryId, data, code, afterUpgrade },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSetSudoer(
    self,
    from: address,
    queryId: uint64,
    sudoer: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSetSudoer { queryId, sudoer },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolUnhalt(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolUnhalt { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolPrepareGovernanceMigration(
    self,
    from: address,
    queryId: uint64,
    governorUpdateAfter: uint48,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolPrepareGovernanceMigration { queryId, governorUpdateAfter },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSetRoles(
    self,
    from: address,
    queryId: uint64,
    governor: TonstakersPoolMaybeRoleAddress,
    interestManager: TonstakersPoolMaybeRoleAddress,
    halter: TonstakersPoolMaybeRoleAddress,
    approver: TonstakersPoolMaybeRoleAddress,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSetRoles { queryId, governor, interestManager, halter, approver },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSetDepositSettings(
    self,
    from: address,
    queryId: uint64,
    optimisticDepositWithdrawals: bool,
    depositsOpen: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSetDepositSettings {
            queryId,
            optimisticDepositWithdrawals,
            depositsOpen,
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSetGovernanceFee(
    self,
    from: address,
    queryId: uint64,
    governanceFee: uint24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSetGovernanceFee { queryId, governanceFee },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolReturnAvailableFunds(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolReturnAvailableFunds { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.sendTonstakersPoolSetInterest(
    self,
    from: address,
    queryId: uint64,
    interestRate: uint24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPoolSetInterest { queryId, interestRate },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TonstakersPool.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TonstakersPool.getPoolFullData(self): TonstakersPoolFullDataReply {
    return net.runGetMethod(self.address, "get_pool_full_data");
}

fun TonstakersPool.getPoolFullDataRaw(self): TonstakersPoolFullDataReply {
    return net.runGetMethod(self.address, "get_pool_full_data_raw");
}

fun TonstakersPool.getControllerAddress(self, controllerId: int, validator: address): address {
    return net.runGetMethod(self.address, "get_controller_address", [controllerId, validator]);
}

fun TonstakersPool.getControllerAddressLegacy(
    self,
    controllerId: int,
    workchain: int,
    addressHash: uint256,
): TonstakersPoolControllerAddressLegacyReply {
    return net.runGetMethod(
        self.address,
        "get_controller_address_legacy",
        [controllerId, workchain, addressHash],
    );
}

fun TonstakersPool.getLoan(
    self,
    controllerId: int,
    validatorAddress: address,
    prev: bool,
    update: bool,
): TonstakersPoolLoanReply {
    return net.runGetMethod(
        self.address,
        "get_loan",
        [controllerId, validatorAddress, prev, update],
    );
}

fun TonstakersPool.getControllerLoanPosition(
    self,
    controllerAddrHash: uint256,
    prev: bool,
): TonstakersPoolControllerLoanPositionReply {
    return net.runGetMethod(
        self.address,
        "get_controller_loan_position",
        [controllerAddrHash, prev],
    );
}

fun TonstakersPool.calculateLoanAmount(self, minLoan: int, maxLoan: int, maxInterest: int): int {
    return net.runGetMethod(self.address, "calculate_loan_amount", [minLoan, maxLoan, maxInterest]);
}
