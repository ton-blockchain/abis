/// Auto-generated wrapper for contract 'TonstakersPayoutItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonstakers_payout_item.types"

/// Storage `TonstakersPayoutItemDeploymentStorage` is defined in `../types/tonstakers_payout_item.types`
struct TonstakersPayoutItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TonstakersPayoutItem.fromStorage(
    storage: TonstakersPayoutItemDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TonstakersPayoutItem {
    val stateInit = ContractState {
        code: build("TonstakersPayoutItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TonstakersPayoutItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TonstakersPayoutItem.fromAddress(address: address): TonstakersPayoutItem {
    return TonstakersPayoutItem { address };
}

/// Deploys the contract to the blockchain
fun TonstakersPayoutItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPayoutItem.sendTonstakersPayoutItemInit(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    amount: coins,
    previousItemAddress: address?,
    nextItemAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPayoutItemInit {
            queryId,
            ownerAddress,
            amount,
            previousItemAddress,
            nextItemAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPayoutItem.sendTonstakersPayoutItemTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayloadPresent: bool,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPayoutItemTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayloadPresent,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPayoutItem.sendTonstakersPayoutItemBurn(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPayoutItemBurn { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonstakersPayoutItem.sendTonstakersPayoutItemGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonstakersPayoutItemGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TonstakersPayoutItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TonstakersPayoutItem.getNftData(self): TonstakersPayoutItemNftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun TonstakersPayoutItem.getBillAmount(self): int {
    return net.runGetMethod(self.address, "get_bill_amount");
}
