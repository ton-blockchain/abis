/// Auto-generated wrapper for contract 'TsTonWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/ts_ton_wallet.types"

/// Storage `TsTonWalletStorage` is defined in `../types/ts_ton_wallet.types`
struct TsTonWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TsTonWallet.fromStorage(
    storage: TsTonWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TsTonWallet {
    val stateInit = ContractState {
        code: build("TsTonWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TsTonWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TsTonWallet.fromAddress(address: address): TsTonWallet {
    return TsTonWallet { address };
}

/// Deploys the contract to the blockchain
fun TsTonWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    fromAddress: address,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletInternalTransfer {
            queryId,
            amount,
            fromAddress,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletWithdrawTons(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletWithdrawTons { queryId },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletWithdrawJettons(
    self,
    from: address,
    queryId: uint64,
    childWallet: address,
    amount: coins,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletWithdrawJettons { queryId, childWallet, amount, customPayload },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletVote(
    self,
    from: address,
    queryId: uint64,
    votingAddress: address,
    expirationDate: uint48,
    vote: bool,
    needConfirmation: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletVote { queryId, votingAddress, expirationDate, vote, needConfirmation },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletCreateVotingThroughWallet(
    self,
    from: address,
    queryId: uint64,
    votingType: uint64,
    expirationDate: uint48,
    proposal: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletCreateVotingThroughWallet {
            queryId,
            votingType,
            expirationDate,
            proposal,
        },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletConfirmVoting(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletConfirmVoting { queryId },
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.sendTsTonWalletVotingCreated(
    self,
    from: address,
    queryId: uint64,
    votingAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TsTonWalletVotingCreated { queryId, votingAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TsTonWallet.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TsTonWallet.getWalletData(self): TsTonWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}

fun TsTonWallet.getDaoWalletData(self): TsTonDaoWalletDataReply {
    return net.runGetMethod(self.address, "get_dao_wallet_data");
}

fun TsTonWallet.getVoteKeeperAddress(self, votingAddress: address): address {
    return net.runGetMethod(self.address, "get_vote_keeper_address", [votingAddress]);
}
