/// Auto-generated wrapper for contract 'MoonOrderNew'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/moon_order_new.types"

/// Storage `MoonOrderNewDeploymentStorage` is defined in `../types/moon_order_new.types`
struct MoonOrderNew {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MoonOrderNew.fromStorage(
    storage: MoonOrderNewDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MoonOrderNew {
    val stateInit = ContractState {
        code: build("MoonOrderNew"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MoonOrderNew { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MoonOrderNew.fromAddress(address: address): MoonOrderNew {
    return MoonOrderNew { address };
}

/// Deploys the contract to the blockchain
fun MoonOrderNew.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderNew.sendMoonOrderNewCancel(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderNewCancel { queryId },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderNew.sendMoonOrderNewFill(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    recipientPayload: Cell<RemainingBitsAndRefs>?,
    rejectAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderNewFill { queryId, recipient, recipientPayload, rejectAddress },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderNew.sendMoonOrderNewJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderNewJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderNew.sendMoonOrderNewPipeWalletResolved(
    self,
    from: address,
    queryId: uint64,
    wallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderNewPipeWalletResolved { queryId, wallet },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderNew.sendMoonOrderNewDeploy(
    self,
    from: address,
    queryId: uint64,
    orderFeeBps: uint8,
    orderParams: MoonOrderNewOrderParams,
    assets: MoonOrderNewAssets,
    wallets: MoonOrderNewWallets,
    funding: Cell<MoonOrderNewFunding>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderNewDeploy {
            queryId,
            orderFeeBps,
            orderParams: orderParams.toCell(),
            assets: assets.toCell(),
            wallets: wallets.toCell(),
            funding,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MoonOrderNew.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MoonOrderNew.getStatus(self): uint8 {
    return net.runGetMethod(self.address, "get_status");
}

fun MoonOrderNew.getOrderAmount(self): int {
    return net.runGetMethod(self.address, "get_order_amount");
}

fun MoonOrderNew.getOrder(self): cell {
    return net.runGetMethod(self.address, "get_order");
}

fun MoonOrderNew.getWallets(self): cell {
    return net.runGetMethod(self.address, "get_wallets");
}

fun MoonOrderNew.getVestingData(self): MoonOrderNewVestingDataReply {
    return net.runGetMethod(self.address, "get_vesting_data");
}

fun MoonOrderNew.getFillOut(self, amountIn: int): int {
    return net.runGetMethod(self.address, "get_fill_out", [amountIn]);
}

fun MoonOrderNew.getAssets(self): cell {
    return net.runGetMethod(self.address, "get_assets");
}
