/// Auto-generated wrapper for contract 'MoonOrderFactory'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/moon_order_factory.types"

/// Storage `MoonOrderFactoryStorage` is defined in `../types/moon_order_factory.types`
struct MoonOrderFactory {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MoonOrderFactory.fromStorage(
    storage: MoonOrderFactoryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MoonOrderFactory {
    val stateInit = ContractState {
        code: build("MoonOrderFactory"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MoonOrderFactory { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MoonOrderFactory.fromAddress(address: address): MoonOrderFactory {
    return MoonOrderFactory { address };
}

/// Deploys the contract to the blockchain
fun MoonOrderFactory.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryFeeNotification(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryFeeNotification { queryId },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryDeployPipeRequest(
    self,
    from: address,
    queryId: uint64,
    asset: address,
    config0: RemainingBitsAndRefs,
    config1: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryDeployPipeRequest {
            queryId,
            asset,
            config0: config0.toCell(),
            config1: config1.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryCreateOrder(
    self,
    from: address,
    queryId: uint64,
    asset2: address,
    orderParams: MoonOrderFactoryOrderParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryCreateOrder {
            queryId,
            asset2,
            orderParams: orderParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryRegisterOrder(
    self,
    from: address,
    queryId: uint64,
    creator: address,
    orderId: uint64,
    assets: MoonOrderFactoryAssets,
    sourceWallet: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryRegisterOrder {
            queryId,
            creator,
            orderId,
            assets: assets.toCell(),
            sourceWallet,
            amount,
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryJettonTransferNotification {
            queryId,
            amount,
            sender,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryForwardRequest(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    payload0: RemainingBitsAndRefs,
    payload1: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryForwardRequest {
            queryId,
            destination,
            payload0: payload0.toCell(),
            payload1: payload1.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.sendMoonOrderFactoryUpgrade(
    self,
    from: address,
    queryId: uint64,
    newCode: RemainingBitsAndRefs,
    newStorage: MoonOrderFactoryUpgradeStorage,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonOrderFactoryUpgrade {
            queryId,
            newCode: newCode.toCell(),
            newStorage: newStorage.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MoonOrderFactory.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MoonOrderFactory.getId(self): uint64 {
    return net.runGetMethod(self.address, "get_id");
}

fun MoonOrderFactory.getPipe(self, asset: address): address {
    return net.runGetMethod(self.address, "get_pipe", [asset]);
}
