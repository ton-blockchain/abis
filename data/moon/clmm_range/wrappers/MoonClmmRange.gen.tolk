/// Auto-generated wrapper for contract 'MoonClmmRange'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/moon_clmm_range.types"

/// Storage `MoonClmmRangeStorage` is defined in `../types/moon_clmm_range.types`
struct MoonClmmRange {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MoonClmmRange.fromStorage(
    storage: MoonClmmRangeStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MoonClmmRange {
    val stateInit = ContractState {
        code: build("MoonClmmRange"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MoonClmmRange { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MoonClmmRange.fromAddress(address: address): MoonClmmRange {
    return MoonClmmRange { address };
}

/// Deploys the contract to the blockchain
fun MoonClmmRange.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MoonClmmRange.sendMoonClmmRangeSwap(
    self,
    from: address,
    queryId: uint64,
    swapAmount: coins,
    excessAddress: address,
    sqrtPriceLimitX128: uint256,
    nextFulfill: Cell<MoonClmmRangeCallback>?,
    nextReject: Cell<MoonClmmRangeCallback>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonClmmRangeSwap {
            queryId,
            swapAmount,
            excessAddress,
            sqrtPriceLimitX128,
            nextFulfill,
            nextReject,
        },
    });
    return net.send(from, genericMsg);
}

fun MoonClmmRange.sendMoonClmmRangeWrappedSwap(
    self,
    from: address,
    queryId: uint64,
    swapAmount: coins,
    swap: MoonClmmRangeSwapPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonClmmRangeWrappedSwap {
            queryId,
            swapAmount,
            swap: swap.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MoonClmmRange.sendMoonClmmRangeDepositLiquidity(
    self,
    from: address,
    queryId: uint64,
    depositAmount: coins,
    isSingle: int1,
    bins: map<int64, MoonClmmRangeDepositBin>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonClmmRangeDepositLiquidity { queryId, depositAmount, isSingle, bins },
    });
    return net.send(from, genericMsg);
}

fun MoonClmmRange.sendMoonClmmRangeJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address?,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MoonClmmRangeJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MoonClmmRange.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MoonClmmRange.getRange(self): cell {
    return net.runGetMethod(self.address, "get_range");
}

fun MoonClmmRange.getBins(self): cell? {
    return net.runGetMethod(self.address, "get_bins");
}

fun MoonClmmRange.getLpAccount(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_lp_account", [ownerAddress]);
}

fun MoonClmmRange.getCurrentBin(self): int {
    return net.runGetMethod(self.address, "get_current_bin");
}

fun MoonClmmRange.getNftAddressByIndex(self, index: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [index]);
}

fun MoonClmmRange.getCurrentSqrtPrice(self): int {
    return net.runGetMethod(self.address, "get_current_sqrt_price");
}
