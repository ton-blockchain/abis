/// Auto-generated wrapper for contract 'AirdropInterlockerV1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/airdrop_interlocker_v1.types"

/// Storage `AirdropInterlockerV1Storage` is defined in `../types/airdrop_interlocker_v1.types`
struct AirdropInterlockerV1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun AirdropInterlockerV1.fromStorage(
    storage: AirdropInterlockerV1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): AirdropInterlockerV1 {
    val stateInit = ContractState {
        code: build("AirdropInterlockerV1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return AirdropInterlockerV1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun AirdropInterlockerV1.fromAddress(address: address): AirdropInterlockerV1 {
    return AirdropInterlockerV1 { address };
}

/// Deploys the contract to the blockchain
fun AirdropInterlockerV1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun AirdropInterlockerV1.sendAirdropInterlockerV1LockAndClaim(
    self,
    from: address,
    queryId: uint64,
    proof: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AirdropInterlockerV1LockAndClaim { queryId, proof },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun AirdropInterlockerV1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun AirdropInterlockerV1.getContractData(self): GetContractDataReply {
    return net.runGetMethod(self.address, "get_contract_data");
}
