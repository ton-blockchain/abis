/// Auto-generated wrapper for contract 'AirdropInterlockerV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/airdrop_interlocker_v2.types"

/// Storage `AirdropInterlockerV2Storage` is defined in `../types/airdrop_interlocker_v2.types`
struct AirdropInterlockerV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun AirdropInterlockerV2.fromStorage(
    storage: AirdropInterlockerV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): AirdropInterlockerV2 {
    val stateInit = ContractState {
        code: build("AirdropInterlockerV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return AirdropInterlockerV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun AirdropInterlockerV2.fromAddress(address: address): AirdropInterlockerV2 {
    return AirdropInterlockerV2 { address };
}

/// Deploys the contract to the blockchain
fun AirdropInterlockerV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun AirdropInterlockerV2.sendAirdropInterlockerV2LockAndClaim(
    self,
    from: address,
    queryId: uint64,
    proof: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AirdropInterlockerV2LockAndClaim { queryId, proof },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun AirdropInterlockerV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun AirdropInterlockerV2.getDistributionInfo(self): GetDistributionInfoReply {
    return net.runGetMethod(self.address, "get_distribution_info");
}

fun AirdropInterlockerV2.getUnlocksInfo(self): GetUnlocksInfoReply {
    return net.runGetMethod(self.address, "get_unlocks_info");
}

fun AirdropInterlockerV2.getAvailableClaimAmount(self): coins {
    return net.runGetMethod(self.address, "get_available_claim_amount");
}
