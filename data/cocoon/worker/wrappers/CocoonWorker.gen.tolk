/// Auto-generated wrapper for contract 'CocoonWorker'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/cocoon_worker.types"

/// Storage `CocoonWorkerStorage` is defined in `../types/cocoon_worker.types`
struct CocoonWorker {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CocoonWorker.fromStorage(
    storage: CocoonWorkerStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CocoonWorker {
    val stateInit = ContractState {
        code: build("CocoonWorker"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CocoonWorker { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CocoonWorker.fromAddress(address: address): CocoonWorker {
    return CocoonWorker { address };
}

/// Deploys the contract to the blockchain
fun CocoonWorker.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonWorker.sendCocoonWorkerDoNotProcess(
    self,
    from: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWorkerDoNotProcess { payload },
    });
    return net.send(from, genericMsg);
}

fun CocoonWorker.sendCocoonWorkerPayoutRequestSigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    signedDataCell: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWorkerPayoutRequestSigned {
            queryId,
            sendExcessesTo,
            signature,
            signedDataCell,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonWorker.sendCocoonWorkerLastPayoutRequestSigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    signedDataCell: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWorkerLastPayoutRequestSigned {
            queryId,
            sendExcessesTo,
            signature,
            signedDataCell,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonWorker.sendCocoonWorkerRegister(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWorkerRegister { queryId, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CocoonWorker.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CocoonWorker.getCocoonWorkerData(self): CocoonWorkerDataResult {
    return net.runGetMethod(self.address, "get_cocoon_worker_data");
}
