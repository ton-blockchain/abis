/// Auto-generated wrapper for contract 'CocoonProxy'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/cocoon_proxy.types"

/// Storage `CocoonProxyStorage` is defined in `../types/cocoon_proxy.types`
struct CocoonProxy {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CocoonProxy.fromStorage(
    storage: CocoonProxyStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CocoonProxy {
    val stateInit = ContractState {
        code: build("CocoonProxy"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CocoonProxy { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CocoonProxy.fromAddress(address: address): CocoonProxy {
    return CocoonProxy { address };
}

/// Deploys the contract to the blockchain
fun CocoonProxy.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyDoNotProcess(
    self,
    from: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyDoNotProcess { payload },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyTextCommand(
    self,
    from: address,
    action: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyTextCommand { action },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyCloseRequestSigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    signedDataCell: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyCloseRequestSigned { queryId, sendExcessesTo, signature, signedDataCell },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyCloseCompleteRequestSigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    signedDataCell: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyCloseCompleteRequestSigned {
            queryId,
            sendExcessesTo,
            signature,
            signedDataCell,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyPayoutRequest(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyPayoutRequest { queryId, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyIncreaseStake(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyIncreaseStake { queryId, amount, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyOwnerClose(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyOwnerClose { queryId, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyWorkerRequest(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    state: uint2,
    tokens: uint64,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyWorkerRequest { queryId, ownerAddress, state, tokens, payload },
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.sendCocoonProxyClientRequest(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    stateData: CocoonProxyClientStateData,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonProxyClientRequest {
            queryId,
            ownerAddress,
            stateData: stateData.toCell(),
            payload,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CocoonProxy.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CocoonProxy.getCocoonProxyData(self): CocoonProxyDataResult {
    return net.runGetMethod(self.address, "get_cocoon_proxy_data");
}
