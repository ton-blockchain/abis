/// Auto-generated wrapper for contract 'CocoonRoot'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/cocoon_root.types"

/// Storage `CocoonRootStorage` is defined in `../types/cocoon_root.types`
struct CocoonRoot {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CocoonRoot.fromStorage(
    storage: CocoonRootStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CocoonRoot {
    val stateInit = ContractState {
        code: build("CocoonRoot"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CocoonRoot { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CocoonRoot.fromAddress(address: address): CocoonRoot {
    return CocoonRoot { address };
}

/// Deploys the contract to the blockchain
fun CocoonRoot.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootAddWorkerType(
    self,
    from: address,
    queryId: uint64,
    workerHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootAddWorkerType { queryId, workerHash },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootDeleteWorkerType(
    self,
    from: address,
    queryId: uint64,
    workerHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootDeleteWorkerType { queryId, workerHash },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootAddModelType(
    self,
    from: address,
    queryId: uint64,
    modelHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootAddModelType { queryId, modelHash },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootDeleteModelType(
    self,
    from: address,
    queryId: uint64,
    modelHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootDeleteModelType { queryId, modelHash },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootAddProxyType(
    self,
    from: address,
    queryId: uint64,
    proxyHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootAddProxyType { queryId, proxyHash },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootDeleteProxyType(
    self,
    from: address,
    queryId: uint64,
    proxyHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootDeleteProxyType { queryId, proxyHash },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootRegisterProxy(
    self,
    from: address,
    queryId: uint64,
    proxyInfo: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootRegisterProxy { queryId, proxyInfo },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootUnregisterProxy(
    self,
    from: address,
    queryId: uint64,
    seqno: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootUnregisterProxy { queryId, seqno },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootUpdateProxy(
    self,
    from: address,
    queryId: uint64,
    seqno: uint32,
    proxyAddress: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootUpdateProxy { queryId, seqno, proxyAddress },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootAddPublicKeySigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    payload: CocoonRootSignedAddPublicKeyPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootAddPublicKeySigned {
            queryId,
            sendExcessesTo,
            signature,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootAddPublicKey(
    self,
    from: address,
    queryId: uint64,
    publicKey: uint256,
    keyType: uint8,
    expiresAt: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootAddPublicKey { queryId, publicKey, keyType, expiresAt },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootChangeFees(
    self,
    from: address,
    queryId: uint64,
    pricePerToken: coins,
    workerFeePerToken: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootChangeFees { queryId, pricePerToken, workerFeePerToken },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootChangeParams(
    self,
    from: address,
    queryId: uint64,
    pricePerToken: coins,
    workerFeePerToken: coins,
    proxyDelayBeforeClose: uint32,
    clientDelayBeforeClose: uint32,
    minProxyStake: coins,
    minClientStake: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootChangeParams {
            queryId,
            pricePerToken,
            workerFeePerToken,
            proxyDelayBeforeClose,
            clientDelayBeforeClose,
            minProxyStake,
            minClientStake,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootUpgradeContracts(
    self,
    from: address,
    queryId: uint64,
    proxyCode: cell,
    workerCode: cell,
    clientCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootUpgradeContracts { queryId, proxyCode, workerCode, clientCode },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootUpgradeCode(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootUpgradeCode { queryId, newCode },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootReset(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootReset { queryId },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootUpgradeFull(
    self,
    from: address,
    queryId: uint64,
    newData: cell,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootUpgradeFull { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootChangeOwner(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootChangeOwner { queryId, newOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.sendCocoonRootChangeKeyManager(
    self,
    from: address,
    queryId: uint64,
    managerPublicKey: uint256,
    managerImageHash: uint256,
    managerNetworkAddress: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonRootChangeKeyManager {
            queryId,
            managerPublicKey,
            managerImageHash,
            managerNetworkAddress,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CocoonRoot.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CocoonRoot.lastProxySeqno(self): int {
    return net.runGetMethod(self.address, "last_proxy_seqno");
}

fun CocoonRoot.getCocoonData(self): CocoonRootDataResult {
    return net.runGetMethod(self.address, "get_cocoon_data");
}

fun CocoonRoot.getCurParams(self): CocoonRootCurrentParamsResult {
    return net.runGetMethod(self.address, "get_cur_params");
}

fun CocoonRoot.proxyHashIsValid(self, hash: int): int {
    return net.runGetMethod(self.address, "proxy_hash_is_valid", [hash]);
}

fun CocoonRoot.workerHashIsValid(self, hash: int): int {
    return net.runGetMethod(self.address, "worker_hash_is_valid", [hash]);
}

fun CocoonRoot.modelHashIsValid(self, hash: int): int {
    return net.runGetMethod(self.address, "model_hash_is_valid", [hash]);
}

fun CocoonRoot.publicKeyIsValid(self, hash: int): int {
    return net.runGetMethod(self.address, "public_key_is_valid", [hash]);
}
