/// Auto-generated wrapper for contract 'CocoonClient'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/cocoon_client.types"

/// Storage `CocoonClientStorage` is defined in `../types/cocoon_client.types`
struct CocoonClient {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CocoonClient.fromStorage(
    storage: CocoonClientStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CocoonClient {
    val stateInit = ContractState {
        code: build("CocoonClient"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CocoonClient { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CocoonClient.fromAddress(address: address): CocoonClient {
    return CocoonClient { address };
}

/// Deploys the contract to the blockchain
fun CocoonClient.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientDoNotProcess(
    self,
    from: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientDoNotProcess { payload },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientChargeSigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    signedDataCell: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientChargeSigned { queryId, sendExcessesTo, signature, signedDataCell },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientGrantRefundSigned(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    signature: bits512,
    signedDataCell: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientGrantRefundSigned { queryId, sendExcessesTo, signature, signedDataCell },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientTopUp(
    self,
    from: address,
    queryId: uint64,
    topUpAmount: coins,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientTopUp { queryId, topUpAmount, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientChangeSecretHashAndTopUp(
    self,
    from: address,
    queryId: uint64,
    topUpAmount: coins,
    newSecretHash: uint256,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientChangeSecretHashAndTopUp {
            queryId,
            topUpAmount,
            newSecretHash,
            sendExcessesTo,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientRegister(
    self,
    from: address,
    queryId: uint64,
    nonce: uint64,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientRegister { queryId, nonce, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientChangeSecretHash(
    self,
    from: address,
    queryId: uint64,
    newSecretHash: uint256,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientChangeSecretHash { queryId, newSecretHash, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientIncreaseStake(
    self,
    from: address,
    queryId: uint64,
    newStake: coins,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientIncreaseStake { queryId, newStake, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientWithdraw(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientWithdraw { queryId, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.sendCocoonClientRequestRefund(
    self,
    from: address,
    queryId: uint64,
    sendExcessesTo: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonClientRequestRefund { queryId, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CocoonClient.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CocoonClient.getCocoonClientData(self): CocoonClientDataResult {
    return net.runGetMethod(self.address, "get_cocoon_client_data");
}
