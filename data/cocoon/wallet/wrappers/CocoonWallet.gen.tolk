/// Auto-generated wrapper for contract 'CocoonWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/cocoon_wallet.types"

/// Storage `CocoonWalletStorage` is defined in `../types/cocoon_wallet.types`
struct CocoonWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CocoonWallet.fromStorage(
    storage: CocoonWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CocoonWallet {
    val stateInit = ContractState {
        code: build("CocoonWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CocoonWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CocoonWallet.fromAddress(address: address): CocoonWallet {
    return CocoonWallet { address };
}

/// Deploys the contract to the blockchain
fun CocoonWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CocoonWallet.sendCocoonWalletSendMessage(
    self,
    from: address,
    queryId: uint64,
    mode: uint8,
    message: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWalletSendMessage { queryId, mode, message },
    });
    return net.send(from, genericMsg);
}

fun CocoonWallet.sendCocoonWalletTextCommand(
    self,
    from: address,
    action: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWalletTextCommand { action },
    });
    return net.send(from, genericMsg);
}

fun CocoonWallet.sendCocoonWalletReturnExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWalletReturnExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

fun CocoonWallet.sendCocoonWalletPayout(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWalletPayout { queryId },
    });
    return net.send(from, genericMsg);
}

fun CocoonWallet.sendCocoonWalletDoNotProcess(
    self,
    from: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CocoonWalletDoNotProcess { payload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CocoonWallet.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CocoonWallet.sendExternalCocoonWalletSignedExternal(
    self,
    signature: bits512,
    subwalletId: uint32,
    validUntil: uint32,
    seqno: uint32,
    messages: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = CocoonWalletSignedExternal { signature, subwalletId, validUntil, seqno, messages };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun CocoonWallet.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun CocoonWallet.seqno(self): uint32 {
    return net.runGetMethod(self.address, "seqno");
}

fun CocoonWallet.getPublicKey(self): uint256 {
    return net.runGetMethod(self.address, "get_public_key");
}

fun CocoonWallet.getOwnerAddress(self): address {
    return net.runGetMethod(self.address, "get_owner_address");
}
