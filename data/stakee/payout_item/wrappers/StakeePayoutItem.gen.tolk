/// Auto-generated wrapper for contract 'StakeePayoutItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stakee_payout_item.types"

/// Storage `StakeePayoutItemDeploymentStorage` is defined in `../types/stakee_payout_item.types`
struct StakeePayoutItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StakeePayoutItem.fromStorage(
    storage: StakeePayoutItemDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StakeePayoutItem {
    val stateInit = ContractState {
        code: build("StakeePayoutItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StakeePayoutItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StakeePayoutItem.fromAddress(address: address): StakeePayoutItem {
    return StakeePayoutItem { address };
}

/// Deploys the contract to the blockchain
fun StakeePayoutItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutItem.sendStakeePayoutItemInit(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    amount: coins,
    previousItemAddress: address?,
    nextItemAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutItemInit {
            queryId,
            ownerAddress,
            amount,
            previousItemAddress,
            nextItemAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutItem.sendStakeePayoutItemTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayloadPresent: bool,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutItemTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayloadPresent,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutItem.sendStakeePayoutItemBurn(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutItemBurn { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutItem.sendStakeePayoutItemGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutItemGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StakeePayoutItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutItem.getNftData(self): StakeePayoutItemNftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun StakeePayoutItem.getBillAmount(self): int {
    return net.runGetMethod(self.address, "get_bill_amount");
}
