/// Auto-generated wrapper for contract 'StakeedWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stakeed_wallet.types"

/// Storage `StakeedWalletStorage` is defined in `../types/stakeed_wallet.types`
struct StakeedWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StakeedWallet.fromStorage(
    storage: StakeedWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StakeedWallet {
    val stateInit = ContractState {
        code: build("StakeedWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StakeedWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StakeedWallet.fromAddress(address: address): StakeedWallet {
    return StakeedWallet { address };
}

/// Deploys the contract to the blockchain
fun StakeedWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    fromAddress: address,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletInternalTransfer {
            queryId,
            amount,
            fromAddress,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletWithdrawTons(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletWithdrawTons { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletWithdrawJettons(
    self,
    from: address,
    queryId: uint64,
    childWallet: address,
    amount: coins,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletWithdrawJettons { queryId, childWallet, amount, customPayload },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletVote(
    self,
    from: address,
    queryId: uint64,
    votingAddress: address,
    expirationDate: uint48,
    vote: bool,
    needConfirmation: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletVote { queryId, votingAddress, expirationDate, vote, needConfirmation },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletCreateVotingThroughWallet(
    self,
    from: address,
    queryId: uint64,
    votingType: uint64,
    expirationDate: uint48,
    proposal: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletCreateVotingThroughWallet {
            queryId,
            votingType,
            expirationDate,
            proposal,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletConfirmVoting(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletConfirmVoting { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.sendStakeedWalletVotingCreated(
    self,
    from: address,
    queryId: uint64,
    votingAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeedWalletVotingCreated { queryId, votingAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StakeedWallet.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StakeedWallet.getWalletData(self): StakeedWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}

fun StakeedWallet.getDaoWalletData(self): StakeedDaoWalletDataReply {
    return net.runGetMethod(self.address, "get_dao_wallet_data");
}

fun StakeedWallet.getVoteKeeperAddress(self, votingAddress: address): address {
    return net.runGetMethod(self.address, "get_vote_keeper_address", [votingAddress]);
}
