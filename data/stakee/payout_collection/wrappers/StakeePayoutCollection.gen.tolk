/// Auto-generated wrapper for contract 'StakeePayoutCollection'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stakee_payout_collection.types"

/// Storage `StakeePayoutCollectionDeploymentStorage` is defined in `../types/stakee_payout_collection.types`
struct StakeePayoutCollection {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StakeePayoutCollection.fromStorage(
    storage: StakeePayoutCollectionDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StakeePayoutCollection {
    val stateInit = ContractState {
        code: build("StakeePayoutCollection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StakeePayoutCollection { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StakeePayoutCollection.fromAddress(address: address): StakeePayoutCollection {
    return StakeePayoutCollection { address };
}

/// Deploys the contract to the blockchain
fun StakeePayoutCollection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutCollection.sendStakeePayoutCollectionInitCollection(
    self,
    from: address,
    queryId: uint64,
    distribution: StakeePayoutCollectionDistribution,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutCollectionInitCollection {
            queryId,
            distribution: distribution.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutCollection.sendStakeePayoutCollectionMintNft(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    amount: coins,
    notificationTon: coins,
    forwardTon: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutCollectionMintNft { queryId, owner, amount, notificationTon, forwardTon },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutCollection.sendStakeePayoutCollectionTransferNotification(
    self,
    from: address,
    queryId: uint64,
    burntAmount: coins,
    fromAddress: address?,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutCollectionTransferNotification {
            queryId,
            burntAmount,
            fromAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutCollection.sendStakeePayoutCollectionStartDistribution(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutCollectionStartDistribution { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutCollection.sendStakeePayoutCollectionBurnNotification(
    self,
    from: address,
    queryId: uint64,
    burntAmount: coins,
    burnerAddress: address,
    burntIndex: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePayoutCollectionBurnNotification {
            queryId,
            burntAmount,
            burnerAddress,
            burntIndex,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StakeePayoutCollection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StakeePayoutCollection.getCollectionData(self): StakeePayoutCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun StakeePayoutCollection.getDistributionData(self): cell? {
    return net.runGetMethod(self.address, "get_distribution_data");
}

fun StakeePayoutCollection.getIssuedBills(self): StakeePayoutCollectionIssuedBillsReply {
    return net.runGetMethod(self.address, "get_issued_bills");
}

fun StakeePayoutCollection.getNftAddressByIndex(self, index: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [index]);
}

fun StakeePayoutCollection.getNftContent(self, index: int, individualNftContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [index, individualNftContent]);
}
