/// Auto-generated wrapper for contract 'StakeePool'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/stakee_pool.types"

/// Storage `StakeePoolStorage` is defined in `../types/stakee_pool.types`
struct StakeePool {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StakeePool.fromStorage(
    storage: StakeePoolStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StakeePool {
    val stateInit = ContractState {
        code: build("StakeePool"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StakeePool { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StakeePool.fromAddress(address: address): StakeePool {
    return StakeePool { address };
}

/// Deploys the contract to the blockchain
fun StakeePool.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolDeposit(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolDeposit { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolWithdraw(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    fromAddress: address,
    responseAddress: address,
    waitTillRoundEnd: bool,
    fillOrKill: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolWithdraw {
            queryId,
            jettonAmount,
            fromAddress,
            responseAddress,
            waitTillRoundEnd,
            fillOrKill,
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolRequestLoan(
    self,
    from: address,
    queryId: uint64,
    minLoan: coins,
    maxLoan: coins,
    maxInterest: uint24,
    controllerData: StakeePoolControllerData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolRequestLoan {
            queryId,
            minLoan,
            maxLoan,
            maxInterest,
            controllerData: controllerData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolLoanRepayment(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolLoanRepayment { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolTouch(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolTouch { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolDonate(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolDonate { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolDeployController(
    self,
    from: address,
    queryId: uint64,
    controllerId: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolDeployController { queryId, controllerId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolHalt(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolHalt { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSudoSendMessage(
    self,
    from: address,
    queryId: uint64,
    mode: uint8,
    message: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSudoSendMessage { queryId, mode, message },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSudoUpgrade(
    self,
    from: address,
    queryId: uint64,
    data: cell?,
    code: cell?,
    afterUpgrade: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSudoUpgrade { queryId, data, code, afterUpgrade },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSetSudoer(
    self,
    from: address,
    queryId: uint64,
    sudoer: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSetSudoer { queryId, sudoer },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolUnhalt(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolUnhalt { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolPrepareGovernanceMigration(
    self,
    from: address,
    queryId: uint64,
    governorUpdateAfter: uint48,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolPrepareGovernanceMigration { queryId, governorUpdateAfter },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSetRoles(
    self,
    from: address,
    queryId: uint64,
    governor: StakeePoolMaybeRoleAddress,
    interestManager: StakeePoolMaybeRoleAddress,
    halter: StakeePoolMaybeRoleAddress,
    approver: StakeePoolMaybeRoleAddress,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSetRoles { queryId, governor, interestManager, halter, approver },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSetDepositSettings(
    self,
    from: address,
    queryId: uint64,
    optimisticDepositWithdrawals: bool,
    depositsOpen: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSetDepositSettings { queryId, optimisticDepositWithdrawals, depositsOpen },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSetGovernanceFee(
    self,
    from: address,
    queryId: uint64,
    governanceFee: uint24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSetGovernanceFee { queryId, governanceFee },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolReturnAvailableFunds(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolReturnAvailableFunds { queryId },
    });
    return net.send(from, genericMsg);
}

fun StakeePool.sendStakeePoolSetInterest(
    self,
    from: address,
    queryId: uint64,
    interestRate: uint24,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StakeePoolSetInterest { queryId, interestRate },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StakeePool.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StakeePool.getPoolFullData(self): StakeePoolFullDataReply {
    return net.runGetMethod(self.address, "get_pool_full_data");
}

fun StakeePool.getPoolFullDataRaw(self): StakeePoolFullDataReply {
    return net.runGetMethod(self.address, "get_pool_full_data_raw");
}

fun StakeePool.getControllerAddress(self, controllerId: int, validator: address): address {
    return net.runGetMethod(self.address, "get_controller_address", [controllerId, validator]);
}

fun StakeePool.getControllerAddressLegacy(
    self,
    controllerId: int,
    workchain: int,
    addressHash: uint256,
): StakeePoolControllerAddressLegacyReply {
    return net.runGetMethod(
        self.address,
        "get_controller_address_legacy",
        [controllerId, workchain, addressHash],
    );
}

fun StakeePool.getLoan(
    self,
    controllerId: int,
    validatorAddress: address,
    prev: bool,
    update: bool,
): StakeePoolLoanReply {
    return net.runGetMethod(
        self.address,
        "get_loan",
        [controllerId, validatorAddress, prev, update],
    );
}

fun StakeePool.getControllerLoanPosition(
    self,
    controllerAddrHash: uint256,
    prev: bool,
): StakeePoolControllerLoanPositionReply {
    return net.runGetMethod(
        self.address,
        "get_controller_loan_position",
        [controllerAddrHash, prev],
    );
}

fun StakeePool.calculateLoanAmount(self, minLoan: int, maxLoan: int, maxInterest: int): int {
    return net.runGetMethod(self.address, "calculate_loan_amount", [minLoan, maxLoan, maxInterest]);
}
