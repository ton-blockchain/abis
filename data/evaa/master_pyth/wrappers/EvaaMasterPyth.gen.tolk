/// Auto-generated wrapper for contract 'EvaaMasterPyth'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/evaa_master_pyth.types"

/// Storage `EvaaMasterPythStorage` is defined in `../types/evaa_master_pyth.types`
struct EvaaMasterPyth {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun EvaaMasterPyth.fromStorage(
    storage: EvaaMasterPythStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): EvaaMasterPyth {
    val stateInit = ContractState {
        code: build("EvaaMasterPyth"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return EvaaMasterPyth { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun EvaaMasterPyth.fromAddress(address: address): EvaaMasterPyth {
    return EvaaMasterPyth { address };
}

/// Deploys the contract to the blockchain
fun EvaaMasterPyth.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplyMaster(
    self,
    from: address,
    queryId: uint64,
    includeUserCode: int2,
    supplyAmount: uint64,
    recipientAddress: address,
    customResponsePayload: cell,
    optionalTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplyMaster {
            queryId,
            includeUserCode,
            supplyAmount,
            recipientAddress,
            customResponsePayload,
            optionalTail,
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplySuccess(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    assetId: uint256,
    amountSupplied: uint64,
    userNewPrincipal: int64,
    repayAmountPrincipal: int64,
    supplyAmountPrincipal: int64,
    customResponsePayload: cell,
    subaccountId: int16,
    returnRepayRemainingsFlag: int2,
    customPayloadRecipient: address,
    customPayloadSaturationFlag: int2,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplySuccess {
            queryId,
            ownerAddress,
            assetId,
            amountSupplied,
            userNewPrincipal,
            repayAmountPrincipal,
            supplyAmountPrincipal,
            customResponsePayload,
            subaccountId,
            returnRepayRemainingsFlag,
            customPayloadRecipient: customPayloadRecipient.toCell(),
            customPayloadSaturationFlag,
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplyFail(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    assetId: uint256,
    amount: uint64,
    customResponsePayload: cell,
    subaccountId: int16,
    returnRepayRemainingsFlag: int2,
    initialSender: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplyFail {
            queryId,
            ownerAddress,
            assetId,
            amount,
            customResponsePayload,
            subaccountId,
            returnRepayRemainingsFlag,
            initialSender: initialSender.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplyWithdrawMaster(
    self,
    from: address,
    queryId: uint64,
    supplyData: cell,
    withdrawData: EvaaMasterPythSupplyWithdrawWithdrawData,
    generalData: EvaaMasterPythSupplyWithdrawGeneralData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplyWithdrawMaster {
            queryId,
            supplyData,
            withdrawData: withdrawData.toCell(),
            generalData: generalData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplyWithdrawMasterWithoutPrices(
    self,
    from: address,
    queryId: uint64,
    supplyData: cell,
    withdrawData: EvaaMasterPythSupplyWithdrawWithdrawData,
    generalData: EvaaMasterPythSupplyWithdrawGeneralData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplyWithdrawMasterWithoutPrices {
            queryId,
            supplyData,
            withdrawData: withdrawData.toCell(),
            generalData: generalData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplyWithdrawSatisfied(
    self,
    from: address,
    queryId: uint64,
    supplyData: EvaaMasterPythSupplyWithdrawSatisfiedSupplyData,
    withdrawData: EvaaMasterPythSupplyWithdrawSatisfiedWithdrawData,
    generalData: EvaaMasterPythSupplyWithdrawSatisfiedGeneralData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplyWithdrawSatisfied {
            queryId,
            supplyData: supplyData.toCell(),
            withdrawData: withdrawData.toCell(),
            generalData: generalData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSupplyWithdrawUnsatisfied(
    self,
    from: address,
    queryId: uint64,
    supplyData: EvaaMasterPythSupplyWithdrawUnsatisfiedSupplyData,
    withdrawData: cell,
    generalData: EvaaMasterPythSupplyWithdrawUnsatisfiedGeneralData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSupplyWithdrawUnsatisfied {
            queryId,
            supplyData: supplyData.toCell(),
            withdrawData,
            generalData: generalData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythLiquidateMaster(
    self,
    from: address,
    queryId: uint64,
    borrowerAddress: address,
    collateralAssetId: uint256,
    minCollateralAmount: uint64,
    includeUserCode: int2,
    liquidateIncomingAmount: uint64,
    requestedRefTokens: cell,
    responsePayload: EvaaMasterPythLiquidateResponsePayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythLiquidateMaster {
            queryId,
            borrowerAddress,
            collateralAssetId,
            minCollateralAmount,
            includeUserCode,
            liquidateIncomingAmount,
            requestedRefTokens,
            responsePayload: responsePayload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythLiquidateUnsatisfied(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    liquidatorAddress: address,
    transferredAssetId: uint256,
    details: EvaaMasterPythLiquidateUnsatisfiedDetails,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythLiquidateUnsatisfied {
            queryId,
            ownerAddress,
            liquidatorAddress,
            transferredAssetId,
            details: details.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythLiquidateSatisfied(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    liquidatorAddress: address,
    transferredAssetId: uint256,
    details: EvaaMasterPythLiquidateSatisfiedDetails,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythLiquidateSatisfied {
            queryId,
            ownerAddress,
            liquidatorAddress,
            transferredAssetId,
            details: details.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythIdleMaster(
    self,
    from: address,
    queryId: uint64,
    targetAddress: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythIdleMaster { queryId, targetAddress, tail },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythDoDataChecks(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythDoDataChecks { queryId },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythParsePriceFeedUpdates(
    self,
    from: address,
    numPriceFeeds: uint8,
    priceFeedsCell: cell,
    initialSender: address,
    operationPayload: EvaaMasterPythNestedOperation,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythParsePriceFeedUpdates {
            numPriceFeeds,
            priceFeedsCell,
            initialSender,
            operationPayload: operationPayload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythResponseError(
    self,
    from: address,
    errorCode: uint32,
    operationCode: uint32,
    operationPayload: EvaaMasterPythNestedOperation,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythResponseError {
            errorCode,
            operationCode,
            operationPayload: operationPayload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythGetStoreRequest(
    self,
    from: address,
    queryId: uint64,
    bodyData: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythGetStoreRequest { queryId, bodyData },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythAddToReserve(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythAddToReserve {},
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: any_address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythInitMaster(
    self,
    from: address,
    queryId: uint64,
    newAssetConfigCollection: map<uint256, EvaaMasterPythAssetConfig>,
    packedData: EvaaMasterPythInitMasterPackedData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythInitMaster {
            queryId,
            newAssetConfigCollection: newAssetConfigCollection.toCell(),
            packedData: packedData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythClaimAssetReserves(
    self,
    from: address,
    queryId: uint64,
    targetAddress: address,
    assetId: uint256,
    amountToClaim: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythClaimAssetReserves { queryId, targetAddress, assetId, amountToClaim },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythDisableContractForUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythDisableContractForUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythInitUpgrade(
    self,
    from: address,
    queryId: uint64,
    newMasterCode: cell?,
    newUserCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythInitUpgrade { queryId, newMasterCode, newUserCode },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSubmitUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSubmitUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythCancelUpgrade(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythCancelUpgrade { queryId },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythUpdateConfig(
    self,
    from: address,
    queryId: uint64,
    newMeta: cell,
    newConfig: EvaaMasterPythConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythUpdateConfig {
            queryId,
            newMeta,
            newConfig: newConfig.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythAddNewToken(
    self,
    from: address,
    queryId: uint64,
    assetKey: uint256,
    newAssetConfig: EvaaMasterPythAssetConfig,
    newAssetDynamics: EvaaMasterPythAssetDynamics,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythAddNewToken {
            queryId,
            assetKey,
            newAssetConfig: newAssetConfig.toCell(),
            newAssetDynamics: newAssetDynamics.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythSetNewSupervisor(
    self,
    from: address,
    queryId: uint64,
    newSupervisor: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythSetNewSupervisor { queryId, newSupervisor },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythForceEnable(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythForceEnable { queryId },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythForceDisableContract(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythForceDisableContract { queryId },
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.sendEvaaMasterPythRevertCall(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    revertBody: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaMasterPythRevertCall { queryId, ownerAddress, revertBody },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun EvaaMasterPyth.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun EvaaMasterPyth.getCollateralQuote(
    self,
    borrowAssetId: int,
    borrowLiquidateAmount: int,
    collateralAssetId: int,
    pricesPacked: cell,
): int {
    return net.runGetMethod(
        self.address,
        "getCollateralQuote",
        [borrowAssetId, borrowLiquidateAmount, collateralAssetId, pricesPacked],
    );
}

fun EvaaMasterPyth.getUpdatedRates(
    self,
    assetConfigCollection: cell,
    assetDynamicsCollection: cell,
    assetId: int,
    timeElapsed: int,
): EvaaMasterPythAssetRate {
    return net.runGetMethod(
        self.address,
        "getUpdatedRates",
        [assetConfigCollection, assetDynamicsCollection, assetId, timeElapsed],
    );
}

fun EvaaMasterPyth.getUpdatedRatesForAllAssets(self, timeElapsed: int): map<
    uint256,
    EvaaMasterPythPackedAssetRate,
> {
    return net.runGetMethod(self.address, "getUpdatedRatesForAllAssets", [timeElapsed]);
}

fun EvaaMasterPyth.getAssetRates(self, assetId: int): EvaaMasterPythAssetRate {
    return net.runGetMethod(self.address, "getAssetRates", [assetId]);
}

fun EvaaMasterPyth.getAssetsRates(self): map<uint256, EvaaMasterPythPackedAssetRate> {
    return net.runGetMethod(self.address, "get_assets_rates");
}

fun EvaaMasterPyth.getAssetReserves(self, assetId: int): int {
    return net.runGetMethod(self.address, "getAssetReserves", [assetId]);
}

fun EvaaMasterPyth.getAssetsReserves(self): map<uint256, int65> {
    return net.runGetMethod(self.address, "get_assets_reserves");
}

fun EvaaMasterPyth.getAssetTotals(self, assetId: int): EvaaMasterPythAssetTotals {
    return net.runGetMethod(self.address, "getAssetTotals", [assetId]);
}

fun EvaaMasterPyth.getAssetsData(self): map<uint256, EvaaMasterPythAssetDynamics> {
    return net.runGetMethod(self.address, "getAssetsData");
}

fun EvaaMasterPyth.getAssetsConfig(self): map<uint256, EvaaMasterPythAssetConfig> {
    return net.runGetMethod(self.address, "getAssetsConfig");
}

fun EvaaMasterPyth.getConfig(self): cell {
    return net.runGetMethod(self.address, "getConfig");
}

fun EvaaMasterPyth.getStore(self): cell {
    return net.runGetMethod(self.address, "getStore");
}

fun EvaaMasterPyth.getUiVariables(self): EvaaMasterPythGetUIVariablesReply {
    return net.runGetMethod(self.address, "getUIVariables");
}

fun EvaaMasterPyth.getUserAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_user_address", [ownerAddress]);
}

fun EvaaMasterPyth.getUserSubaccountAddress(
    self,
    ownerAddress: address,
    subaccountId: int,
): address {
    return net.runGetMethod(
        self.address,
        "get_user_subaccount_address",
        [ownerAddress, subaccountId],
    );
}

fun EvaaMasterPyth.claimAssetReservesMinAttachment(self, fwdFee: int): int {
    return net.runGetMethod(self.address, "claim_asset_reserves_min_attachment", [fwdFee]);
}

fun EvaaMasterPyth.dummySupplyUserMessage(self): cell {
    return net.runGetMethod(self.address, "dummy_supply_user_message");
}

fun EvaaMasterPyth.supplyMinAttachment(self, fwdFee: int, supplyUserMessage: cell): int {
    return net.runGetMethod(self.address, "supply_min_attachment", [fwdFee, supplyUserMessage]);
}

fun EvaaMasterPyth.withdrawMinAttachment(self, fwdFee: int, withdrawUserMessage: cell): int {
    return net.runGetMethod(self.address, "withdraw_min_attachment", [fwdFee, withdrawUserMessage]);
}

fun EvaaMasterPyth.dummyLiquidateUserMessage(self): cell {
    return net.runGetMethod(self.address, "dummy_liquidate_user_message");
}

fun EvaaMasterPyth.liquidateMinAttachment(self, fwdFee: int, liquidateUserMessage: cell): int {
    return net.runGetMethod(
        self.address,
        "liquidate_min_attachment",
        [fwdFee, liquidateUserMessage],
    );
}

fun EvaaMasterPyth.getAssetTotalPrincipals(self, assetId: int): EvaaMasterPythAssetTotals {
    return net.runGetMethod(self.address, "get_asset_total_principals", [assetId]);
}

fun EvaaMasterPyth.dummySupplyWithdrawUserMessage(self): cell {
    return net.runGetMethod(self.address, "dummy_supply_withdraw_user_message");
}

fun EvaaMasterPyth.supplyWithdrawMinAttachment(
    self,
    fwdFee: int,
    supplyWithdrawUserMessage: cell,
): int {
    return net.runGetMethod(
        self.address,
        "supply_withdraw_min_attachment",
        [fwdFee, supplyWithdrawUserMessage],
    );
}

fun EvaaMasterPyth.getAssetBalance(self, assetId: int): int {
    return net.runGetMethod(self.address, "get_asset_balance", [assetId]);
}

fun EvaaMasterPyth.getAssetLiquidityById(self, assetId: int): int {
    return net.runGetMethod(self.address, "get_asset_liquidity_by_id", [assetId]);
}

fun EvaaMasterPyth.getAssetLiquidityMinusReservesById(self, assetId: int): int {
    return net.runGetMethod(self.address, "get_asset_liquidity_minus_reserves_by_id", [assetId]);
}

fun EvaaMasterPyth.getAssetSbRate(self, assetId: int): EvaaMasterPythAssetRate {
    return net.runGetMethod(self.address, "get_asset_sb_rate", [assetId]);
}

fun EvaaMasterPyth.getActive(self): int {
    return net.runGetMethod(self.address, "get_active");
}

fun EvaaMasterPyth.getTokensKeys(self): map<uint256, uint256> {
    return net.runGetMethod(self.address, "getTokensKeys");
}

fun EvaaMasterPyth.getLastUserScVersion(self): int {
    return net.runGetMethod(self.address, "getLastUserScVersion");
}

fun EvaaMasterPyth.getUpgradeConfig(self): EvaaMasterPythUpgradeConfigReply {
    return net.runGetMethod(self.address, "getUpgradeConfig");
}

fun EvaaMasterPyth.getAssetTrackingInfo(self, assetId: int): EvaaMasterPythAssetTrackingInfo {
    return net.runGetMethod(self.address, "get_asset_tracking_info", [assetId]);
}

fun EvaaMasterPyth.getSupervisor(self): address {
    return net.runGetMethod(self.address, "get_supervisor");
}

fun EvaaMasterPyth.getOraclesInfo(self): EvaaMasterPythGetOraclesInfoReply {
    return net.runGetMethod(self.address, "get_oracles_info");
}
