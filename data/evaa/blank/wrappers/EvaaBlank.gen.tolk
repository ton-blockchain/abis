/// Auto-generated wrapper for contract 'EvaaBlank'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/evaa_blank.types"

/// Storage `EvaaBlankStorage` is defined in `../types/evaa_blank.types`
struct EvaaBlank {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun EvaaBlank.fromStorage(
    storage: EvaaBlankStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): EvaaBlank {
    val stateInit = ContractState {
        code: build("EvaaBlank"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return EvaaBlank { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun EvaaBlank.fromAddress(address: address): EvaaBlank {
    return EvaaBlank { address };
}

/// Deploys the contract to the blockchain
fun EvaaBlank.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun EvaaBlank.sendEvaaBlankIncomingUpgrade(
    self,
    from: address,
    header: EvaaBlankUpgradeHeader,
    commandTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: EvaaBlankIncomingUpgrade { header, commandTail },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun EvaaBlank.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
