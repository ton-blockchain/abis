/// Auto-generated wrapper for contract 'WalletHighloadV1r1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/wallet_highload_v1r1.types"

/// Storage `WalletHighloadV1r1Storage` is defined in `../types/wallet_highload_v1r1.types`
struct WalletHighloadV1r1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WalletHighloadV1r1.fromStorage(
    storage: WalletHighloadV1r1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WalletHighloadV1r1 {
    val stateInit = ContractState {
        code: build("WalletHighloadV1r1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WalletHighloadV1r1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WalletHighloadV1r1.fromAddress(address: address): WalletHighloadV1r1 {
    return WalletHighloadV1r1 { address };
}

/// Deploys the contract to the blockchain
fun WalletHighloadV1r1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WalletHighloadV1r1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WalletHighloadV1r1.sendExternalHighloadWalletSignedV1r1(
    self,
    signature: bits512,
    subwalletId: uint32,
    validUntil: uint32,
    msgSeqno: uint32,
    payload: cell,
): ExternalSendResult {
    val body = HighloadWalletSignedV1r1 { signature, subwalletId, validUntil, msgSeqno, payload };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun WalletHighloadV1r1.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun WalletHighloadV1r1.seqno(self): uint32 {
    return net.runGetMethod(self.address, "seqno");
}
