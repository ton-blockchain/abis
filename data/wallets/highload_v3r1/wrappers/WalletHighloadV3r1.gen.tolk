/// Auto-generated wrapper for contract 'WalletHighloadV3r1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/wallet_highload_v3r1.types"

/// Storage `WalletHighloadV3r1Storage` is defined in `../types/wallet_highload_v3r1.types`
struct WalletHighloadV3r1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WalletHighloadV3r1.fromStorage(
    storage: WalletHighloadV3r1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WalletHighloadV3r1 {
    val stateInit = ContractState {
        code: build("WalletHighloadV3r1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WalletHighloadV3r1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WalletHighloadV3r1.fromAddress(address: address): WalletHighloadV3r1 {
    return WalletHighloadV3r1 { address };
}

/// Deploys the contract to the blockchain
fun WalletHighloadV3r1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun WalletHighloadV3r1.sendHighloadV3InternalTransfer(
    self,
    from: address,
    queryId: uint64,
    actions: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HighloadV3InternalTransfer { queryId, actions },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WalletHighloadV3r1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WalletHighloadV3r1.sendExternalHighloadWalletSignedV3(
    self,
    signature: bits512,
    msgInner: HighloadV3MsgInner,
): ExternalSendResult {
    val body = HighloadWalletSignedV3 {
        signature,
        msgInner: msgInner.toCell(),
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun WalletHighloadV3r1.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun WalletHighloadV3r1.getPublicKey(self): int {
    return net.runGetMethod(self.address, "get_public_key");
}

fun WalletHighloadV3r1.getSubwalletId(self): uint32 {
    return net.runGetMethod(self.address, "get_subwallet_id");
}

fun WalletHighloadV3r1.getLastCleanTime(self): uint64 {
    return net.runGetMethod(self.address, "get_last_clean_time");
}

fun WalletHighloadV3r1.getTimeout(self): uint32 {
    return net.runGetMethod(self.address, "get_timeout");
}

fun WalletHighloadV3r1.processed(self, queryId: int, needClean: int): bool {
    return net.runGetMethod(self.address, "processed?", [queryId, needClean]);
}
