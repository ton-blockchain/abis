/// Auto-generated wrapper for contract 'WalletPreprocessedV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/wallet_preprocessed_v2.types"

/// Storage `WalletPreprocessedV2Storage` is defined in `../types/wallet_preprocessed_v2.types`
struct WalletPreprocessedV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WalletPreprocessedV2.fromStorage(
    storage: WalletPreprocessedV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WalletPreprocessedV2 {
    val stateInit = ContractState {
        code: build("WalletPreprocessedV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WalletPreprocessedV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WalletPreprocessedV2.fromAddress(address: address): WalletPreprocessedV2 {
    return WalletPreprocessedV2 { address };
}

/// Deploys the contract to the blockchain
fun WalletPreprocessedV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WalletPreprocessedV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WalletPreprocessedV2.sendExternalPreprocessedWalletSignedV2(
    self,
    sign: bits512,
    inner: PreprocessedWalletV2MsgInner,
): ExternalSendResult {
    val body = PreprocessedWalletSignedV2 {
        sign,
        inner: inner.toCell(),
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun WalletPreprocessedV2.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}
