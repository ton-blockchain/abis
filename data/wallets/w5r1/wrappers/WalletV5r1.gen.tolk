/// Auto-generated wrapper for contract 'WalletV5r1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/wallet_v5r1.types"

/// Storage `WalletV5r1Storage` is defined in `../types/wallet_v5r1.types`
struct WalletV5r1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WalletV5r1.fromStorage(
    storage: WalletV5r1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WalletV5r1 {
    val stateInit = ContractState {
        code: build("WalletV5r1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WalletV5r1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WalletV5r1.fromAddress(address: address): WalletV5r1 {
    return WalletV5r1 { address };
}

/// Deploys the contract to the blockchain
fun WalletV5r1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun WalletV5r1.sendWalletSignedInternalV5r1(
    self,
    from: address,
    walletId: uint32,
    validUntil: uint32,
    seqno: uint32,
    outActions: cell?,
    extendedActions: cell?,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WalletSignedInternalV5r1 {
            walletId,
            validUntil,
            seqno,
            outActions,
            extendedActions,
            signature,
        },
    });
    return net.send(from, genericMsg);
}

fun WalletV5r1.sendWalletExtensionActionV5r1(
    self,
    from: address,
    queryId: uint64,
    outActions: cell?,
    extendedActions: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WalletExtensionActionV5r1 { queryId, outActions, extendedActions },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WalletV5r1.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WalletV5r1.sendExternalWalletSignedExternalV5r1(
    self,
    walletId: uint32,
    validUntil: uint32,
    seqno: uint32,
    outActions: cell?,
    extendedActions: cell?,
    signature: bits512,
): ExternalSendResult {
    val body = WalletSignedExternalV5r1 {
        walletId,
        validUntil,
        seqno,
        outActions,
        extendedActions,
        signature,
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun WalletV5r1.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun WalletV5r1.seqno(self): int {
    return net.runGetMethod(self.address, "seqno");
}

fun WalletV5r1.getPublicKey(self): int {
    return net.runGetMethod(self.address, "get_public_key");
}

fun WalletV5r1.getSubwalletId(self): int {
    return net.runGetMethod(self.address, "get_subwallet_id");
}

fun WalletV5r1.getExtensions(self): map<uint256, bool> {
    return net.runGetMethod(self.address, "get_extensions");
}

fun WalletV5r1.isSignatureAllowed(self): bool {
    return net.runGetMethod(self.address, "is_signature_allowed");
}
