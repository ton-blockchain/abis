/// Auto-generated wrapper for contract 'AquaUsdMasterVault'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/aqua_usd_master_vault.types"

/// Storage `AquaUsdMasterVaultStorage` is defined in `../types/aqua_usd_master_vault.types`
struct AquaUsdMasterVault {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun AquaUsdMasterVault.fromStorage(
    storage: AquaUsdMasterVaultStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): AquaUsdMasterVault {
    val stateInit = ContractState {
        code: build("AquaUsdMasterVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return AquaUsdMasterVault { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun AquaUsdMasterVault.fromAddress(address: address): AquaUsdMasterVault {
    return AquaUsdMasterVault { address };
}

/// Deploys the contract to the blockchain
fun AquaUsdMasterVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaExecuteAssetOperation(
    self,
    from: address,
    queryId: uint64,
    operation: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaExecuteAssetOperation { queryId, operation },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaFinalizePayout(
    self,
    from: address,
    queryId: uint64,
    operationsEnabled: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaFinalizePayout { queryId, operationsEnabled },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaRoutedAssetCommand(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaRoutedAssetCommand { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaAssetRouteRequest(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaAssetRouteRequest { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaSignedBundleCommand(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaSignedBundleCommand { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaAssetCallback(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaAssetCallback { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.sendAquaAccountUpdateCommand(
    self,
    from: address,
    queryId: uint64,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AquaAccountUpdateCommand { queryId, payload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun AquaUsdMasterVault.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun AquaUsdMasterVault.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun AquaUsdMasterVault.getJettonData(self): GetAquaJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun AquaUsdMasterVault.getAssets(self): GetAquaAssetsReply {
    return net.runGetMethod(self.address, "get_assets");
}
