/// Auto-generated wrapper for contract 'SingleNominatorV11'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/single_nominator_v1_1.types"

/// Storage `SingleNominatorV11Storage` is defined in `../types/single_nominator_v1_1.types`
struct SingleNominatorV11 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun SingleNominatorV11.fromStorage(
    storage: SingleNominatorV11Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): SingleNominatorV11 {
    val stateInit = ContractState {
        code: build("SingleNominatorV11"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return SingleNominatorV11 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun SingleNominatorV11.fromAddress(address: address): SingleNominatorV11 {
    return SingleNominatorV11 { address };
}

/// Deploys the contract to the blockchain
fun SingleNominatorV11.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11Withdraw(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11Withdraw { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11TextWithdraw(
    self,
    from: address,
    action: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11TextWithdraw { action },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11ChangeValidatorAddress(
    self,
    from: address,
    queryId: uint64,
    newValidatorAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11ChangeValidatorAddress { queryId, newValidatorAddress },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11SendRawMsg(
    self,
    from: address,
    queryId: uint64,
    mode: uint8,
    msg: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11SendRawMsg { queryId, mode, msg },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11Upgrade(
    self,
    from: address,
    queryId: uint64,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11Upgrade { queryId, code },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11NewStake(
    self,
    from: address,
    queryId: uint64,
    stake: coins,
    validatorPubkey: bits256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddr: bits256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11NewStake {
            queryId,
            stake,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddr,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11RecoverStake(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11RecoverStake { queryId },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11NewStakeConfirmation(
    self,
    from: address,
    queryId: uint64,
    comment: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11NewStakeConfirmation { queryId, comment },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.sendSingleNominatorV11RecoverStakeResponse(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV11RecoverStakeResponse { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun SingleNominatorV11.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV11.getRoles(self): SingleNominatorV11Roles {
    return net.runGetMethod(self.address, "get_roles");
}

fun SingleNominatorV11.getPoolData(self): SingleNominatorV11PoolDataReply {
    return net.runGetMethod(self.address, "get_pool_data");
}
