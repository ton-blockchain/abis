/// Auto-generated wrapper for contract 'SingleNominatorV10'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/single_nominator_v1_0.types"

/// Storage `SingleNominatorV10Storage` is defined in `../types/single_nominator_v1_0.types`
struct SingleNominatorV10 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun SingleNominatorV10.fromStorage(
    storage: SingleNominatorV10Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): SingleNominatorV10 {
    val stateInit = ContractState {
        code: build("SingleNominatorV10"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return SingleNominatorV10 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun SingleNominatorV10.fromAddress(address: address): SingleNominatorV10 {
    return SingleNominatorV10 { address };
}

/// Deploys the contract to the blockchain
fun SingleNominatorV10.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10Withdraw(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10Withdraw { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10ChangeValidatorAddress(
    self,
    from: address,
    queryId: uint64,
    newValidatorAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10ChangeValidatorAddress { queryId, newValidatorAddress },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10SendRawMsg(
    self,
    from: address,
    queryId: uint64,
    mode: uint8,
    msg: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10SendRawMsg { queryId, mode, msg },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10Upgrade(
    self,
    from: address,
    queryId: uint64,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10Upgrade { queryId, code },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10NewStake(
    self,
    from: address,
    queryId: uint64,
    stake: coins,
    validatorPubkey: bits256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddr: bits256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10NewStake {
            queryId,
            stake,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddr,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10RecoverStake(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10RecoverStake { queryId },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10NewStakeConfirmation(
    self,
    from: address,
    queryId: uint64,
    comment: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10NewStakeConfirmation { queryId, comment },
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.sendSingleNominatorV10RecoverStakeResponse(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SingleNominatorV10RecoverStakeResponse { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun SingleNominatorV10.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun SingleNominatorV10.getRoles(self): SingleNominatorV10Roles {
    return net.runGetMethod(self.address, "get_roles");
}

fun SingleNominatorV10.getPoolData(self): SingleNominatorV10PoolDataReply {
    return net.runGetMethod(self.address, "get_pool_data");
}
