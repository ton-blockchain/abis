/// Auto-generated wrapper for contract 'XtrMaster'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/xtr_master.types"

/// Storage `XtrMasterStorage` is defined in `../types/xtr_master.types`
struct XtrMaster {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun XtrMaster.fromStorage(
    storage: XtrMasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): XtrMaster {
    val stateInit = ContractState {
        code: build("XtrMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return XtrMaster { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun XtrMaster.fromAddress(address: address): XtrMaster {
    return XtrMaster { address };
}

/// Deploys the contract to the blockchain
fun XtrMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterBuyXtr(
    self,
    from: address,
    userAddress: address,
    amount: coins,
    starPrice: XtrMasterStarPrice,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterBuyXtr { userAddress, amount, starPrice },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterRouteToChild(
    self,
    from: address,
    destination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterRouteToChild { destination },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address?,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    customPayload: Cell<RemainingBitsAndRefs>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterBurnNotification {
            queryId,
            amount,
            sender,
            responseDestination,
            customPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterTransferFromWallet(
    self,
    from: address,
    destination: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterTransferFromWallet { destination, amount },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterUpdatePayment(
    self,
    from: address,
    destination: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterUpdatePayment {
            destination,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterUpdateUser(
    self,
    from: address,
    destination: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterUpdateUser {
            destination,
            payload: payload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterInstallUserVersion(
    self,
    from: address,
    jettonWalletCode: cell,
    component1: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterInstallUserVersion { jettonWalletCode, component1 },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterInstallPaymentVersion(
    self,
    from: address,
    component2: cell,
    component1: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterInstallPaymentVersion { component2, component1 },
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.sendXtrMasterNoop(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: XtrMasterNoop {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun XtrMaster.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun XtrMaster.getUserLatestVersion(self): uint32 {
    return net.runGetMethod(self.address, "get_user_latest_version");
}

fun XtrMaster.getWalletAddress(self, ownerAddress: any_address): any_address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun XtrMaster.getJettonData(self): XtrMasterJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun XtrMaster.getPaymentLatestVersion(self): uint32 {
    return net.runGetMethod(self.address, "get_payment_latest_version");
}
