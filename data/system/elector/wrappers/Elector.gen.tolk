/// Auto-generated wrapper for contract 'Elector'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/elector.types"

/// Storage `ElectorStorage` is defined in `../types/elector.types`
struct Elector {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Elector.fromStorage(
    storage: ElectorStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Elector {
    val stateInit = ContractState {
        code: build("Elector"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Elector { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Elector.fromAddress(address: address): Elector {
    return Elector { address };
}

/// Deploys the contract to the blockchain
fun Elector.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorTextComment(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorTextComment { tail },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorNewStake(
    self,
    from: address,
    queryId: uint64,
    validatorPubkey: uint256,
    stakeAt: uint32,
    maxFactor: uint32,
    adnlAddress: uint256,
    signature: bits512,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorNewStake {
            queryId,
            validatorPubkey,
            stakeAt,
            maxFactor,
            adnlAddress,
            signature: signature.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorRecoverStake(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorRecoverStake { queryId },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorUpgradeCode(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorUpgradeCode { queryId, newCode, tail },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorConfigSetAccepted(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorConfigSetAccepted { queryId },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorConfigSetRejected(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorConfigSetRejected { queryId },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorNewComplaint(
    self,
    from: address,
    queryId: uint64,
    electionId: uint32,
    complaint: ElectorComplaint,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorNewComplaint { queryId, electionId, complaint },
    });
    return net.send(from, genericMsg);
}

fun Elector.sendElectorSignedComplaintVote(
    self,
    from: address,
    queryId: uint64,
    signature: bits512,
    vote: ElectorComplaintVoteRequest,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ElectorSignedComplaintVote { queryId, signature, vote },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Elector.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Elector.activeElectionId(self): uint32 {
    return net.runGetMethod(self.address, "active_election_id");
}

fun Elector.participatesIn(self, validatorPubkey: uint256): coins {
    return net.runGetMethod(self.address, "participates_in", [validatorPubkey]);
}

fun Elector.participantList(self): lisp_list<ElectorParticipantEntry> {
    return net.runGetMethod(self.address, "participant_list");
}

fun Elector.participantListExtended(self): ElectorParticipantListExtendedReply {
    return net.runGetMethod(self.address, "participant_list_extended");
}

fun Elector.computeReturnedStake(self, walletAddress: uint256): coins {
    return net.runGetMethod(self.address, "compute_returned_stake", [walletAddress]);
}

fun Elector.pastElectionIds(self): lisp_list<uint32> {
    return net.runGetMethod(self.address, "past_election_ids");
}

fun Elector.pastElections(self): lisp_list<ElectorPastElectionEntry> {
    return net.runGetMethod(self.address, "past_elections");
}

fun Elector.pastElectionsList(self): lisp_list<ElectorPastElectionShortEntry> {
    return net.runGetMethod(self.address, "past_elections_list");
}

fun Elector.getPastComplaints(self, electionId: uint32): cell? {
    return net.runGetMethod(self.address, "get_past_complaints", [electionId]);
}

fun Elector.showComplaint(self, electionId: uint32, complaintHash: uint256): [
    ElectorComplaintUnpacked,
    lisp_list<int>,
    uint256,
    int64,
]? {
    return net.runGetMethod(self.address, "show_complaint", [electionId, complaintHash]);
}

fun Elector.listComplaints(self, electionId: uint32): lisp_list<ElectorComplaintListEntry> {
    return net.runGetMethod(self.address, "list_complaints", [electionId]);
}

fun Elector.complaintStoragePrice(self, bits: int, refs: int, expireIn: int): coins {
    return net.runGetMethod(self.address, "complaint_storage_price", [bits, refs, expireIn]);
}
