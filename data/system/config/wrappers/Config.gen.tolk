/// Auto-generated wrapper for contract 'Config'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/config.types"

/// Storage `ConfigStorage` is defined in `../types/config.types`
struct Config {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Config.fromStorage(
    storage: ConfigStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Config {
    val stateInit = ContractState {
        code: build("Config"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Config { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Config.fromAddress(address: address): Config {
    return Config { address };
}

/// Deploys the contract to the blockchain
fun Config.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Config.sendConfigSetNextValidatorSetRequest(
    self,
    from: address,
    queryId: uint64,
    vset: ConfigValidatorSetExt,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfigSetNextValidatorSetRequest {
            queryId,
            vset: vset.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun Config.sendConfigNewVotingProposalRequest(
    self,
    from: address,
    queryId: uint64,
    expireAt: uint32,
    proposal: ConfigProposal,
    critical: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfigNewVotingProposalRequest {
            queryId,
            expireAt,
            proposal: proposal.toCell(),
            critical,
        },
    });
    return net.send(from, genericMsg);
}

fun Config.sendConfigVoteForProposalRequest(
    self,
    from: address,
    queryId: uint64,
    signature: bits512,
    signedBody: ConfigSignedVotePayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ConfigVoteForProposalRequest { queryId, signature, signedBody },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Config.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Config.sendExternalConfigIncomingExternalMessage(
    self,
    signature: bits512,
    action: ConfigExternalAction,
): ExternalSendResult {
    val body = ConfigIncomingExternalMessage { signature, action };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun Config.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun Config.seqno(self): int {
    return net.runGetMethod(self.address, "seqno");
}

fun Config.getProposal(self, proposalHash: int): ConfigProposalTuple? {
    return net.runGetMethod(self.address, "get_proposal", [proposalHash]);
}

fun Config.listProposals(self): lisp_list<ConfigProposalListEntry> {
    return net.runGetMethod(self.address, "list_proposals");
}

fun Config.proposalStoragePrice(self, critical: int, seconds: int, bits: int, refs: int): int {
    return net.runGetMethod(
        self.address,
        "proposal_storage_price",
        [critical, seconds, bits, refs],
    );
}
