/// Auto-generated wrapper for contract 'WtonMinter'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/wton_minter.types"

/// Storage `WtonMinterStorage` is defined in `../types/wton_minter.types`
struct WtonMinter {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun WtonMinter.fromStorage(
    storage: WtonMinterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): WtonMinter {
    val stateInit = ContractState {
        code: build("WtonMinter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return WtonMinter { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun WtonMinter.fromAddress(address: address): WtonMinter {
    return WtonMinter { address };
}

/// Deploys the contract to the blockchain
fun WtonMinter.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonDeposit(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonDeposit { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonWithdraw(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonWithdraw { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    ownerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonBurnNotification {
            queryId,
            amount,
            ownerAddress,
            responseDestination,
            customPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeOwnerAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonProvideWalletAddress { queryId, ownerAddress, includeOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonChangeNextAdmin(
    self,
    from: address,
    queryId: uint64,
    nextAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonChangeNextAdmin { queryId, nextAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonAcceptAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonAcceptAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonChangeContent(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonChangeContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.sendWtonIncomingExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: WtonIncomingExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun WtonMinter.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun WtonMinter.getJettonData(self): WtonJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun WtonMinter.getJettonExtraData(self): address? {
    return net.runGetMethod(self.address, "get_jetton_extra_data");
}

fun WtonMinter.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun WtonMinter.version(self): int {
    return net.runGetMethod(self.address, "version");
}
