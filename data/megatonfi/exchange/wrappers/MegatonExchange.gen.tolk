/// Auto-generated wrapper for contract 'MegatonExchange'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/megaton_exchange.types"

/// Storage `MegatonExchangeStorage` is defined in `../types/megaton_exchange.types`
struct MegatonExchange {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MegatonExchange.fromStorage(
    storage: MegatonExchangeStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MegatonExchange {
    val stateInit = ContractState {
        code: build("MegatonExchange"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MegatonExchange { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MegatonExchange.fromAddress(address: address): MegatonExchange {
    return MegatonExchange { address };
}

/// Deploys the contract to the blockchain
fun MegatonExchange.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeInitialize(
    self,
    from: address,
    queryId: uint64,
    totalSupply: coins,
    maxLiquidityProviderCount: uint32,
    tokenAWalletAddress: address,
    tokenBWalletAddress: address,
    miningSchedule: MegatonExchangeMiningSchedule,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeInitialize {
            queryId,
            totalSupply,
            maxLiquidityProviderCount,
            tokenAWalletAddress,
            tokenBWalletAddress,
            miningSchedule: miningSchedule.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeAddLiquidity(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    tokenAAmount: coins,
    tokenBAmount: coins,
    minimumTokenAAmount: coins,
    minimumTokenBAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeAddLiquidity {
            queryId,
            ownerAddress,
            tokenAAmount,
            tokenBAmount,
            minimumTokenAAmount,
            minimumTokenBAmount,
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeBurnLiquidity(
    self,
    from: address,
    queryId: uint64,
    lpAmount: coins,
    minimumAmounts: Cell<MegatonExchangeBurnMinimumAmounts>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeBurnLiquidity { queryId, lpAmount, minimumAmounts },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeRouterBurnLiquidity(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeRouterBurnLiquidity { queryId, ownerAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeOwnerAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeProvideWalletAddress { queryId, ownerAddress, includeOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeLpTransferCallback(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    recipientAddress: address,
    responseDestination: address?,
    forwardTonAmount: coins,
    originalOwner: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeLpTransferCallback {
            queryId,
            amount,
            recipientAddress,
            responseDestination,
            forwardTonAmount,
            originalOwner: originalOwner.toCell(),
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSwapNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderAddress: address,
    swapPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSwapNotification { queryId, amount, senderAddress, swapPayload },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeClaimRewards(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeClaimRewards { queryId },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeLpWalletRequest(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeLpWalletRequest { queryId },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeReleasePendingTokens(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    tokenMasterAddress: address,
    ownerAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeReleasePendingTokens {
            queryId,
            amount,
            tokenMasterAddress,
            ownerAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeUpdateMiningRate(
    self,
    from: address,
    queryId: uint64,
    miningRate: uint32,
    startsAt: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeUpdateMiningRate { queryId, miningRate, startsAt },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSetMiningAmount(
    self,
    from: address,
    queryId: uint64,
    miningAmount: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSetMiningAmount { queryId, miningAmount },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSetMinimumLpAmount(
    self,
    from: address,
    queryId: uint64,
    minimumLpAmount: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSetMinimumLpAmount { queryId, minimumLpAmount },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    adminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeChangeAdmin { queryId, adminAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeChangeRouter(
    self,
    from: address,
    queryId: uint64,
    routerAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeChangeRouter { queryId, routerAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeChangeContent(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeChangeContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSetSwapFee(
    self,
    from: address,
    queryId: uint64,
    swapFee: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSetSwapFee { queryId, swapFee },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSetStopped(
    self,
    from: address,
    queryId: uint64,
    stopped: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSetStopped { queryId, stopped },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSetMaxLiquidityProviders(
    self,
    from: address,
    queryId: uint64,
    maxLiquidityProviderCount: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSetMaxLiquidityProviders { queryId, maxLiquidityProviderCount },
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.sendMegatonExchangeSetOperationFee(
    self,
    from: address,
    queryId: uint64,
    operation: uint32,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonExchangeSetOperationFee { queryId, operation, amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MegatonExchange.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MegatonExchange.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun MegatonExchange.getJettonData(self): MegatonExchangeJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun MegatonExchange.getLpMiningData(self): MegatonExchangeMiningData {
    return net.runGetMethod(self.address, "get_lp_mining_data");
}

fun MegatonExchange.getLpSwapData(self): MegatonExchangeSwapData {
    return net.runGetMethod(self.address, "get_lp_swap_data");
}
