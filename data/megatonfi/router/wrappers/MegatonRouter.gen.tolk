/// Auto-generated wrapper for contract 'MegatonRouter'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/megaton_router.types"

/// Storage `MegatonRouterStorage` is defined in `../types/megaton_router.types`
struct MegatonRouter {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun MegatonRouter.fromStorage(
    storage: MegatonRouterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): MegatonRouter {
    val stateInit = ContractState {
        code: build("MegatonRouter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return MegatonRouter { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun MegatonRouter.fromAddress(address: address): MegatonRouter {
    return MegatonRouter { address };
}

/// Deploys the contract to the blockchain
fun MegatonRouter.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterCreatePool(
    self,
    from: address,
    queryId: uint64,
    token0: address,
    token1: address,
    fee: coins,
    parameter: uint32,
    addresses: MegatonRouterCreatePoolAddresses,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterCreatePool {
            queryId,
            token0,
            token1,
            fee,
            parameter,
            addresses: addresses.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterPoolCreated(
    self,
    from: address,
    queryId: uint64,
    token0: address,
    token1: address,
    poolTonAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterPoolCreated { queryId, token0, token1, poolTonAmount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterClaimMiningReward(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterClaimMiningReward { queryId, recipient, amount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    route: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterJettonTransferNotification { queryId, amount, sender, route },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterChangeNextAdmin(
    self,
    from: address,
    queryId: uint64,
    nextAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterChangeNextAdmin { queryId, nextAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterAcceptAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterAcceptAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolAdmin(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolAdmin { queryId, poolAddress, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolAddress(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    newAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolAddress { queryId, poolAddress, newAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolFactory(
    self,
    from: address,
    queryId: uint64,
    poolFactoryAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolFactory { queryId, poolFactoryAddress },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetMiningController(
    self,
    from: address,
    queryId: uint64,
    miningController: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetMiningController { queryId, miningController },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolController(
    self,
    from: address,
    queryId: uint64,
    poolController: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolController { queryId, poolController },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolContent(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterDistributeMiningShares(
    self,
    from: address,
    queryId: uint64,
    scheduleValue: uint64,
    shares: MegatonRouterMiningShares,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterDistributeMiningShares { queryId, scheduleValue, shares },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterBroadcastMiningAmount(
    self,
    from: address,
    queryId: uint64,
    totalMiningAmount: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterBroadcastMiningAmount { queryId, totalMiningAmount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolAmount(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolAmount { queryId, poolAddress, amount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetDirectTransfer(
    self,
    from: address,
    queryId: uint64,
    enabled: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetDirectTransfer { queryId, enabled },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterChangePoolContent(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterChangePoolContent { queryId, poolAddress, content },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetRoutedTransferTonAmount(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetRoutedTransferTonAmount { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolMiningShare(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    share: uint32,
    scheduleValue: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolMiningShare { queryId, poolAddress, share, scheduleValue },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetPoolMiningAmount(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    totalMiningAmount: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetPoolMiningAmount { queryId, poolAddress, totalMiningAmount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterSetMiningAmount(
    self,
    from: address,
    queryId: uint64,
    totalMiningAmount: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterSetMiningAmount { queryId, totalMiningAmount },
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.sendMegatonRouterReplacePoolRegistry(
    self,
    from: address,
    queryId: uint64,
    poolCount: uint16,
    pairHashToPool: map<uint256, address>,
    poolHashToPair: map<uint256, MegatonRouterPoolPair>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MegatonRouterReplacePoolRegistry {
            queryId,
            poolCount,
            pairHashToPool,
            poolHashToPair,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun MegatonRouter.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun MegatonRouter.getMiningData(self): MegatonRouterMiningData {
    return net.runGetMethod(self.address, "get_mining_data");
}

fun MegatonRouter.getLpData(self): MegatonRouterLpData {
    return net.runGetMethod(self.address, "get_lp_data");
}
