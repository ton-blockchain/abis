/// Auto-generated wrapper for contract 'FfVault'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/ff_vault.types"

/// Storage `FfVaultStorage` is defined in `../types/ff_vault.types`
struct FfVault {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun FfVault.fromStorage(
    storage: FfVaultStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): FfVault {
    val stateInit = ContractState {
        code: build("FfVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return FfVault { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun FfVault.fromAddress(address: address): FfVault {
    return FfVault { address };
}

/// Deploys the contract to the blockchain
fun FfVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultUnstakeExecute(
    self,
    from: address,
    queryId: uint64,
    priceUpdateData: cell,
    amountToUnstake: coins,
    itemIndex: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultUnstakeExecute { queryId, priceUpdateData, amountToUnstake, itemIndex },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultPythResponseSuccess(
    self,
    from: address,
    feedsCount: uint8,
    priceFeeds: cell,
    initialSender: address,
    operation: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultPythResponseSuccess { feedsCount, priceFeeds, initialSender, operation },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultPythErrorResponse(
    self,
    from: address,
    errorCode: uint32,
    operationCode: uint32,
    operation: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultPythErrorResponse { errorCode, operationCode, operation },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultWithdrawJetton(
    self,
    from: address,
    queryId: uint64,
    jettonWallet: address,
    toAddress: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultWithdrawJetton { queryId, jettonWallet, toAddress, amount },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultUpgrade(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultUpgrade { queryId, newCode },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultUnstakeExecuteInternalCallback(
    self,
    from: address,
    queryId: uint64,
    itemIndex: uint64,
    amountToUnstake: coins,
    assetPriceActual: uint256,
    owner: address,
    positionData: FfVaultPositionData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultUnstakeExecuteInternalCallback {
            queryId,
            itemIndex,
            amountToUnstake,
            assetPriceActual,
            owner,
            positionData: positionData.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun FfVault.sendFfVaultChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdmin: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FfVaultChangeAdmin { queryId, newAdmin },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FfVault.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun FfVault.getCollectionData(self): FfVaultCollectionData {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun FfVault.getNftAddressByIndex(self, itemIndex: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [itemIndex]);
}

fun FfVault.getNftContent(self, itemIndex: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [itemIndex, individualContent]);
}

fun FfVault.getStakingData(self): FfVaultStakingData {
    return net.runGetMethod(self.address, "get_staking_data");
}

fun FfVault.getBalance(self): FfVaultBalance {
    return net.runGetMethod(self.address, "get_balance");
}
