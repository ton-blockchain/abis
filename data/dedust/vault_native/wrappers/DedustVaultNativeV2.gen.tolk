/// Auto-generated wrapper for contract 'DedustVaultNativeV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_vault_native_v2.types"

/// Storage `DedustVaultNativeV2Storage` is defined in `../types/dedust_vault_native_v2.types`
struct DedustVaultNativeV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustVaultNativeV2.fromStorage(
    storage: DedustVaultNativeV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustVaultNativeV2 {
    val stateInit = ContractState {
        code: build("DedustVaultNativeV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustVaultNativeV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustVaultNativeV2.fromAddress(address: address): DedustVaultNativeV2 {
    return DedustVaultNativeV2 { address };
}

/// Deploys the contract to the blockchain
fun DedustVaultNativeV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.sendDedustVaultNativeV2Swap(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    step: DedustSwapStep,
    swapParams: DedustSwapParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustVaultNativeV2Swap {
            queryId,
            amount,
            step,
            swapParams: swapParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.sendDedustVaultNativeV2DepositLiquidity(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    poolParams: DedustPoolParams,
    params: DedustDepositLiquidityParams,
    fulfillPayload: cell?,
    rejectPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustVaultNativeV2DepositLiquidity {
            queryId,
            amount,
            poolParams,
            params: params.toCell(),
            fulfillPayload,
            rejectPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.sendDedustVaultNativeV2PayOutFromPool(
    self,
    from: address,
    queryId: uint64,
    proof: cell,
    amount: coins,
    recipientAddr: address?,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustVaultNativeV2PayOutFromPool { queryId, proof, amount, recipientAddr, payload },
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.sendDedustVaultNativeV2PayOutFromDeposit(
    self,
    from: address,
    queryId: uint64,
    proof: cell,
    amount: coins,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustVaultNativeV2PayOutFromDeposit { queryId, proof, amount, payload },
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.sendDedustVaultNativeV2ProvideVaultState(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustVaultNativeV2ProvideVaultState { queryId },
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.sendDedustVaultNativeV2ResetGas(
    self,
    from: address,
    queryId: uint64,
    responseDestination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustVaultNativeV2ResetGas { queryId, responseDestination },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustVaultNativeV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DedustVaultNativeV2.getVersion(self): uint16 {
    return net.runGetMethod(self.address, "get_version");
}

fun DedustVaultNativeV2.getFactoryAddr(self): address {
    return net.runGetMethod(self.address, "get_factory_addr");
}

fun DedustVaultNativeV2.getAsset(self): slice {
    return net.runGetMethod(self.address, "get_asset");
}
