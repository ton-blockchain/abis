/// Auto-generated wrapper for contract 'DedustUranusMemeV3'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_uranus_meme_v3.types"

/// Storage `DedustUranusMemeV3Storage` is defined in `../types/dedust_uranus_meme_v3.types`
struct DedustUranusMemeV3 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustUranusMemeV3.fromStorage(
    storage: DedustUranusMemeV3Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustUranusMemeV3 {
    val stateInit = ContractState {
        code: build("DedustUranusMemeV3"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustUranusMemeV3 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustUranusMemeV3.fromAddress(address: address): DedustUranusMemeV3 {
    return DedustUranusMemeV3 { address };
}

/// Deploys the contract to the blockchain
fun DedustUranusMemeV3.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusInitMeme(
    self,
    from: address,
    queryId: uint64,
    initialBuy: coins,
    partnerConfig: DedustUranusPartnerConfig?,
    referrerConfig: DedustUranusReferrerConfig?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusInitMeme { queryId, initialBuy, partnerConfig, referrerConfig },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusBuyMessage(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    minimalAmountOut: coins,
    excessesTo: address,
    partnerConfig: Cell<DedustUranusPartnerConfig>?,
    referrerConfig: Cell<DedustUranusReferrerConfig>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusBuyMessage {
            queryId,
            amount,
            minimalAmountOut,
            excessesTo,
            partnerConfig,
            referrerConfig,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusSellMessage(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    minimalAmountOut: coins,
    from_: address,
    excessesTo: address?,
    partnerConfig: Cell<DedustUranusPartnerConfig>?,
    referrerConfig: Cell<DedustUranusReferrerConfig>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusSellMessage {
            queryId,
            amount,
            minimalAmountOut,
            from: from_,
            excessesTo,
            partnerConfig,
            referrerConfig,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address?,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusClaimCreatorFee(
    self,
    from: address,
    queryId: uint64,
    to: address?,
    excessesTo: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusClaimCreatorFee { queryId, to, excessesTo },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusClaimPartnerFee(
    self,
    from: address,
    queryId: uint64,
    to: address?,
    excessesTo: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusClaimPartnerFee { queryId, to, excessesTo },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusOpcodeF14b54f3(
    self,
    from: address,
    queryId: uint64,
    to: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusOpcodeF14b54f3 { queryId, to },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.sendDedustUranusCpmmInitResult(
    self,
    from: address,
    queryId: uint64,
    exitCode: int32,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusCpmmInitResult { queryId, exitCode, payload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustUranusMemeV3.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeV3.getMemeData(self): DedustUranusGetMemeDataReply {
    return net.runGetMethod(self.address, "get_meme_data");
}

fun DedustUranusMemeV3.getBondingCurveData(self): DedustUranusGetBondingCurveDataReply {
    return net.runGetMethod(self.address, "get_bonding_curve_data");
}

fun DedustUranusMemeV3.getPartnerData(self): DedustUranusGetPartnerDataReply {
    return net.runGetMethod(self.address, "get_partner_data");
}

fun DedustUranusMemeV3.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun DedustUranusMemeV3.getJettonData(self): DedustUranusJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}
