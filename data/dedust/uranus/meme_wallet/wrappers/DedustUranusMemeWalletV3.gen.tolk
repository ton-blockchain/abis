/// Auto-generated wrapper for contract 'DedustUranusMemeWalletV3'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_uranus_meme_wallet_v3.types"

/// Storage `DedustUranusMemeWalletV3Storage` is defined in `../types/dedust_uranus_meme_wallet_v3.types`
struct DedustUranusMemeWalletV3 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustUranusMemeWalletV3.fromStorage(
    storage: DedustUranusMemeWalletV3Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustUranusMemeWalletV3 {
    val stateInit = ContractState {
        code: build("DedustUranusMemeWalletV3"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustUranusMemeWalletV3 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustUranusMemeWalletV3.fromAddress(address: address): DedustUranusMemeWalletV3 {
    return DedustUranusMemeWalletV3 { address };
}

/// Deploys the contract to the blockchain
fun DedustUranusMemeWalletV3.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeWalletV3.sendDedustUranusJettonTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusJettonTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeWalletV3.sendDedustUranusJettonInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    from_: address,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusJettonInternalTransfer {
            queryId,
            amount,
            from: from_,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeWalletV3.sendDedustUranusJettonBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusJettonBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeWalletV3.sendDedustUranusSellTokensMessage(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    minimalAmountOut: coins,
    excessesTo: address?,
    partnerConfig: Cell<DedustUranusPartnerConfig>?,
    referrerConfig: Cell<DedustUranusReferrerConfig>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusSellTokensMessage {
            queryId,
            amount,
            minimalAmountOut,
            excessesTo,
            partnerConfig,
            referrerConfig,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustUranusMemeWalletV3.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DedustUranusMemeWalletV3.getWalletData(self): DedustUranusMemeWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}
