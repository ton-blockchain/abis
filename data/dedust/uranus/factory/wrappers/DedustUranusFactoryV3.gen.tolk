/// Auto-generated wrapper for contract 'DedustUranusFactoryV3'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_uranus_factory_v3.types"

/// Storage `DedustUranusFactoryV3Storage` is defined in `../types/dedust_uranus_factory_v3.types`
struct DedustUranusFactoryV3 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustUranusFactoryV3.fromStorage(
    storage: DedustUranusFactoryV3Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustUranusFactoryV3 {
    val stateInit = ContractState {
        code: build("DedustUranusFactoryV3"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustUranusFactoryV3 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustUranusFactoryV3.fromAddress(address: address): DedustUranusFactoryV3 {
    return DedustUranusFactoryV3 { address };
}

/// Deploys the contract to the blockchain
fun DedustUranusFactoryV3.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusFactoryV3.sendDedustUranusDeployMeme(
    self,
    from: address,
    queryId: uint64,
    presetId: uint4,
    metadata: DedustUranusMemeMetadata,
    initialBuy: coins,
    partnerConfig: DedustUranusPartnerConfig?,
    referrerConfig: DedustUranusReferrerConfig?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusDeployMeme {
            queryId,
            presetId,
            metadata,
            initialBuy,
            partnerConfig,
            referrerConfig,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustUranusFactoryV3.sendDedustUranusDeployCustomizedMeme(
    self,
    from: address,
    queryId: uint64,
    totalSupplyPresetId: uint4,
    baseFeePresetId: uint4,
    raisingFunds: coins,
    onSellSupplyPercent: uint8,
    partnerAddress: address,
    partnerFeeBps: uint16,
    poolPartnerFeeBps: uint16,
    poolBaseFeePresetId: uint16,
    poolLiquidityOwnerAddress: address?,
    metadataUri: string,
    initialBuy: coins,
    partnerConfig: Cell<DedustUranusPartnerConfig>?,
    referrerConfig: Cell<DedustUranusReferrerConfig>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustUranusDeployCustomizedMeme {
            queryId,
            totalSupplyPresetId,
            baseFeePresetId,
            raisingFunds,
            onSellSupplyPercent,
            partnerAddress,
            partnerFeeBps,
            poolPartnerFeeBps,
            poolBaseFeePresetId,
            poolLiquidityOwnerAddress,
            metadataUri,
            initialBuy,
            partnerConfig,
            referrerConfig,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustUranusFactoryV3.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
