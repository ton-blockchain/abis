/// Auto-generated wrapper for contract 'DedustFactoryV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_factory_v2.types"

/// Storage `DedustFactoryV2Storage` is defined in `../types/dedust_factory_v2.types`
struct DedustFactoryV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustFactoryV2.fromStorage(
    storage: DedustFactoryV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustFactoryV2 {
    val stateInit = ContractState {
        code: build("DedustFactoryV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustFactoryV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustFactoryV2.fromAddress(address: address): DedustFactoryV2 {
    return DedustFactoryV2 { address };
}

/// Deploys the contract to the blockchain
fun DedustFactoryV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2TransferOwnership(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2TransferOwnership { queryId, newOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2AcceptOwnership(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2AcceptOwnership { queryId },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2CancelOwnershipTransfer(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2CancelOwnershipTransfer { queryId },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2InstallVaultCode(
    self,
    from: address,
    queryId: uint64,
    assetType: uint4,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2InstallVaultCode { queryId, assetType, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2CreateVault(
    self,
    from: address,
    queryId: uint64,
    asset: DedustFactoryV2Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2CreateVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2CreateLegacyJettonVault(
    self,
    from: address,
    queryId: uint64,
    minterAddress: address,
    resolverAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2CreateLegacyJettonVault { queryId, minterAddress, resolverAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2UpgradeVault(
    self,
    from: address,
    queryId: uint64,
    asset: DedustFactoryV2Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2UpgradeVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2DestroyNonReadyVault(
    self,
    from: address,
    queryId: uint64,
    asset: DedustFactoryV2Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2DestroyNonReadyVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2Upgrade(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2Upgrade { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2DeployOperator(
    self,
    from: address,
    queryId: uint64,
    role: uint8,
    userAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2DeployOperator { queryId, role, userAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2ChangeOperatorUserAddress(
    self,
    from: address,
    queryId: uint64,
    role: uint8,
    userAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2ChangeOperatorUserAddress { queryId, role, userAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2InstallOperatorCode(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2InstallOperatorCode { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2UpgradeOperator(
    self,
    from: address,
    queryId: uint64,
    role: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2UpgradeOperator { queryId, role },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2InstallPoolCode(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2InstallPoolCode { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2CreateVolatilePool(
    self,
    from: address,
    queryId: uint64,
    asset0: DedustFactoryV2Asset,
    asset1: DedustFactoryV2Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2CreateVolatilePool { queryId, asset0, asset1 },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2CreateStablePool(
    self,
    from: address,
    queryId: uint64,
    asset0: DedustFactoryV2Asset,
    asset0Decimals: uint8,
    asset1: DedustFactoryV2Asset,
    asset1Decimals: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2CreateStablePool {
            queryId,
            asset0,
            asset0Decimals,
            asset1,
            asset1Decimals,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2UpgradePool(
    self,
    from: address,
    queryId: uint64,
    poolParams: DedustFactoryV2PoolParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2UpgradePool { queryId, poolParams },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2InstallLiquidityDepositCode(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2InstallLiquidityDepositCode { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.sendDedustFactoryV2CreateLiquidityDeposit(
    self,
    from: address,
    queryId: uint64,
    proof: DedustFactoryV2VaultProof,
    ownerAddress: address,
    poolParams: DedustFactoryV2PoolParams,
    extra: DedustFactoryV2CreateLiquidityDepositExtra,
    fulfillPayload: cell?,
    rejectPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV2CreateLiquidityDeposit {
            queryId,
            proof: proof.toCell(),
            ownerAddress,
            poolParams,
            extra: extra.toCell(),
            fulfillPayload,
            rejectPayload,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustFactoryV2.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV2.getOwnership(self): DedustFactoryV2Ownership {
    return net.runGetMethod(self.address, "get_ownership");
}

fun DedustFactoryV2.getVersion(self): uint16 {
    return net.runGetMethod(self.address, "get_version");
}

fun DedustFactoryV2.getVaultAddress(self, asset: slice): address {
    return net.runGetMethod(self.address, "get_vault_address", [asset]);
}

fun DedustFactoryV2.getPoolAddress(self, poolType: int, asset0: slice, asset1: slice): address {
    return net.runGetMethod(self.address, "get_pool_address", [poolType, asset0, asset1]);
}

fun DedustFactoryV2.getLiquidityDepositAddress(
    self,
    ownerAddress: address,
    poolType: int,
    asset0: slice,
    asset1: slice,
): address {
    return net.runGetMethod(
        self.address,
        "get_liquidity_deposit_address",
        [ownerAddress, poolType, asset0, asset1],
    );
}
