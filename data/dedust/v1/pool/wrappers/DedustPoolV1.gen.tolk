/// Auto-generated wrapper for contract 'DedustPoolV1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_pool_v1.types"

/// Storage `DedustPoolV1Storage` is defined in `../types/dedust_pool_v1.types`
struct DedustPoolV1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustPoolV1.fromStorage(
    storage: DedustPoolV1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustPoolV1 {
    val stateInit = ContractState {
        code: build("DedustPoolV1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustPoolV1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustPoolV1.fromAddress(address: address): DedustPoolV1 {
    return DedustPoolV1 { address };
}

/// Deploys the contract to the blockchain
fun DedustPoolV1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.sendDedustPoolV1UpgradeCode(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustPoolV1UpgradeCode { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.sendDedustPoolV1SwapExternal(
    self,
    from: address,
    queryId: uint64,
    proof: DedustPoolV1VaultProof,
    amount: coins,
    senderAddr: address,
    current: DedustPoolV1SwapStepParams,
    swapParams: DedustPoolV1SwapParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustPoolV1SwapExternal {
            queryId,
            proof: proof.toCell(),
            amount,
            senderAddr,
            current,
            swapParams: swapParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.sendDedustPoolV1SwapPeer(
    self,
    from: address,
    queryId: uint64,
    proof: DedustPoolV1PoolProof,
    asset: DedustPoolV1Asset,
    amount: coins,
    senderAddr: address,
    current: DedustPoolV1SwapStepParams,
    swapParams: DedustPoolV1SwapParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustPoolV1SwapPeer {
            queryId,
            proof: proof.toCell(),
            asset,
            amount,
            senderAddr,
            current,
            swapParams: swapParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.sendDedustPoolV1DepositLiquidityAll(
    self,
    from: address,
    queryId: uint64,
    proof: DedustPoolV1LiquidityDepositProof,
    ownerAddr: address,
    minLpAmount: coins,
    depositAmounts: DedustPoolV1DepositLiquidityAllAmounts,
    fulfillPayload: cell?,
    rejectPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustPoolV1DepositLiquidityAll {
            queryId,
            proof: proof.toCell(),
            ownerAddr,
            minLpAmount,
            depositAmounts: depositAmounts.toCell(),
            fulfillPayload,
            rejectPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.sendDedustPoolV1JettonBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustPoolV1JettonBurnNotification { queryId, amount, sender, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.sendDedustPoolV1WithdrawProtocolFees(
    self,
    from: address,
    queryId: uint64,
    recipientAddr: address,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustPoolV1WithdrawProtocolFees { queryId, recipientAddr, payload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustPoolV1.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DedustPoolV1.getVersion(self): uint16 {
    return net.runGetMethod(self.address, "get_version");
}

fun DedustPoolV1.isStable(self): bool {
    return net.runGetMethod(self.address, "is_stable");
}

fun DedustPoolV1.getTradeFee(self): DedustPoolV1GetTradeFeeReply {
    return net.runGetMethod(self.address, "get_trade_fee");
}

fun DedustPoolV1.getReserves(self): DedustPoolV1GetReservesReply {
    return net.runGetMethod(self.address, "get_reserves");
}

fun DedustPoolV1.getProtocolFees(self): DedustPoolV1GetProtocolFeesReply {
    return net.runGetMethod(self.address, "get_protocol_fees");
}

fun DedustPoolV1.getAssets(self): DedustPoolV1GetAssetsReply {
    return net.runGetMethod(self.address, "get_assets");
}

fun DedustPoolV1.estimateSwapOut(
    self,
    assetIn: slice,
    amountIn: int,
): DedustPoolV1EstimateSwapOutReply {
    return net.runGetMethod(self.address, "estimate_swap_out", [assetIn, amountIn]);
}

fun DedustPoolV1.estimateDepositOut(
    self,
    amount0: int,
    amount1: int,
): DedustPoolV1EstimateDepositOutReply {
    return net.runGetMethod(self.address, "estimate_deposit_out", [amount0, amount1]);
}

fun DedustPoolV1.getJettonData(self): DedustPoolV1JettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun DedustPoolV1.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}
