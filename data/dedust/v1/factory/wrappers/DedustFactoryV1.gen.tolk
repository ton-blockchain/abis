/// Auto-generated wrapper for contract 'DedustFactoryV1'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dedust_factory_v1.types"

/// Storage `DedustFactoryV1Storage` is defined in `../types/dedust_factory_v1.types`
struct DedustFactoryV1 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DedustFactoryV1.fromStorage(
    storage: DedustFactoryV1Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DedustFactoryV1 {
    val stateInit = ContractState {
        code: build("DedustFactoryV1"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DedustFactoryV1 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DedustFactoryV1.fromAddress(address: address): DedustFactoryV1 {
    return DedustFactoryV1 { address };
}

/// Deploys the contract to the blockchain
fun DedustFactoryV1.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1TransferOwnership(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1TransferOwnership { queryId, newOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1AcceptOwnership(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1AcceptOwnership { tail },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1CancelOwnershipTransfer(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1CancelOwnershipTransfer { tail },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1InstallVaultCode(
    self,
    from: address,
    queryId: uint64,
    assetType: uint4,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1InstallVaultCode { queryId, assetType, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1CreateVault(
    self,
    from: address,
    queryId: uint64,
    asset: DedustFactoryV1Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1CreateVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1CreateLegacyJettonVault(
    self,
    from: address,
    queryId: uint64,
    minterAddress: address,
    resolverAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1CreateLegacyJettonVault { queryId, minterAddress, resolverAddress },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1UpgradeVault(
    self,
    from: address,
    queryId: uint64,
    asset: DedustFactoryV1Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1UpgradeVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1DestroyNonReadyVault(
    self,
    from: address,
    queryId: uint64,
    asset: DedustFactoryV1Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1DestroyNonReadyVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1Upgrade(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1Upgrade { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1ResetGas(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1ResetGas { tail },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1InstallPoolCode(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1InstallPoolCode { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1CreateVolatilePool(
    self,
    from: address,
    queryId: uint64,
    asset0: DedustFactoryV1Asset,
    asset1: DedustFactoryV1Asset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1CreateVolatilePool { queryId, asset0, asset1 },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1CreateStablePool(
    self,
    from: address,
    queryId: uint64,
    asset0: DedustFactoryV1Asset,
    asset0Decimals: uint8,
    asset1: DedustFactoryV1Asset,
    asset1Decimals: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1CreateStablePool {
            queryId,
            asset0,
            asset0Decimals,
            asset1,
            asset1Decimals,
        },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1UpgradePool(
    self,
    from: address,
    queryId: uint64,
    poolParams: DedustFactoryV1PoolParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1UpgradePool { queryId, poolParams },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1InstallLiquidityDepositCode(
    self,
    from: address,
    queryId: uint64,
    codeVersion: uint16,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1InstallLiquidityDepositCode { queryId, codeVersion, code },
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.sendDedustFactoryV1CreateLiquidityDeposit(
    self,
    from: address,
    queryId: uint64,
    proof: DedustFactoryV1VaultProof,
    ownerAddress: address,
    poolParams: DedustFactoryV1PoolParams,
    extra: DedustFactoryV1CreateLiquidityDepositExtra,
    fulfillPayload: cell?,
    rejectPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DedustFactoryV1CreateLiquidityDeposit {
            queryId,
            proof: proof.toCell(),
            ownerAddress,
            poolParams,
            extra: extra.toCell(),
            fulfillPayload,
            rejectPayload,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DedustFactoryV1.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DedustFactoryV1.getOwnership(self): DedustFactoryV1Ownership {
    return net.runGetMethod(self.address, "get_ownership");
}

fun DedustFactoryV1.getVersion(self): uint16 {
    return net.runGetMethod(self.address, "get_version");
}

fun DedustFactoryV1.getVaultAddress(self, asset: slice): address {
    return net.runGetMethod(self.address, "get_vault_address", [asset]);
}

fun DedustFactoryV1.getPoolAddress(self, poolType: int, asset0: slice, asset1: slice): address {
    return net.runGetMethod(self.address, "get_pool_address", [poolType, asset0, asset1]);
}

fun DedustFactoryV1.getLiquidityDepositAddress(
    self,
    ownerAddress: address,
    poolType: int,
    asset0: slice,
    asset1: slice,
): address {
    return net.runGetMethod(
        self.address,
        "get_liquidity_deposit_address",
        [ownerAddress, poolType, asset0, asset1],
    );
}
