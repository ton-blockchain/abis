/// Auto-generated wrapper for contract 'PtonWalletV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/pton_wallet_v2.types"

/// Storage `PtonWalletV2Storage` is defined in `../types/pton_wallet_v2.types`
struct PtonWalletV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun PtonWalletV2.fromStorage(
    storage: PtonWalletV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): PtonWalletV2 {
    val stateInit = ContractState {
        code: build("PtonWalletV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return PtonWalletV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun PtonWalletV2.fromAddress(address: address): PtonWalletV2 {
    return PtonWalletV2 { address };
}

/// Deploys the contract to the blockchain
fun PtonWalletV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun PtonWalletV2.sendPtonWalletV2TonTransfer(
    self,
    from: address,
    queryId: uint64,
    tonAmount: coins,
    refundAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonWalletV2TonTransfer { queryId, tonAmount, refundAddress, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun PtonWalletV2.sendPtonWalletV2InternalDeploy(
    self,
    from: address,
    queryId: uint64,
    excessesAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonWalletV2InternalDeploy { queryId, excessesAddress },
    });
    return net.send(from, genericMsg);
}

fun PtonWalletV2.sendPtonWalletV2Transfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonWalletV2Transfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun PtonWalletV2.sendPtonWalletV2ResetGas(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonWalletV2ResetGas { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PtonWalletV2.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PtonWalletV2.getWalletData(self): PtonWalletV2WalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}
