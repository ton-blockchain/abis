/// Auto-generated wrapper for contract 'PtonMinterV2'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/pton_minter_v2.types"

/// Storage `PtonMinterV2Storage` is defined in `../types/pton_minter_v2.types`
struct PtonMinterV2 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun PtonMinterV2.fromStorage(
    storage: PtonMinterV2Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): PtonMinterV2 {
    val stateInit = ContractState {
        code: build("PtonMinterV2"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return PtonMinterV2 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun PtonMinterV2.fromAddress(address: address): PtonMinterV2 {
    return PtonMinterV2 { address };
}

/// Deploys the contract to the blockchain
fun PtonMinterV2.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun PtonMinterV2.sendPtonMinterV2DeployWallet(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    excessesAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonMinterV2DeployWallet { queryId, ownerAddress, excessesAddress },
    });
    return net.send(from, genericMsg);
}

fun PtonMinterV2.sendPtonMinterV2ProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PtonMinterV2ProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PtonMinterV2.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PtonMinterV2.getJettonData(self): PtonMinterV2JettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun PtonMinterV2.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun PtonMinterV2.getVersion(self): PtonMinterV2VersionReply {
    return net.runGetMethod(self.address, "get_version");
}
