/// Auto-generated wrapper for contract 'FrtGramAdapterWorker'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/frt_gram_worker.types"

/// Storage `FrtGramAdapterWorkerStorage` is defined in `../types/frt_gram_worker.types`
struct FrtGramAdapterWorker {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun FrtGramAdapterWorker.fromStorage(
    storage: FrtGramAdapterWorkerStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): FrtGramAdapterWorker {
    val stateInit = ContractState {
        code: build("FrtGramAdapterWorker"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return FrtGramAdapterWorker { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun FrtGramAdapterWorker.fromAddress(address: address): FrtGramAdapterWorker {
    return FrtGramAdapterWorker { address };
}

/// Deploys the contract to the blockchain
fun FrtGramAdapterWorker.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun FrtGramAdapterWorker.sendFrtGramWorkerRouteState(
    self,
    from: address,
    sequence: uint32,
    destination: address,
    action: uint8,
    state: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramWorkerRouteState { sequence, destination, action, state },
    });
    return net.send(from, genericMsg);
}

fun FrtGramAdapterWorker.sendFrtGramWorkerStepRequest(
    self,
    from: address,
    sequence: uint32,
    amount: coins,
    destination: address,
    enabled: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramWorkerStepRequest { sequence, amount, destination, enabled },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FrtGramAdapterWorker.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun FrtGramAdapterWorker.getMethod67522Ffrz(self): uint32 {
    return net.runGetMethod(self.address, "get_method_67522_ffrz");
}

fun FrtGramAdapterWorker.info(self): (uint16, uint16, address, address) {
    return net.runGetMethod(self.address, "info");
}

fun FrtGramAdapterWorker.getMethod78997Blse(self, index: int): any_address {
    return net.runGetMethod(self.address, "get_method_78997_blse", [index]);
}

fun FrtGramAdapterWorker.getMethod83655Chdd(self): uint32 {
    return net.runGetMethod(self.address, "get_method_83655_chdd");
}

fun FrtGramAdapterWorker.getMethod101138Cdtm(self, index: int): uint8 {
    return net.runGetMethod(self.address, "get_method_101138_cdtm", [index]);
}

fun FrtGramAdapterWorker.getMethod109070Adqd(self): any_address {
    return net.runGetMethod(self.address, "get_method_109070_adqd");
}

fun FrtGramAdapterWorker.getMethod111858Bnct(self, index: int): coins {
    return net.runGetMethod(self.address, "get_method_111858_bnct", [index]);
}

fun FrtGramAdapterWorker.getMethod130594Ehro(self): any_address {
    return net.runGetMethod(self.address, "get_method_130594_ehro");
}
