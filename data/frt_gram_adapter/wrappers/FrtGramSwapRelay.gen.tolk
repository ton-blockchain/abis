/// Auto-generated wrapper for contract 'FrtGramSwapRelay'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/frt_gram_swap_relay.types"

/// Storage `FrtGramSwapRelayDeploymentStorage` is defined in `../types/frt_gram_swap_relay.types`
struct FrtGramSwapRelay {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun FrtGramSwapRelay.fromStorage(
    storage: FrtGramSwapRelayDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): FrtGramSwapRelay {
    val stateInit = ContractState {
        code: build("FrtGramSwapRelay"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return FrtGramSwapRelay { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun FrtGramSwapRelay.fromAddress(address: address): FrtGramSwapRelay {
    return FrtGramSwapRelay { address };
}

/// Deploys the contract to the blockchain
fun FrtGramSwapRelay.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapRelay.sendForTonFrtSwapRequest(
    self,
    from: address,
    receiver: address,
    routeAuthorization: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ForTonFrtSwapRequest { receiver, routeAuthorization },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FrtGramSwapRelay.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapRelay.getMethod68602Obmz(self): bool {
    return net.runGetMethod(self.address, "get_method_68602_obmz");
}
