/// Auto-generated wrapper for contract 'FrtGramSwapExecutor'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/frt_gram_swap_executor.types"

/// Storage `FrtGramSwapExecutorDeploymentStorage` is defined in `../types/frt_gram_swap_executor.types`
struct FrtGramSwapExecutor {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun FrtGramSwapExecutor.fromStorage(
    storage: FrtGramSwapExecutorDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): FrtGramSwapExecutor {
    val stateInit = ContractState {
        code: build("FrtGramSwapExecutor"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return FrtGramSwapExecutor { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun FrtGramSwapExecutor.fromAddress(address: address): FrtGramSwapExecutor {
    return FrtGramSwapExecutor { address };
}

/// Deploys the contract to the blockchain
fun FrtGramSwapExecutor.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendForTonFrtSwapRequest(
    self,
    from: address,
    receiver: address,
    routeAuthorization: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ForTonFrtSwapRequest { receiver, routeAuthorization },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendFrtGramSwapExecutorSetAuthorizationRoot(
    self,
    from: address,
    authorizationRoot: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramSwapExecutorSetAuthorizationRoot { authorizationRoot },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendFrtGramSwapExecutorSetPaused(
    self,
    from: address,
    paused: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramSwapExecutorSetPaused { paused },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendFrtGramSwapExecutorSetSecondaryController(
    self,
    from: address,
    secondaryController: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramSwapExecutorSetSecondaryController { secondaryController },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendFrtGramSwapExecutorSetPrimaryController(
    self,
    from: address,
    primaryController: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramSwapExecutorSetPrimaryController { primaryController },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendFrtGramSwapExecutorWithdrawGram(
    self,
    from: address,
    destination: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramSwapExecutorWithdrawGram { destination, amount },
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.sendFrtGramSwapExecutorDrainGram(
    self,
    from: address,
    destination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FrtGramSwapExecutorDrainGram { destination },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FrtGramSwapExecutor.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun FrtGramSwapExecutor.getMethod123059Enem(
    self,
    receiver: address,
    authorizationRoot: uint256,
): any_address {
    return net.runGetMethod(self.address, "get_method_123059_enem", [receiver, authorizationRoot]);
}

fun FrtGramSwapExecutor.isPaused(self): bool {
    return net.runGetMethod(self.address, "isPaused");
}

fun FrtGramSwapExecutor.getMethod126756Emdl(self): uint256 {
    return net.runGetMethod(self.address, "get_method_126756_emdl");
}
