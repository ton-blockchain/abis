/// Auto-generated wrapper for contract 'DnsDomainItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/dns_domain_item.types"

/// Storage `DnsDomainItemDeploymentStorage` is defined in `../types/dns_domain_item.types`
struct DnsDomainItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DnsDomainItem.fromStorage(
    storage: DnsDomainItemDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DnsDomainItem {
    val stateInit = ContractState {
        code: build("DnsDomainItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DnsDomainItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DnsDomainItem.fromAddress(address: address): DnsDomainItem {
    return DnsDomainItem { address };
}

/// Deploys the contract to the blockchain
fun DnsDomainItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.sendDnsDomainItemTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DnsDomainItemTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.sendDnsDomainItemEditContent(
    self,
    from: address,
    queryId: uint64,
    content: DnsDomainItemContent,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DnsDomainItemEditContent {
            queryId,
            content: content.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.sendDnsDomainItemChangeDnsRecord(
    self,
    from: address,
    queryId: uint64,
    key: uint256,
    value: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DnsDomainItemChangeDnsRecord { queryId, key, value },
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.sendDnsDomainItemProcessGovernanceDecision(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DnsDomainItemProcessGovernanceDecision { queryId },
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.sendDnsDomainItemBalanceRelease(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DnsDomainItemBalanceRelease { queryId },
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.sendDnsDomainItemGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DnsDomainItemGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DnsDomainItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DnsDomainItem.getNftData(self): DnsDomainItemDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun DnsDomainItem.getEditor(self): any_address {
    return net.runGetMethod(self.address, "get_editor");
}

fun DnsDomainItem.getDomain(self): slice {
    return net.runGetMethod(self.address, "get_domain");
}

fun DnsDomainItem.getAuctionInfo(self): DnsDomainAuctionInfoReply {
    return net.runGetMethod(self.address, "get_auction_info");
}

fun DnsDomainItem.getLastFillUpTime(self): int64 {
    return net.runGetMethod(self.address, "get_last_fill_up_time");
}

fun DnsDomainItem.dnsresolve(self, subdomain: slice, category: int): DnsDomainResolveReply {
    return net.runGetMethod(self.address, "dnsresolve", [subdomain, category]);
}
