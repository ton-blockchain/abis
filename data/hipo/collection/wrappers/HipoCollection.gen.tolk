/// Auto-generated wrapper for contract 'HipoCollection'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/hipo_collection.types"

/// Storage `HipoCollectionStorage` is defined in `../types/hipo_collection.types`
struct HipoCollection {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun HipoCollection.fromStorage(
    storage: HipoCollectionStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): HipoCollection {
    val stateInit = ContractState {
        code: build("HipoCollection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return HipoCollection { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun HipoCollection.fromAddress(address: address): HipoCollection {
    return HipoCollection { address };
}

/// Deploys the contract to the blockchain
fun HipoCollection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun HipoCollection.sendHipoCollectionMintBill(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    unstake: bool,
    owner: address,
    parent: address,
    ownershipAssignedAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoCollectionMintBill {
            queryId,
            amount,
            unstake,
            owner,
            parent,
            ownershipAssignedAmount,
        },
    });
    return net.send(from, genericMsg);
}

fun HipoCollection.sendHipoCollectionBillBurned(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    unstake: bool,
    owner: address,
    parent: address,
    index: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoCollectionBillBurned { queryId, amount, unstake, owner, parent, index },
    });
    return net.send(from, genericMsg);
}

fun HipoCollection.sendHipoCollectionBurnAll(
    self,
    from: address,
    queryId: uint64,
    startIndex: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoCollectionBurnAll { queryId, startIndex },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun HipoCollection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun HipoCollection.getCollectionData(self): HipoCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun HipoCollection.getNftAddressByIndex(self, index: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [index]);
}

fun HipoCollection.getNftContent(self, index: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [index, individualContent]);
}
