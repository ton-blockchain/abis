/// Auto-generated wrapper for contract 'HipoLoan'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/hipo_loan.types"

/// Storage `HipoLoanStorage` is defined in `../types/hipo_loan.types`
struct HipoLoan {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun HipoLoan.fromStorage(
    storage: HipoLoanStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): HipoLoan {
    val stateInit = ContractState {
        code: build("HipoLoan"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return HipoLoan { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun HipoLoan.fromAddress(address: address): HipoLoan {
    return HipoLoan { address };
}

/// Deploys the contract to the blockchain
fun HipoLoan.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanProxyNewStake(
    self,
    from: address,
    queryId: uint64,
    newStakeMsg: HipoLoanNewStakeMsg,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanProxyNewStake {
            queryId,
            newStakeMsg: newStakeMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanProxyRecoverStake(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanProxyRecoverStake { queryId },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanNewStakeError(
    self,
    from: address,
    queryId: uint64,
    reason: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanNewStakeError { queryId, reason },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanNewStakeOk(
    self,
    from: address,
    queryId: uint64,
    comment: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanNewStakeOk { queryId, comment },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanRecoverStakeError(
    self,
    from: address,
    queryId: uint64,
    reason: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanRecoverStakeError { queryId, reason },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanRecoverStakeOk(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanRecoverStakeOk { queryId },
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.sendHipoLoanTopUp(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoLoanTopUp {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun HipoLoan.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun HipoLoan.getLoanState(self): HipoLoanState {
    return net.runGetMethod(self.address, "get_loan_state");
}
