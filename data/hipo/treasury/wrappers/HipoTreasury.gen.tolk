/// Auto-generated wrapper for contract 'HipoTreasury'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/hipo_treasury.types"

/// Storage `HipoTreasuryStorage` is defined in `../types/hipo_treasury.types`
struct HipoTreasury {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun HipoTreasury.fromStorage(
    storage: HipoTreasuryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): HipoTreasury {
    val stateInit = ContractState {
        code: build("HipoTreasury"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return HipoTreasury { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun HipoTreasury.fromAddress(address: address): HipoTreasury {
    return HipoTreasury { address };
}

/// Deploys the contract to the blockchain
fun HipoTreasury.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryDepositCoins(
    self,
    from: address,
    queryId: uint64,
    owner: address?,
    coins: coins,
    ownershipAssignedAmount: coins,
    referrer: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryDepositCoins { queryId, owner, coins, ownershipAssignedAmount, referrer },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySendUnstakeAll(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySendUnstakeAll { queryId },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryReserveTokens(
    self,
    from: address,
    queryId: uint64,
    tokens: coins,
    owner: address,
    mode: uint4,
    ownershipAssignedAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryReserveTokens { queryId, tokens, owner, mode, ownershipAssignedAmount },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryMintTokens(
    self,
    from: address,
    queryId: uint64,
    coins: coins,
    owner: address,
    parent: address,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryMintTokens { queryId, coins, owner, parent, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryBurnTokens(
    self,
    from: address,
    queryId: uint64,
    tokens: coins,
    owner: address,
    parent: address,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryBurnTokens { queryId, tokens, owner, parent, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRequestLoan(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    loanAmount: coins,
    minPayment: coins,
    borrowerRewardShare: uint8,
    newStakeMsg: HipoTreasuryNewStakeMsg,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRequestLoan {
            queryId,
            roundSince,
            loanAmount,
            minPayment,
            borrowerRewardShare,
            newStakeMsg: newStakeMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryParticipateInElection(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryParticipateInElection { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryDecideLoanRequests(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryDecideLoanRequests { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProcessLoanRequests(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProcessLoanRequests { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryVsetChanged(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryVsetChanged { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryFinishParticipation(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryFinishParticipation { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRecoverStakes(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRecoverStakes { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRecoverStakeResult(
    self,
    from: address,
    queryId: uint64,
    ok: bool,
    borrower: address,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRecoverStakeResult { queryId, ok, borrower, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryLastBillBurned(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryLastBillBurned { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProposeGovernor(
    self,
    from: address,
    queryId: uint64,
    newGovernor: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProposeGovernor { queryId, newGovernor },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryAcceptGovernance(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryAcceptGovernance { queryId },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySetHalter(
    self,
    from: address,
    queryId: uint64,
    newHalter: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySetHalter { queryId, newHalter },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySetStopped(
    self,
    from: address,
    queryId: uint64,
    newStopped: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySetStopped { queryId, newStopped },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySetInstantMint(
    self,
    from: address,
    queryId: uint64,
    newInstantMint: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySetInstantMint { queryId, newInstantMint },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySetGovernanceFee(
    self,
    from: address,
    queryId: uint64,
    newGovernanceFee: uint16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySetGovernanceFee { queryId, newGovernanceFee },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySetRoundsImbalance(
    self,
    from: address,
    queryId: uint64,
    newRoundsImbalance: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySetRoundsImbalance { queryId, newRoundsImbalance },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySendMessageToLoan(
    self,
    from: address,
    queryId: uint64,
    borrower: address,
    roundSince: uint32,
    message: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySendMessageToLoan { queryId, borrower, roundSince, message },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRetryDistribute(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRetryDistribute { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRetryRecoverStakes(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRetryRecoverStakes { queryId, roundSince },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRetryMintBill(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    amount: coins,
    unstake: bool,
    owner: address,
    parent: address,
    ownershipAssignedAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRetryMintBill {
            queryId,
            roundSince,
            amount,
            unstake,
            owner,
            parent,
            ownershipAssignedAmount,
        },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryRetryBurnAll(
    self,
    from: address,
    queryId: uint64,
    roundSince: uint32,
    startIndex: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryRetryBurnAll { queryId, roundSince, startIndex },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySetParent(
    self,
    from: address,
    queryId: uint64,
    newParent: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySetParent { queryId, newParent },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProxySetContent(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    newContent: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProxySetContent { queryId, destination, newContent },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryWithdrawSurplus(
    self,
    from: address,
    queryId: uint64,
    returnExcess: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryWithdrawSurplus { queryId, returnExcess },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProxyWithdrawSurplus(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProxyWithdrawSurplus { queryId, destination },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryUpgradeCode(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    newData: cell?,
    returnExcess: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryUpgradeCode { queryId, newCode, newData, returnExcess },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProxyUpgradeCode(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    newCode: cell,
    newData: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProxyUpgradeCode { queryId, destination, newCode, newData },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasurySendUpgradeWallet(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    owner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasurySendUpgradeWallet { queryId, destination, owner },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryMigrateWallet(
    self,
    from: address,
    queryId: uint64,
    tokens: coins,
    owner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryMigrateWallet { queryId, tokens, owner },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProxyAddLibrary(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProxyAddLibrary { queryId, destination, code },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryProxyRemoveLibrary(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    codeHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryProxyRemoveLibrary { queryId, destination, codeHash },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryGiftCoins(
    self,
    from: address,
    queryId: uint64,
    coins: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryGiftCoins { queryId, coins },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryTopUp(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryTopUp { queryId },
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendHipoTreasuryTextCommand(
    self,
    from: address,
    command: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HipoTreasuryTextCommand { command },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun HipoTreasury.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun HipoTreasury.sendExternalHipoTreasuryParticipateInElection(
    self,
    queryId: uint64,
    roundSince: uint32,
): ExternalSendResult {
    val body = HipoTreasuryParticipateInElection { queryId, roundSince };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun HipoTreasury.sendExternalHipoTreasuryVsetChanged(
    self,
    queryId: uint64,
    roundSince: uint32,
): ExternalSendResult {
    val body = HipoTreasuryVsetChanged { queryId, roundSince };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun HipoTreasury.sendExternalHipoTreasuryFinishParticipation(
    self,
    queryId: uint64,
    roundSince: uint32,
): ExternalSendResult {
    val body = HipoTreasuryFinishParticipation { queryId, roundSince };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun HipoTreasury.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun HipoTreasury.getTreasuryState(self): HipoTreasuryStateReply {
    return net.runGetMethod(self.address, "get_treasury_state");
}

fun HipoTreasury.getParticipation(self, roundSince: int): HipoTreasuryParticipationReply {
    return net.runGetMethod(self.address, "get_participation", [roundSince]);
}

fun HipoTreasury.getCollectionAddress(self, roundSince: int): any_address {
    return net.runGetMethod(self.address, "get_collection_address", [roundSince]);
}

fun HipoTreasury.getBillAddress(self, roundSince: int, index: int): any_address {
    return net.runGetMethod(self.address, "get_bill_address", [roundSince, index]);
}

fun HipoTreasury.getLoanAddress(self, borrower: slice, roundSince: int): any_address {
    return net.runGetMethod(self.address, "get_loan_address", [borrower, roundSince]);
}

fun HipoTreasury.getTreasuryFees(self, ownershipAssignedAmount: int): HipoTreasuryFeesReply {
    return net.runGetMethod(self.address, "get_treasury_fees", [ownershipAssignedAmount]);
}

fun HipoTreasury.getTimes(self): HipoTreasuryTimesReply {
    return net.runGetMethod(self.address, "get_times");
}

fun HipoTreasury.getMaxBurnableTokens(self): coins {
    return net.runGetMethod(self.address, "get_max_burnable_tokens");
}

fun HipoTreasury.getSurplus(self): int {
    return net.runGetMethod(self.address, "get_surplus");
}

fun HipoTreasury.getMaxPunishment(self, stake: int): coins {
    return net.runGetMethod(self.address, "get_max_punishment", [stake]);
}
