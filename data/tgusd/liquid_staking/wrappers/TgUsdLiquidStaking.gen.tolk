/// Auto-generated wrapper for contract 'TgUsdLiquidStaking'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tgusd_liquid_staking.types"

/// Storage `TgUsdLiquidStakingStorage` is defined in `../types/tgusd_liquid_staking.types`
struct TgUsdLiquidStaking {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TgUsdLiquidStaking.fromStorage(
    storage: TgUsdLiquidStakingStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TgUsdLiquidStaking {
    val stateInit = ContractState {
        code: build("TgUsdLiquidStaking"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TgUsdLiquidStaking { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TgUsdLiquidStaking.fromAddress(address: address): TgUsdLiquidStaking {
    return TgUsdLiquidStaking { address };
}

/// Deploys the contract to the blockchain
fun TgUsdLiquidStaking.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdTransferNotification(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    jettonSender: address,
    forwardPayload: Cell<TgUsdTransferForwardPayload>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdTransferNotification { queryId, jettonAmount, jettonSender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdAllocateStaked(
    self,
    from: address,
    queryId: uint64,
    stakedAmount: coins,
    recipient: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdAllocateStaked { queryId, stakedAmount, recipient, customPayload },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdWithdrawInternal(
    self,
    from: address,
    queryId: uint64,
    unstaker: address,
    unstakedAmount: coins,
    recipient: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdWithdrawInternal { queryId, unstaker, unstakedAmount, recipient },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdExtraBurnInfo(
    self,
    from: address,
    queryId: uint64,
    unstaker: address,
    jettonAmount: coins,
    customPayload: Cell<TgUsdRequestUnstakeForwardPayload>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdExtraBurnInfo { queryId, unstaker, jettonAmount, customPayload },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdProvideCurrentQuote(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdProvideCurrentQuote { queryId, recipient, customPayload },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdResetGas(
    self,
    from: address,
    queryId: uint64,
    recipient: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdResetGas { queryId, recipient },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUpdateCooldownPeriod(
    self,
    from: address,
    queryId: uint64,
    cooldownPeriod: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUpdateCooldownPeriod { queryId, cooldownPeriod },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUpdateVestingPeriod(
    self,
    from: address,
    queryId: uint64,
    vestingPeriod: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUpdateVestingPeriod { queryId, vestingPeriod },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUpdateUnstakeAccountCode(
    self,
    from: address,
    queryId: uint64,
    unstakeAccountCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUpdateUnstakeAccountCode { queryId, unstakeAccountCode },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdForceWithdraw(
    self,
    from: address,
    queryId: uint64,
    unstakeAccount: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdForceWithdraw { queryId, unstakeAccount },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdHalt(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdHalt { queryId },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUnhalt(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUnhalt { queryId },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    nextAdmin: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdChangeAdmin { queryId, nextAdmin },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdClaimAdmin(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdClaimAdmin { queryId },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdCallToJettonMaster(
    self,
    from: address,
    queryId: uint64,
    to: address,
    tonAmount: coins,
    masterMessage: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdCallToJettonMaster { queryId, to, tonAmount, masterMessage },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdChangeJettonMasterAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdmin: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdChangeJettonMasterAdmin { queryId, newAdmin },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUpgradeContract(
    self,
    from: address,
    queryId: uint64,
    newData: cell?,
    newCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUpgradeContract { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUpgradeUnstakeAccount(
    self,
    from: address,
    queryId: uint64,
    unstakeAccount: address,
    newData: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUpgradeUnstakeAccount { queryId, unstakeAccount, newData },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdUpgradeJettonMaster(
    self,
    from: address,
    queryId: uint64,
    newData: cell,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdUpgradeJettonMaster { queryId, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdChangeJettonMasterContentRequest(
    self,
    from: address,
    queryId: uint64,
    newContent: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdChangeJettonMasterContentRequest { queryId, newContent },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdTopUp(
    self,
    from: address,
    queryId: uint64,
    stgUsdJettonMaster: address,
    stakedManager: address,
    vestingPeriod: uint32,
    cooldownPeriod: uint32,
    baseCode: cell,
    unstakeAccountCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdTopUp {
            queryId,
            stgUsdJettonMaster,
            stakedManager,
            vestingPeriod,
            cooldownPeriod,
            baseCode,
            unstakeAccountCode,
        },
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.sendTgUsdTakeWalletAddress(
    self,
    from: address,
    queryId: uint64,
    walletAddress: address?,
    ownerAddress: Cell<address>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TgUsdTakeWalletAddress { queryId, walletAddress, ownerAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TgUsdLiquidStaking.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TgUsdLiquidStaking.conversionRatio(self): TgUsdLiquidStakingConversionRatio {
    return net.runGetMethod(self.address, "conversionRatio");
}

fun TgUsdLiquidStaking.unstakeAccount(self, unstaker: any_address): any_address {
    return net.runGetMethod(self.address, "unstakeAccount", [unstaker]);
}
