/// Auto-generated wrapper for contract 'CoffeeVaultNative'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_vault_native.types"

/// Storage `CoffeeVaultNativeStorage` is defined in `../types/coffee_vault_native.types`
struct CoffeeVaultNative {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeVaultNative.fromStorage(
    storage: CoffeeVaultNativeStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeVaultNative {
    val stateInit = ContractState {
        code: build("CoffeeVaultNative"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeVaultNative { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeVaultNative.fromAddress(address: address): CoffeeVaultNative {
    return CoffeeVaultNative { address };
}

/// Deploys the contract to the blockchain
fun CoffeeVaultNative.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultNative.sendCoffeePayoutInternal(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    amount: coins,
    notificationData: Cell<CoffeeNotificationDataSingle>?,
    proof: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeePayoutInternal { queryId, recipient, amount, notificationData, proof },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultNative.sendCoffeeSwapNative(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    step: CoffeeSwapStepParams,
    params: CoffeeSwapParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeSwapNative {
            queryId,
            amount,
            step,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultNative.sendCoffeeDepositLiquidityNative(
    self,
    from: address,
    queryId: uint64,
    inputAmount: coins,
    params: CoffeeDepositLiquidityParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeDepositLiquidityNative { queryId, inputAmount, params },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultNative.sendCoffeeCreatePoolNative(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    params: CoffeePoolParams,
    creationParams: CoffeePoolCreationParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCreatePoolNative { queryId, amount, params, creationParams },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultNative.sendCoffeeUpdateContractInternal(
    self,
    from: address,
    queryId: uint64,
    excessesReceiver: address,
    update: CoffeeContractUpdate,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeUpdateContractInternal { queryId, excessesReceiver, update },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeVaultNative.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultNative.getAsset(self): slice {
    return net.runGetMethod(self.address, "get_asset");
}

fun CoffeeVaultNative.isActive(self): bool {
    return net.runGetMethod(self.address, "is_active");
}
