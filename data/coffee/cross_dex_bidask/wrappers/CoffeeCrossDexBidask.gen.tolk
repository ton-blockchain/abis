/// Auto-generated wrapper for contract 'CoffeeCrossDexBidask'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_cross_dex_bidask.types"

/// Storage `CoffeeCrossDexBidaskDeploymentStorage` is defined in `../types/coffee_cross_dex_bidask.types`
struct CoffeeCrossDexBidask {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeCrossDexBidask.fromStorage(
    storage: CoffeeCrossDexBidaskDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeCrossDexBidask {
    val stateInit = ContractState {
        code: build("CoffeeCrossDexBidask"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeCrossDexBidask { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeCrossDexBidask.fromAddress(address: address): CoffeeCrossDexBidask {
    return CoffeeCrossDexBidask { address };
}

/// Deploys the contract to the blockchain
fun CoffeeCrossDexBidask.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address?,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskJettonTransferNotification {
            queryId,
            amount,
            sender,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskDedustPayout(
    self,
    from: address,
    queryId: uint64,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskDedustPayout { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskPtonTonTransfer(
    self,
    from: address,
    queryId: uint64,
    tonAmount: coins,
    refundAddress: address?,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskPtonTonTransfer {
            queryId,
            tonAmount,
            refundAddress,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskCoffeeNotification(
    self,
    from: address,
    queryId: uint64,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskCoffeeNotification { queryId, body },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskBidaskNativeTransferNotification(
    self,
    from: address,
    queryId: uint64,
    nativeAmount: coins,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskBidaskNativeTransferNotification {
            queryId,
            nativeAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskResend(
    self,
    from: address,
    queryId: uint64,
    nextRecipient: address,
    next: cell,
    totalGas: coins,
    nextGas: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskResend { queryId, nextRecipient, next, totalGas, nextGas },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskSetAdmin(
    self,
    from: address,
    queryId: uint64,
    adminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskSetAdmin { queryId, adminAddress },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskUpdateContract(
    self,
    from: address,
    queryId: uint64,
    code: cell?,
    data: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskUpdateContract { queryId, code, data },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskWithdrawTon(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskWithdrawTon { queryId, recipient, amount },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskWithdrawJettons(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    jettonWallet: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskWithdrawJettons { queryId, recipient, jettonWallet, amount },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.sendCoffeeCrossDexBidaskExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeCrossDexBidask.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDexBidask.getAdmin(self): address {
    return net.runGetMethod(self.address, "get_admin");
}
