/// Auto-generated wrapper for contract 'CoffeeVaultExtra'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_vault_extra.types"

/// Storage `CoffeeVaultExtraStorage` is defined in `../types/coffee_vault_extra.types`
struct CoffeeVaultExtra {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeVaultExtra.fromStorage(
    storage: CoffeeVaultExtraStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeVaultExtra {
    val stateInit = ContractState {
        code: build("CoffeeVaultExtra"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeVaultExtra { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeVaultExtra.fromAddress(address: address): CoffeeVaultExtra {
    return CoffeeVaultExtra { address };
}

/// Deploys the contract to the blockchain
fun CoffeeVaultExtra.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultExtra.sendCoffeePayoutInternal(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    amount: coins,
    notificationData: Cell<CoffeeNotificationDataSingle>?,
    proof: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeePayoutInternal { queryId, recipient, amount, notificationData, proof },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultExtra.sendCoffeeSwapExtra(
    self,
    from: address,
    queryId: uint64,
    step: CoffeeSwapStepParams,
    params: CoffeeSwapParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeSwapExtra {
            queryId,
            step,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultExtra.sendCoffeeDepositLiquidityExtra(
    self,
    from: address,
    queryId: uint64,
    params: CoffeeDepositLiquidityParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeDepositLiquidityExtra { queryId, params },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultExtra.sendCoffeeCreatePoolExtra(
    self,
    from: address,
    queryId: uint64,
    params: CoffeePoolParams,
    creationParams: CoffeePoolCreationParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCreatePoolExtra { queryId, params, creationParams },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultExtra.sendCoffeeUpdateContractInternal(
    self,
    from: address,
    queryId: uint64,
    excessesReceiver: address,
    update: CoffeeContractUpdate,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeUpdateContractInternal { queryId, excessesReceiver, update },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeVaultExtra.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultExtra.getAsset(self): slice {
    return net.runGetMethod(self.address, "get_asset");
}

fun CoffeeVaultExtra.isActive(self): bool {
    return net.runGetMethod(self.address, "is_active");
}
