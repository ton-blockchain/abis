/// Auto-generated wrapper for contract 'JettonWalletCoffeeLp'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/jetton_wallet_coffee_lp.types"

/// Storage `JettonWalletCoffeeLpStorage` is defined in `../types/jetton_wallet_coffee_lp.types`
struct JettonWalletCoffeeLp {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun JettonWalletCoffeeLp.fromStorage(
    storage: JettonWalletCoffeeLpStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): JettonWalletCoffeeLp {
    val stateInit = ContractState {
        code: build("JettonWalletCoffeeLp"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return JettonWalletCoffeeLp { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun JettonWalletCoffeeLp.fromAddress(address: address): JettonWalletCoffeeLp {
    return JettonWalletCoffeeLp { address };
}

/// Deploys the contract to the blockchain
fun JettonWalletCoffeeLp.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonWalletCoffeeLp.sendJettonWalletCoffeeLpTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseAddress: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonWalletCoffeeLpTransfer {
            queryId,
            amount,
            destination,
            responseAddress,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonWalletCoffeeLp.sendJettonWalletCoffeeLpInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    fromAddress: address,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonWalletCoffeeLpInternalTransfer {
            queryId,
            amount,
            fromAddress,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonWalletCoffeeLp.sendJettonWalletCoffeeLpBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseAddress: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonWalletCoffeeLpBurn { queryId, amount, responseAddress, customPayload },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun JettonWalletCoffeeLp.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun JettonWalletCoffeeLp.getWalletData(self): GetJettonWalletCoffeeLpDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}
