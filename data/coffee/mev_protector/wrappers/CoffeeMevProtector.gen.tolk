/// Auto-generated wrapper for contract 'CoffeeMevProtector'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_mev_protector.types"

/// Storage `CoffeeMevProtectorStorage` is defined in `../types/coffee_mev_protector.types`
struct CoffeeMevProtector {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeMevProtector.fromStorage(
    storage: CoffeeMevProtectorStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeMevProtector {
    val stateInit = ContractState {
        code: build("CoffeeMevProtector"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeMevProtector { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeMevProtector.fromAddress(address: address): CoffeeMevProtector {
    return CoffeeMevProtector { address };
}

/// Deploys the contract to the blockchain
fun CoffeeMevProtector.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorJettonTransferNotification {
            queryId,
            amount,
            sender,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectHoldFunds(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectHoldFunds { queryId },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorDedustPayout(
    self,
    from: address,
    queryId: uint64,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorDedustPayout { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorCoffeeNotification(
    self,
    from: address,
    queryId: uint64,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorCoffeeNotification { queryId, body },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorSetAdmin(
    self,
    from: address,
    queryId: uint64,
    adminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorSetAdmin { queryId, adminAddress },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorSetPublicKey(
    self,
    from: address,
    queryId: uint64,
    publicKey: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorSetPublicKey { queryId, publicKey },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorUpdateContract(
    self,
    from: address,
    queryId: uint64,
    code: cell?,
    data: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorUpdateContract { queryId, code, data },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorAdminRaw2bf71be6(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorAdminRaw2bf71be6 { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorAdminRaw7b4b82fe(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorAdminRaw7b4b82fe { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.sendCoffeeMevProtectorExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeMevProtectorExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeMevProtector.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeMevProtector.getPublicKey(self): uint256 {
    return net.runGetMethod(self.address, "get_public_key");
}

fun CoffeeMevProtector.processed(self, queryId: uint64): bool {
    return net.runGetMethod(self.address, "processed?", [queryId]);
}
