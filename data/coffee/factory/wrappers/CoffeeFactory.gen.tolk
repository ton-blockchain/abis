/// Auto-generated wrapper for contract 'CoffeeFactory'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_factory.types"

/// Storage `CoffeeFactoryStorage` is defined in `../types/coffee_factory.types`
struct CoffeeFactory {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeFactory.fromStorage(
    storage: CoffeeFactoryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeFactory {
    val stateInit = ContractState {
        code: build("CoffeeFactory"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeFactory { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeFactory.fromAddress(address: address): CoffeeFactory {
    return CoffeeFactory { address };
}

/// Deploys the contract to the blockchain
fun CoffeeFactory.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryCreatePoolCreatorRequest(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    params: CoffeeFactoryPoolParams,
    creationParams: CoffeeFactoryPoolCreationParams,
    sender: address,
    proof: CoffeeFactoryVaultProof,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryCreatePoolCreatorRequest {
            queryId,
            amount,
            params,
            creationParams: creationParams.toCell(),
            sender,
            proof: proof.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryCreateLiquidityDepositoryRequest(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    params: CoffeeFactoryDepositLiquidityParams,
    sender: address,
    proof: CoffeeFactoryVaultProof,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryCreateLiquidityDepositoryRequest {
            queryId,
            amount,
            params,
            sender,
            proof: proof.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryCreatePoolRequest(
    self,
    from: address,
    queryId: uint64,
    amount1: coins,
    amount2: coins,
    txInitiator: address,
    creationParams: CoffeeFactoryPoolCreationParams,
    proof: CoffeeFactoryPoolCreatorProof,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryCreatePoolRequest {
            queryId,
            amount1,
            amount2,
            txInitiator,
            creationParams: creationParams.toCell(),
            proof: proof.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryCreateVault(
    self,
    from: address,
    queryId: uint64,
    asset: CoffeeFactoryAsset,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryCreateVault { queryId, asset },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryUpdateAdmin(
    self,
    from: address,
    queryId: uint64,
    address: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryUpdateAdmin { queryId, address },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryUpdateWithdrawer(
    self,
    from: address,
    queryId: uint64,
    address: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryUpdateWithdrawer { queryId, address },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryUpdateCodeCells(
    self,
    from: address,
    queryId: uint64,
    first: CoffeeFactoryCodeCells,
    second: CoffeeFactoryCodeDictionaries,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryUpdateCodeCells {
            queryId,
            first: first.toCell(),
            second: second.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryUpdateContract(
    self,
    from: address,
    queryId: uint64,
    contractAddress: address?,
    update: CoffeeFactoryContractUpdate,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryUpdateContract { queryId, contractAddress, update },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryUpdatePool(
    self,
    from: address,
    queryId: uint64,
    poolParams: CoffeeFactoryPoolParams,
    params: CoffeeFactoryPoolUpdateParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryUpdatePool {
            queryId,
            poolParams,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryActivateVault(
    self,
    from: address,
    queryId: uint64,
    asset: CoffeeFactoryAsset,
    wallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryActivateVault { queryId, asset, wallet },
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.sendCoffeeFactoryWithdraw(
    self,
    from: address,
    queryId: uint64,
    poolAddress: address,
    asset: CoffeeFactoryAsset,
    amount: coins,
    receiver: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeFactoryWithdraw { queryId, poolAddress, asset, amount, receiver },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeFactory.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeFactory.getVaultAddress(self, asset: slice): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(self.address, "get_vault_address", [asset]);
}

fun CoffeeFactory.getPoolAddress(
    self,
    asset1: slice,
    asset2: slice,
    amm: int,
    ammSettings: cell,
): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(self.address, "get_pool_address", [asset1, asset2, amm, ammSettings]);
}

fun CoffeeFactory.getPoolAddressNoSettings(
    self,
    asset1: slice,
    asset2: slice,
    amm: int,
): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(self.address, "get_pool_address_no_settings", [asset1, asset2, amm]);
}

fun CoffeeFactory.getPoolCreatorAddress(
    self,
    owner: address,
    asset1: slice,
    asset2: slice,
    amm: int,
    ammSettings: cell,
): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(
        self.address,
        "get_pool_creator_address",
        [owner, asset1, asset2, amm, ammSettings],
    );
}

fun CoffeeFactory.getPoolCreatorAddressNoSettings(
    self,
    owner: address,
    asset1: slice,
    asset2: slice,
    amm: int,
): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(
        self.address,
        "get_pool_creator_address_no_settings",
        [owner, asset1, asset2, amm],
    );
}

fun CoffeeFactory.getLiquidityDepositoryAddress(
    self,
    owner: address,
    asset1: slice,
    asset2: slice,
    amm: int,
    ammSettings: cell,
): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(
        self.address,
        "get_liquidity_depository_address",
        [owner, asset1, asset2, amm, ammSettings],
    );
}

fun CoffeeFactory.getLiquidityDepositoryAddressNoSettings(
    self,
    owner: address,
    asset1: slice,
    asset2: slice,
    amm: int,
): CoffeeFactoryAddressWithHash {
    return net.runGetMethod(
        self.address,
        "get_liquidity_depository_address_no_settings",
        [owner, asset1, asset2, amm],
    );
}

fun CoffeeFactory.getAdminAddress(self): address {
    return net.runGetMethod(self.address, "get_admin_address");
}

fun CoffeeFactory.getCode(self): CoffeeFactoryCodeRefs {
    return net.runGetMethod(self.address, "get_code");
}
