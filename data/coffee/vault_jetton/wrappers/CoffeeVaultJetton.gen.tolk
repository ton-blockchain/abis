/// Auto-generated wrapper for contract 'CoffeeVaultJetton'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_vault_jetton.types"

/// Storage `CoffeeVaultJettonInactiveStorage` is defined in `../types/coffee_vault_jetton.types`
struct CoffeeVaultJetton {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeVaultJetton.fromStorage(
    storage: CoffeeVaultJettonInactiveStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeVaultJetton {
    val stateInit = ContractState {
        code: build("CoffeeVaultJetton"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeVaultJetton { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeVaultJetton.fromAddress(address: address): CoffeeVaultJetton {
    return CoffeeVaultJetton { address };
}

/// Deploys the contract to the blockchain
fun CoffeeVaultJetton.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.sendCoffeeTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: CoffeeVaultJettonForwardPayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeTransferNotification {
            queryId,
            amount,
            sender,
            forwardPayload: forwardPayload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.sendCoffeePayoutInternal(
    self,
    from: address,
    queryId: uint64,
    recipient: address,
    amount: coins,
    notificationData: Cell<CoffeeNotificationDataSingle>?,
    proof: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeePayoutInternal { queryId, recipient, amount, notificationData, proof },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.sendCoffeeCreateVaultInternal(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCreateVaultInternal { queryId },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.sendCoffeeTakeWalletAddress(
    self,
    from: address,
    queryId: uint64,
    walletAddress: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeTakeWalletAddress { queryId, walletAddress, tail },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.sendCoffeeActivateVaultInternal(
    self,
    from: address,
    queryId: uint64,
    wallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeActivateVaultInternal { queryId, wallet },
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.sendCoffeeUpdateContractInternal(
    self,
    from: address,
    queryId: uint64,
    excessesReceiver: address,
    update: CoffeeContractUpdate,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeUpdateContractInternal { queryId, excessesReceiver, update },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeVaultJetton.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeVaultJetton.getAsset(self): slice {
    return net.runGetMethod(self.address, "get_asset");
}

fun CoffeeVaultJetton.isActive(self): bool {
    return net.runGetMethod(self.address, "is_active");
}
