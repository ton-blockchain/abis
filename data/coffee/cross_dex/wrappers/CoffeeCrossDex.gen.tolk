/// Auto-generated wrapper for contract 'CoffeeCrossDex'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_cross_dex.types"

/// Storage `CoffeeCrossDexStorage` is defined in `../types/coffee_cross_dex.types`
struct CoffeeCrossDex {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeCrossDex.fromStorage(
    storage: CoffeeCrossDexStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeCrossDex {
    val stateInit = ContractState {
        code: build("CoffeeCrossDex"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeCrossDex { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeCrossDex.fromAddress(address: address): CoffeeCrossDex {
    return CoffeeCrossDex { address };
}

/// Deploys the contract to the blockchain
fun CoffeeCrossDex.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexJettonTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexJettonTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexDedustPayout(
    self,
    from: address,
    queryId: uint64,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexDedustPayout { queryId, payload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexPtonTonTransfer(
    self,
    from: address,
    queryId: uint64,
    tonAmount: coins,
    refundAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexPtonTonTransfer { queryId, tonAmount, refundAddress, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexCoffeeNotification(
    self,
    from: address,
    queryId: uint64,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexCoffeeNotification { queryId, body },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexBidaskNativeTransferNotification(
    self,
    from: address,
    queryId: uint64,
    nativeAmount: coins,
    forwardPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexBidaskNativeTransferNotification {
            queryId,
            nativeAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexWrappedPayload(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    payload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexWrappedPayload { queryId, amount, payload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexResend(
    self,
    from: address,
    queryId: uint64,
    nextRecipient: address,
    next: cell,
    totalGas: coins,
    nextGas: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexResend { queryId, nextRecipient, next, totalGas, nextGas },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexSetAdmin(
    self,
    from: address,
    queryId: uint64,
    adminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexSetAdmin { queryId, adminAddress },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexUpdateContract(
    self,
    from: address,
    queryId: uint64,
    code: cell?,
    data: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexUpdateContract { queryId, code, data },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexAdminRaw2bf71be6(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexAdminRaw2bf71be6 { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexAdminRaw7b4b82fe(
    self,
    from: address,
    queryId: uint64,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexAdminRaw7b4b82fe { queryId, tail },
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.sendCoffeeCrossDexExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeCrossDexExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeCrossDex.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeCrossDex.getAdmin(self): address {
    return net.runGetMethod(self.address, "get_admin");
}
