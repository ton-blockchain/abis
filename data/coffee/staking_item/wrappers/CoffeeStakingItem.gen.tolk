/// Auto-generated wrapper for contract 'CoffeeStakingItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_staking_item.types"

/// Storage `CoffeeStakingItemStorage` is defined in `../types/coffee_staking_item.types`
struct CoffeeStakingItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeStakingItem.fromStorage(
    storage: CoffeeStakingItemStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeStakingItem {
    val stateInit = ContractState {
        code: build("CoffeeStakingItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeStakingItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeStakingItem.fromAddress(address: address): CoffeeStakingItem {
    return CoffeeStakingItem { address };
}

/// Deploys the contract to the blockchain
fun CoffeeStakingItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingItem.sendCoffeeStakingItemInit(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    jettonData: CoffeeStakingAssetData,
    positionData: CoffeeStakingPositionData,
    periods: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingItemInit {
            queryId,
            owner,
            jettonData: jettonData.toCell(),
            positionData: positionData.toCell(),
            periods,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingItem.sendCoffeeStakingItemUnlock(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingItemUnlock { queryId },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingItem.sendCoffeeStakingItemExtend(
    self,
    from: address,
    queryId: uint64,
    periodId: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingItemExtend { queryId, periodId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeStakingItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingItem.getStoredData(self): CoffeeStakingItemStoredDataReply {
    return net.runGetMethod(self.address, "get_stored_data");
}

fun CoffeeStakingItem.getNftData(self): CoffeeStakingItemNftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}
