/// Auto-generated wrapper for contract 'CoffeeStakingVault'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/coffee_staking_vault.types"

/// Storage `CoffeeStakingVaultStorage` is defined in `../types/coffee_staking_vault.types`
struct CoffeeStakingVault {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CoffeeStakingVault.fromStorage(
    storage: CoffeeStakingVaultStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CoffeeStakingVault {
    val stateInit = ContractState {
        code: build("CoffeeStakingVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CoffeeStakingVault { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CoffeeStakingVault.fromAddress(address: address): CoffeeStakingVault {
    return CoffeeStakingVault { address };
}

/// Deploys the contract to the blockchain
fun CoffeeStakingVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.sendCoffeeStakingVaultInit(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    jettons: cell?,
    periods: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingVaultInit { queryId, owner, jettons, periods },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.sendCoffeeStakingVaultTransferNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingVaultTransferNotification { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.sendCoffeeStakingVaultSetOwner(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingVaultSetOwner { queryId, owner },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.sendCoffeeStakingVaultSetJettons(
    self,
    from: address,
    queryId: uint64,
    jettons: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingVaultSetJettons { queryId, jettons },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.sendCoffeeStakingVaultSetPeriods(
    self,
    from: address,
    queryId: uint64,
    periods: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingVaultSetPeriods { queryId, periods },
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.sendCoffeeStakingVaultUpdateContract(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    newData: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CoffeeStakingVaultUpdateContract { queryId, newCode, newData },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CoffeeStakingVault.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CoffeeStakingVault.getStoredData(self): CoffeeStakingVaultStoredDataReply {
    return net.runGetMethod(self.address, "get_stored_data");
}

fun CoffeeStakingVault.getMasterAddress(self): address {
    return net.runGetMethod(self.address, "get_master_address");
}
