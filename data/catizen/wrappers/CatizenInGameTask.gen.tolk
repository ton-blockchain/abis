/// Auto-generated wrapper for contract 'CatizenInGameTask'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/catizen_in_game_task.types"

/// Storage `CatizenInGameTaskDeploymentStorage` is defined in `../types/catizen_in_game_task.types`
struct CatizenInGameTask {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CatizenInGameTask.fromStorage(
    storage: CatizenInGameTaskDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CatizenInGameTask {
    val stateInit = ContractState {
        code: build("CatizenInGameTask"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CatizenInGameTask { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CatizenInGameTask.fromAddress(address: address): CatizenInGameTask {
    return CatizenInGameTask { address };
}

/// Deploys the contract to the blockchain
fun CatizenInGameTask.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenInGameTaskRecord(
    self,
    from: address,
    taskData: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenInGameTaskRecord { taskData },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenInGameTaskRecordWithValue(
    self,
    from: address,
    applicationValue: int257,
    taskData: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenInGameTaskRecordWithValue { applicationValue, taskData },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenOwnerGramWithdrawal(
    self,
    from: address,
    amount: int257,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenOwnerGramWithdrawal { amount },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenOwnerJettonWithdrawal(
    self,
    from: address,
    jettonAmount: coins,
    unusedAmount: coins,
    jettonWallet: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenOwnerJettonWithdrawal { jettonAmount, unusedAmount, jettonWallet },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenDeploy(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenDeploy { queryId },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenChangeOwner(
    self,
    from: address,
    queryId: uint64,
    newOwner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenChangeOwner { queryId, newOwner },
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.sendCatizenTextComment(
    self,
    from: address,
    text: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CatizenTextComment { text },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CatizenInGameTask.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CatizenInGameTask.owner(self): any_address {
    return net.runGetMethod(self.address, "owner");
}

fun CatizenInGameTask.balance(self): int {
    return net.runGetMethod(self.address, "balance");
}

fun CatizenInGameTask.getMethod107884Blzd(self): int {
    return net.runGetMethod(self.address, "get_method_107884_blzd");
}

fun CatizenInGameTask.supportedInterfaces(self): CatizenSupportedInterfaces {
    return net.runGetMethod(self.address, "supported_interfaces");
}

fun CatizenInGameTask.lazyDeploymentCompleted(self): CatizenLazyDeploymentStatus {
    return net.runGetMethod(self.address, "lazy_deployment_completed");
}

fun CatizenInGameTask.getAbiIpfs(self): slice {
    return net.runGetMethod(self.address, "get_abi_ipfs");
}
