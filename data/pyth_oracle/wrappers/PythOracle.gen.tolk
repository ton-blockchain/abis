/// Auto-generated wrapper for contract 'PythOracle'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/pyth_oracle.types"

/// Storage `PythOracleStorage` is defined in `../types/pyth_oracle.types`
struct PythOracle {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun PythOracle.fromStorage(
    storage: PythOracleStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): PythOracle {
    val stateInit = ContractState {
        code: build("PythOracle"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return PythOracle { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun PythOracle.fromAddress(address: address): PythOracle {
    return PythOracle { address };
}

/// Deploys the contract to the blockchain
fun PythOracle.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun PythOracle.sendPythUpdateGuardianSet(
    self,
    from: address,
    vm: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PythUpdateGuardianSet { vm },
    });
    return net.send(from, genericMsg);
}

fun PythOracle.sendPythUpdatePriceFeeds(
    self,
    from: address,
    updateData: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PythUpdatePriceFeeds { updateData },
    });
    return net.send(from, genericMsg);
}

fun PythOracle.sendPythExecuteGovernanceAction(
    self,
    from: address,
    governanceAction: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PythExecuteGovernanceAction { governanceAction },
    });
    return net.send(from, genericMsg);
}

fun PythOracle.sendPythUpgradeContract(
    self,
    from: address,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PythUpgradeContract { newCode },
    });
    return net.send(from, genericMsg);
}

fun PythOracle.sendPythParsePriceFeedUpdates(
    self,
    from: address,
    updateData: cell,
    priceIds: cell,
    minPublishTime: uint64,
    maxPublishTime: uint64,
    targetAddress: address?,
    customPayload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PythParsePriceFeedUpdates {
            updateData,
            priceIds,
            minPublishTime,
            maxPublishTime,
            targetAddress,
            customPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun PythOracle.sendPythParseUniquePriceFeedUpdates(
    self,
    from: address,
    updateData: cell,
    priceIds: cell,
    publishTime: uint64,
    maxStaleness: uint64,
    targetAddress: address?,
    customPayload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: PythParseUniquePriceFeedUpdates {
            updateData,
            priceIds,
            publishTime,
            maxStaleness,
            targetAddress,
            customPayload,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PythOracle.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PythOracle.getUpdateFee(self, data: slice): int {
    return net.runGetMethod(self.address, "get_update_fee", [data]);
}

fun PythOracle.getSingleUpdateFee(self): int {
    return net.runGetMethod(self.address, "get_single_update_fee");
}

fun PythOracle.getGovernanceDataSourceIndex(self): int {
    return net.runGetMethod(self.address, "get_governance_data_source_index");
}

fun PythOracle.getGovernanceDataSource(self): cell {
    return net.runGetMethod(self.address, "get_governance_data_source");
}

fun PythOracle.getLastExecutedGovernanceSequence(self): int {
    return net.runGetMethod(self.address, "get_last_executed_governance_sequence");
}

fun PythOracle.getIsValidDataSource(self, dataSource: cell): int {
    return net.runGetMethod(self.address, "get_is_valid_data_source", [dataSource]);
}

fun PythOracle.getPriceUnsafe(self, priceFeedId: int): PythPrice {
    return net.runGetMethod(self.address, "get_price_unsafe", [priceFeedId]);
}

fun PythOracle.getPriceNoOlderThan(self, timePeriod: int, priceFeedId: int): PythPrice {
    return net.runGetMethod(self.address, "get_price_no_older_than", [timePeriod, priceFeedId]);
}

fun PythOracle.getEmaPriceUnsafe(self, priceFeedId: int): PythPrice {
    return net.runGetMethod(self.address, "get_ema_price_unsafe", [priceFeedId]);
}

fun PythOracle.getEmaPriceNoOlderThan(self, timePeriod: int, priceFeedId: int): PythPrice {
    return net.runGetMethod(self.address, "get_ema_price_no_older_than", [timePeriod, priceFeedId]);
}

fun PythOracle.getChainId(self): int {
    return net.runGetMethod(self.address, "get_chain_id");
}

fun PythOracle.getCurrentGuardianSetIndex(self): int {
    return net.runGetMethod(self.address, "get_current_guardian_set_index");
}

fun PythOracle.getGuardianSet(self, index: int): GetPythGuardianSetReply {
    return net.runGetMethod(self.address, "get_guardian_set", [index]);
}

fun PythOracle.getGovernanceChainId(self): int {
    return net.runGetMethod(self.address, "get_governance_chain_id");
}

fun PythOracle.getGovernanceContract(self): uint256 {
    return net.runGetMethod(self.address, "get_governance_contract");
}

fun PythOracle.governanceActionIsConsumed(self, hash: uint256): int {
    return net.runGetMethod(self.address, "governance_action_is_consumed", [hash]);
}
