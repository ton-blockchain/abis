/// Auto-generated wrapper for contract 'TonpayInvoiceV9'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tonpay_invoice_v9.types"

/// Storage `TonpayInvoiceV9Storage` is defined in `../types/tonpay_invoice_v9.types`
struct TonpayInvoiceV9 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TonpayInvoiceV9.fromStorage(
    storage: TonpayInvoiceV9Storage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TonpayInvoiceV9 {
    val stateInit = ContractState {
        code: build("TonpayInvoiceV9"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TonpayInvoiceV9 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TonpayInvoiceV9.fromAddress(address: address): TonpayInvoiceV9 {
    return TonpayInvoiceV9 { address };
}

/// Deploys the contract to the blockchain
fun TonpayInvoiceV9.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TonpayInvoiceV9.sendTonpayInvoiceV9Pay(
    self,
    from: address,
    queryId: uint64,
    payerTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonpayInvoiceV9Pay { queryId, payerTail },
    });
    return net.send(from, genericMsg);
}

fun TonpayInvoiceV9.sendTonpayInvoiceV9Edit(
    self,
    from: address,
    queryId: uint64,
    hasCustomer: int2,
    customer: address,
    invoiceId: TonpayInvoiceV9Text,
    metadata: TonpayInvoiceV9Text,
    amount: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonpayInvoiceV9Edit {
            queryId,
            hasCustomer,
            customer: customer.toCell(),
            invoiceId: invoiceId.toCell(),
            metadata: metadata.toCell(),
            amount,
        },
    });
    return net.send(from, genericMsg);
}

fun TonpayInvoiceV9.sendTonpayInvoiceV9Deactivate(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonpayInvoiceV9Deactivate { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonpayInvoiceV9.sendTonpayInvoiceV9Activate(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonpayInvoiceV9Activate { queryId },
    });
    return net.send(from, genericMsg);
}

fun TonpayInvoiceV9.sendTonpayInvoiceV9Upgrade(
    self,
    from: address,
    queryId: uint64,
    code: cell,
    hasNewData: int2,
    newDataTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TonpayInvoiceV9Upgrade { queryId, code, hasNewData, newDataTail },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TonpayInvoiceV9.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TonpayInvoiceV9.getInvoiceStore(self): address {
    return net.runGetMethod(self.address, "get_invoice_store");
}

fun TonpayInvoiceV9.getInvoiceMerchant(self): address {
    return net.runGetMethod(self.address, "get_invoice_merchant");
}

fun TonpayInvoiceV9.getInvoiceBeneficiary(self): address {
    return net.runGetMethod(self.address, "get_invoice_beneficiary");
}

fun TonpayInvoiceV9.getInvoiceHasCustomer(self): int {
    return net.runGetMethod(self.address, "get_invoice_has_customer");
}

fun TonpayInvoiceV9.getInvoiceCustomer(self): cell {
    return net.runGetMethod(self.address, "get_invoice_customer");
}

fun TonpayInvoiceV9.getInvoiceId(self): cell {
    return net.runGetMethod(self.address, "get_invoice_id");
}

fun TonpayInvoiceV9.getInvoiceMetadata(self): cell {
    return net.runGetMethod(self.address, "get_invoice_metadata");
}

fun TonpayInvoiceV9.getInvoiceAmount(self): int {
    return net.runGetMethod(self.address, "get_invoice_amount");
}

fun TonpayInvoiceV9.getInvoicePaid(self): int {
    return net.runGetMethod(self.address, "get_invoice_paid");
}

fun TonpayInvoiceV9.getInvoiceActive(self): int {
    return net.runGetMethod(self.address, "get_invoice_active");
}

fun TonpayInvoiceV9.getInvoiceVersion(self): int {
    return net.runGetMethod(self.address, "get_invoice_version");
}

fun TonpayInvoiceV9.getInvoiceData(self): TonpayInvoiceV9DataReply {
    return net.runGetMethod(self.address, "get_invoice_data");
}
