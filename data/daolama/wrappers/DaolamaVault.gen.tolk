/// Auto-generated wrapper for contract 'DaolamaVault'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/daolama_vault.types"

/// Storage `DaolamaVaultStorage` is defined in `../types/daolama_vault.types`
struct DaolamaVault {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DaolamaVault.fromStorage(
    storage: DaolamaVaultStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DaolamaVault {
    val stateInit = ContractState {
        code: build("DaolamaVault"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DaolamaVault { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DaolamaVault.fromAddress(address: address): DaolamaVault {
    return DaolamaVault { address };
}

/// Deploys the contract to the blockchain
fun DaolamaVault.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaSupply(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    proof: DaolamaSupplyAuthorizationProof,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaSupply {
            queryId,
            amount,
            proof: proof.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaLpBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderAddress: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaLpBurnNotification { queryId, amount, senderAddress, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaNftOwnershipAssigned(
    self,
    from: address,
    queryId: uint64,
    previousOwnerAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaNftOwnershipAssigned { queryId, previousOwnerAddress, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaBorrow(
    self,
    from: address,
    queryId: uint64,
    proof: DaolamaLoanAuthorizationProof,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaBorrow {
            queryId,
            proof: proof.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaRepay(
    self,
    from: address,
    queryId: uint64,
    principal: coins,
    receivedAmount: coins,
    excessDestination: address?,
    proof: Cell<DaolamaLoanAuthorizationProof>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaRepay { queryId, principal, receivedAmount, excessDestination, proof },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaCollectFees(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaCollectFees { queryId },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaUpdateParams(
    self,
    from: address,
    queryId: uint64,
    params: DaolamaVaultParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaUpdateParams {
            queryId,
            params: params.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaUpdateConfig(
    self,
    from: address,
    queryId: uint64,
    config_: DaolamaVaultConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaUpdateConfig {
            queryId,
            config: config_.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaSetHalted(
    self,
    from: address,
    queryId: uint64,
    halted: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaSetHalted { queryId, halted },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaAdminSendMessage(
    self,
    from: address,
    queryId: uint64,
    message: cell,
    mode: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaAdminSendMessage { queryId, message, mode },
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.sendDaolamaUpgrade(
    self,
    from: address,
    queryId: uint64,
    newCode: cell?,
    newData: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: DaolamaUpgrade { queryId, newCode, newData },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DaolamaVault.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DaolamaVault.getPoolData(self): DaolamaPoolData {
    return net.runGetMethod(self.address, "get_pool_data");
}

fun DaolamaVault.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun DaolamaVault.getJettonData(self): DaolamaJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}
