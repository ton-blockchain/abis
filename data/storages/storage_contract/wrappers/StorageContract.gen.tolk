/// Auto-generated wrapper for contract 'StorageContract'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storage_contract.types"

/// Storage `StorageContractStorage` is defined in `../types/storage_contract.types`
struct StorageContract {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StorageContract.fromStorage(
    storage: StorageContractStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StorageContract {
    val stateInit = ContractState {
        code: build("StorageContract"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StorageContract { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StorageContract.fromAddress(address: address): StorageContract {
    return StorageContract { address };
}

/// Deploys the contract to the blockchain
fun StorageContract.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StorageContract.sendStorageContractTopUp(
    self,
    from: address,
    text: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageContractTopUp { text },
    });
    return net.send(from, genericMsg);
}

fun StorageContract.sendStorageDeployStorageContract(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageDeployStorageContract { queryId },
    });
    return net.send(from, genericMsg);
}

fun StorageContract.sendStorageAcceptStorageContract(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageAcceptStorageContract { queryId },
    });
    return net.send(from, genericMsg);
}

fun StorageContract.sendStorageCloseContract(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageCloseContract { queryId },
    });
    return net.send(from, genericMsg);
}

fun StorageContract.sendStorageWithdraw(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageWithdraw { queryId },
    });
    return net.send(from, genericMsg);
}

fun StorageContract.sendStorageProofStorage(
    self,
    from: address,
    queryId: uint64,
    fileDictProof: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageProofStorage { queryId, fileDictProof },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StorageContract.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StorageContract.getStorageContractData(self): StorageContractDataReply {
    return net.runGetMethod(self.address, "get_storage_contract_data");
}

fun StorageContract.getTorrentHash(self): uint256 {
    return net.runGetMethod(self.address, "get_torrent_hash");
}

fun StorageContract.isActive(self): bool {
    return net.runGetMethod(self.address, "is_active");
}

fun StorageContract.getNextProofInfo(self): StorageContractNextProofInfoReply {
    return net.runGetMethod(self.address, "get_next_proof_info");
}
