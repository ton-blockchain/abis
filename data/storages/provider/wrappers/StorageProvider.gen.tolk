/// Auto-generated wrapper for contract 'StorageProvider'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storage_provider.types"

/// Storage `StorageProviderStorage` is defined in `../types/storage_provider.types`
struct StorageProvider {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StorageProvider.fromStorage(
    storage: StorageProviderStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StorageProvider {
    val stateInit = ContractState {
        code: build("StorageProvider"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StorageProvider { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StorageProvider.fromAddress(address: address): StorageProvider {
    return StorageProvider { address };
}

/// Deploys the contract to the blockchain
fun StorageProvider.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StorageProvider.sendStorageProviderTextComment(
    self,
    from: address,
    text: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageProviderTextComment { text },
    });
    return net.send(from, genericMsg);
}

fun StorageProvider.sendStorageProviderOfferStorageContract(
    self,
    from: address,
    queryId: uint64,
    info: TorrentInfo,
    merkleHash: uint256,
    expectedRate: coins,
    expectedMaxSpan: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageProviderOfferStorageContract {
            queryId,
            info: info.toCell(),
            merkleHash,
            expectedRate,
            expectedMaxSpan,
        },
    });
    return net.send(from, genericMsg);
}

fun StorageProvider.sendStorageProviderStorageContractTerminated(
    self,
    from: address,
    curLt: uint64,
    torrentHash: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageProviderStorageContractTerminated { curLt, torrentHash },
    });
    return net.send(from, genericMsg);
}

fun StorageProvider.sendStorageProviderUpdatePubkey(
    self,
    from: address,
    queryId: uint64,
    newPubkey: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageProviderUpdatePubkey { queryId, newPubkey },
    });
    return net.send(from, genericMsg);
}

fun StorageProvider.sendStorageProviderUpdateStorageParams(
    self,
    from: address,
    queryId: uint64,
    acceptNewContracts: bool,
    ratePerMbDay: coins,
    maxSpan: uint32,
    minimalFileSize: uint64,
    maximalFileSize: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StorageProviderUpdateStorageParams {
            queryId,
            acceptNewContracts,
            ratePerMbDay,
            maxSpan,
            minimalFileSize,
            maximalFileSize,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StorageProvider.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StorageProvider.sendExternalStorageProviderSignedExternal(
    self,
    signature: bits512,
    subwallet: uint32,
    validUntil: uint32,
    msgSeqno: uint32,
    messages: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = StorageProviderSignedExternal {
        signature,
        subwallet,
        validUntil,
        msgSeqno,
        messages,
    };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun StorageProvider.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun StorageProvider.seqno(self): uint32 {
    return net.runGetMethod(self.address, "seqno");
}

fun StorageProvider.getPublicKey(self): uint256 {
    return net.runGetMethod(self.address, "get_public_key");
}

fun StorageProvider.getWalletParams(self): StorageProviderWalletParamsReply {
    return net.runGetMethod(self.address, "get_wallet_params");
}

fun StorageProvider.getStorageParams(self): StorageProviderStorageParamsReply {
    return net.runGetMethod(self.address, "get_storage_params");
}

fun StorageProvider.getStorageContractAddress(
    self,
    merkleHash: uint256,
    fileSize: uint64,
    client: address,
    torrentHash: uint256,
): address {
    return net.runGetMethod(
        self.address,
        "get_storage_contract_address",
        [merkleHash, fileSize, client, torrentHash],
    );
}
