/// Auto-generated wrapper for contract 'GramboJettonWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/grambo_jetton_wallet.types"

/// Storage `GramboJettonWalletStorage` is defined in `../types/grambo_jetton_wallet.types`
struct GramboJettonWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GramboJettonWallet.fromStorage(
    storage: GramboJettonWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GramboJettonWallet {
    val stateInit = ContractState {
        code: build("GramboJettonWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GramboJettonWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GramboJettonWallet.fromAddress(address: address): GramboJettonWallet {
    return GramboJettonWallet { address };
}

/// Deploys the contract to the blockchain
fun GramboJettonWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonWallet.sendGramboJettonWalletInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    from_: address?,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonWalletInternalTransfer {
            queryId,
            amount,
            from: from_,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonWallet.sendGramboJettonWalletTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseDestination: address?,
    customPayload: Cell<RemainingBitsAndRefs>?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonWalletTransfer {
            queryId,
            amount,
            destination,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonWallet.sendGramboJettonWalletBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseDestination: address?,
    customPayload: Cell<RemainingBitsAndRefs>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonWalletBurn { queryId, amount, responseDestination, customPayload },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonWallet.sendGramboJettonWalletActivate(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonWalletActivate { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GramboJettonWallet.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GramboJettonWallet.getWalletData(self): GramboJettonWalletData {
    return net.runGetMethod(self.address, "get_wallet_data");
}

fun GramboJettonWallet.getWalletStatus(self): bool {
    return net.runGetMethod(self.address, "get_wallet_status");
}
