/// Auto-generated wrapper for contract 'GramboJettonMaster'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/grambo_jetton_master.types"

/// Storage `GramboJettonMasterStorage` is defined in `../types/grambo_jetton_master.types`
struct GramboJettonMaster {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GramboJettonMaster.fromStorage(
    storage: GramboJettonMasterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GramboJettonMaster {
    val stateInit = ContractState {
        code: build("GramboJettonMaster"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GramboJettonMaster { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GramboJettonMaster.fromAddress(address: address): GramboJettonMaster {
    return GramboJettonMaster { address };
}

/// Deploys the contract to the blockchain
fun GramboJettonMaster.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.sendGramboJettonMasterLaunch(
    self,
    from: address,
    queryId: uint64,
    buyer: address,
    virtualTonReserve: coins,
    tonTarget: coins,
    prebuyAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonMasterLaunch {
            queryId,
            buyer,
            virtualTonReserve,
            tonTarget,
            prebuyAmount,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.sendGramboJettonMasterBuy(
    self,
    from: address,
    queryId: uint64,
    tonAmount: coins,
    recipient: address,
    minTokensOut: coins,
    referrer: address?,
    customPayload: Cell<RemainingBitsAndRefs>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonMasterBuy {
            queryId,
            tonAmount,
            recipient,
            minTokensOut,
            referrer,
            customPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.sendGramboJettonMasterSellNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    from_: address,
    responseDestination: address?,
    sellParams: Cell<GramboJettonMasterSellParams>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonMasterSellNotification {
            queryId,
            amount,
            from: from_,
            responseDestination,
            sellParams,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.sendGramboJettonMasterProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonMasterProvideWalletAddress { queryId, owner, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.sendGramboJettonMasterUnlockWallet(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonMasterUnlockWallet { queryId, owner },
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.sendGramboJettonMasterWithdraw(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboJettonMasterWithdraw { queryId, amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GramboJettonMaster.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GramboJettonMaster.getCurveState(self): GramboJettonMasterCurveStateResult {
    return net.runGetMethod(self.address, "get_curve_state");
}

fun GramboJettonMaster.getAmountOut(self, tonIn: coins): int {
    return net.runGetMethod(self.address, "get_amount_out", [tonIn]);
}

fun GramboJettonMaster.getTonOut(self, tokensIn: int): coins {
    return net.runGetMethod(self.address, "get_ton_out", [tokensIn]);
}

fun GramboJettonMaster.getJettonData(self): GramboJettonMasterJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun GramboJettonMaster.getWalletAddress(self, ownerAddress: any_address): any_address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}
