/// Auto-generated wrapper for contract 'GramboFactory'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/grambo_factory.types"

/// Storage `GramboFactoryStorage` is defined in `../types/grambo_factory.types`
struct GramboFactory {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GramboFactory.fromStorage(
    storage: GramboFactoryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GramboFactory {
    val stateInit = ContractState {
        code: build("GramboFactory"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GramboFactory { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GramboFactory.fromAddress(address: address): GramboFactory {
    return GramboFactory { address };
}

/// Deploys the contract to the blockchain
fun GramboFactory.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GramboFactory.sendGramboFactoryDeploy(
    self,
    from: address,
    queryId: uint64,
    metadata: cell,
    tonTarget: coins,
    prebuyAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboFactoryDeploy { queryId, metadata, tonTarget, prebuyAmount },
    });
    return net.send(from, genericMsg);
}

fun GramboFactory.sendGramboFactoryUpdateDeployConfig(
    self,
    from: address,
    queryId: uint64,
    deployConfig: GramboFactoryDeployConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboFactoryUpdateDeployConfig {
            queryId,
            deployConfig: deployConfig.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun GramboFactory.sendGramboFactoryUpgradeContract(
    self,
    from: address,
    queryId: uint64,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboFactoryUpgradeContract { queryId, code },
    });
    return net.send(from, genericMsg);
}

fun GramboFactory.sendGramboFactoryLegacyNoop(
    self,
    from: address,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramboFactoryLegacyNoop { tail },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GramboFactory.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GramboFactory.getDeployConfig(self): GramboFactoryDeployConfigResult {
    return net.runGetMethod(self.address, "get_deploy_config");
}
