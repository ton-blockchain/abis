/// Auto-generated wrapper for contract 'JettonVesting'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/jetton_vesting.types"

/// Storage `JettonVestingDeploymentStorage` is defined in `../types/jetton_vesting.types`
struct JettonVesting {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun JettonVesting.fromStorage(
    storage: JettonVestingDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): JettonVesting {
    val stateInit = ContractState {
        code: build("JettonVesting"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return JettonVesting { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun JettonVesting.fromAddress(address: address): JettonVesting {
    return JettonVesting { address };
}

/// Deploys the contract to the blockchain
fun JettonVesting.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingClaimToken(
    self,
    from: address,
    queryId: uint64,
    destination: address,
    amount: coins?,
    responseDestination: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingClaimToken {
            queryId,
            destination,
            amount,
            responseDestination,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingClaimGram(
    self,
    from: address,
    receiver: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingClaimGram { receiver },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingOpcode678ed83d(
    self,
    from: address,
    payload: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingOpcode678ed83d { payload },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingOpcodeB148d27f(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingOpcodeB148d27f { amount },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingOpcodeAb800f82(
    self,
    from: address,
    vestingStartTime: uint32?,
    vestingDurationYears: uint32?,
    remainingJettonAmount: coins?,
    totalJettonAmount: coins?,
    unknownFlag0: bool?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingOpcodeAb800f82 {
            vestingStartTime,
            vestingDurationYears,
            remainingJettonAmount,
            totalJettonAmount,
            unknownFlag0,
        },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingOpcode4665955b(
    self,
    from: address,
    unknownFlag1: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingOpcode4665955b { unknownFlag1 },
    });
    return net.send(from, genericMsg);
}

fun JettonVesting.sendJettonVestingOpcode0ddabdd2(
    self,
    from: address,
    destination: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: JettonVestingOpcode0ddabdd2 { destination, amount },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun JettonVesting.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
