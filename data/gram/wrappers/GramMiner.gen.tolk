/// Auto-generated wrapper for contract 'GramMiner'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/gram_miner.types"

/// Storage `GramMinerStorage` is defined in `../types/gram_miner.types`
struct GramMiner {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GramMiner.fromStorage(
    storage: GramMinerStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GramMiner {
    val stateInit = ContractState {
        code: build("GramMiner"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GramMiner { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GramMiner.fromAddress(address: address): GramMiner {
    return GramMiner { address };
}

/// Deploys the contract to the blockchain
fun GramMiner.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GramMiner.sendGramInitializeJettonWallet(
    self,
    from: address,
    jettonWalletAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramInitializeJettonWallet { jettonWalletAddress },
    });
    return net.send(from, genericMsg);
}

fun GramMiner.sendGramSubmitProofOfWork(
    self,
    from: address,
    flags: int8,
    expire: uint32,
    whom: bits256,
    rdata1: bits256,
    rseed: uint128,
    rdata2: bits256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GramSubmitProofOfWork { flags, expire, whom, rdata1, rseed, rdata2 },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GramMiner.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GramMiner.sendExternalGramMinerExternalBody(
    self,
    body: RemainingBitsAndRefs,
): ExternalSendResult {
    val body = GramMinerExternalBody { body };
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

/// Send an external-in message to the contract with a custom body cell
fun GramMiner.sendAnyExternal(self, body: cell): ExternalSendResult {
    val msg = net.createExternalMessage(self.address, body);
    return net.sendExternal(msg);
}

fun GramMiner.seqno(self): uint32 {
    return net.runGetMethod(self.address, "seqno");
}

fun GramMiner.getPublicKey(self): int {
    return net.runGetMethod(self.address, "get_public_key");
}

fun GramMiner.getPowParams(self): GramMinerPowParamsReply {
    return net.runGetMethod(self.address, "get_pow_params");
}

fun GramMiner.getContractData(self): GramMinerContractDataReply {
    return net.runGetMethod(self.address, "get_contract_data");
}
