/// Auto-generated wrapper for contract 'VerifierSourceItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/verifier_source_item.types"

/// Storage `VerifierSourceItemDeploymentStorage` is defined in `../types/verifier_source_item.types`
struct VerifierSourceItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun VerifierSourceItem.fromStorage(
    storage: VerifierSourceItemDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): VerifierSourceItem {
    val stateInit = ContractState {
        code: build("VerifierSourceItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return VerifierSourceItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun VerifierSourceItem.fromAddress(address: address): VerifierSourceItem {
    return VerifierSourceItem { address };
}

/// Deploys the contract to the blockchain
fun VerifierSourceItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun VerifierSourceItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun VerifierSourceItem.getSourceItemData(self): VerifierSourceItemData {
    return net.runGetMethod(self.address, "get_source_item_data");
}
