/// Auto-generated wrapper for contract 'VerifierSourcesRegistry'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/verifier_sources_registry.types"

/// Storage `VerifierSourcesRegistryStorage` is defined in `../types/verifier_sources_registry.types`
struct VerifierSourcesRegistry {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun VerifierSourcesRegistry.fromStorage(
    storage: VerifierSourcesRegistryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): VerifierSourcesRegistry {
    val stateInit = ContractState {
        code: build("VerifierSourcesRegistry"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return VerifierSourcesRegistry { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun VerifierSourcesRegistry.fromAddress(address: address): VerifierSourcesRegistry {
    return VerifierSourcesRegistry { address };
}

/// Deploys the contract to the blockchain
fun VerifierSourcesRegistry.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.sendVerifierSourcesRegistryDeploySourceItemPayload(
    self,
    from: address,
    queryId: uint64,
    verifierId: uint256,
    verifiedCodeCellHash: uint256,
    sourceContent: VerifierSourceContent,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierSourcesRegistryDeploySourceItemPayload {
            queryId,
            verifierId,
            verifiedCodeCellHash,
            sourceContent: sourceContent.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.sendVerifierSourcesRegistryChangeVerifierRegistry(
    self,
    from: address,
    queryId: uint64,
    newVerifierRegistry: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierSourcesRegistryChangeVerifierRegistry { queryId, newVerifierRegistry },
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.sendVerifierSourcesRegistryChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdmin: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierSourcesRegistryChangeAdmin { queryId, newAdmin },
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.sendVerifierSourcesRegistrySetSourceItemCode(
    self,
    from: address,
    queryId: uint64,
    newSourceItemCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierSourcesRegistrySetSourceItemCode { queryId, newSourceItemCode },
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.sendVerifierSourcesRegistrySetCode(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierSourcesRegistrySetCode { queryId, newCode },
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.sendVerifierSourcesRegistrySetDeploymentCosts(
    self,
    from: address,
    queryId: uint64,
    newMinGram: coins,
    newMaxGram: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierSourcesRegistrySetDeploymentCosts { queryId, newMinGram, newMaxGram },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun VerifierSourcesRegistry.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun VerifierSourcesRegistry.getSourceItemAddress(
    self,
    verifierId: int,
    verifiedCodeCellHash: int,
): address {
    return net.runGetMethod(
        self.address,
        "get_source_item_address",
        [verifierId, verifiedCodeCellHash],
    );
}

fun VerifierSourcesRegistry.getVerifierRegistryAddress(self): address {
    return net.runGetMethod(self.address, "get_verifier_registry_address");
}

fun VerifierSourcesRegistry.getAdminAddress(self): address {
    return net.runGetMethod(self.address, "get_admin_address");
}

fun VerifierSourcesRegistry.getDeploymentCosts(self): VerifierSourcesRegistryDeploymentCosts {
    return net.runGetMethod(self.address, "get_deployment_costs");
}
