/// Auto-generated wrapper for contract 'VerifierRegistry'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/verifier_registry.types"

/// Storage `VerifierRegistryStorage` is defined in `../types/verifier_registry.types`
struct VerifierRegistry {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun VerifierRegistry.fromStorage(
    storage: VerifierRegistryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): VerifierRegistry {
    val stateInit = ContractState {
        code: build("VerifierRegistry"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return VerifierRegistry { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun VerifierRegistry.fromAddress(address: address): VerifierRegistry {
    return VerifierRegistry { address };
}

/// Deploys the contract to the blockchain
fun VerifierRegistry.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun VerifierRegistry.sendVerifierRegistryForwardMessage(
    self,
    from: address,
    queryId: uint64,
    msg: VerifierRegistryMessageDescription,
    signatures: VerifierRegistrySignatureItem,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierRegistryForwardMessage {
            queryId,
            msg: msg.toCell(),
            signatures: signatures.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun VerifierRegistry.sendVerifierRegistryUpdateVerifier(
    self,
    from: address,
    queryId: uint64,
    verifierId: uint256,
    settings: VerifierRegistrySettings,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierRegistryUpdateVerifier { queryId, verifierId, settings },
    });
    return net.send(from, genericMsg);
}

fun VerifierRegistry.sendVerifierRegistryRemoveVerifier(
    self,
    from: address,
    queryId: uint64,
    id: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VerifierRegistryRemoveVerifier { queryId, id },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun VerifierRegistry.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun VerifierRegistry.getVerifier(self, id: int): VerifierRegistryInfo {
    return net.runGetMethod(self.address, "get_verifier", [id]);
}

fun VerifierRegistry.getVerifiersNum(self): int {
    return net.runGetMethod(self.address, "get_verifiers_num");
}

fun VerifierRegistry.getVerifiers(self): slice {
    return net.runGetMethod(self.address, "get_verifiers");
}
