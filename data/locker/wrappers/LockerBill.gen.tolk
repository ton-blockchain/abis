/// Auto-generated wrapper for contract 'LockerBill'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/locker_bill.types"

/// Storage `LockerBillStorage` is defined in `../types/locker_bill.types`
struct LockerBill {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun LockerBill.fromStorage(
    storage: LockerBillStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): LockerBill {
    val stateInit = ContractState {
        code: build("LockerBill"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return LockerBill { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun LockerBill.fromAddress(address: address): LockerBill {
    return LockerBill { address };
}

/// Deploys the contract to the blockchain
fun LockerBill.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun LockerBill.sendLockerBillDepositToBill(
    self,
    from: address,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: LockerBillDepositToBill { amount },
    });
    return net.send(from, genericMsg);
}

fun LockerBill.sendLockerBillTextCommand(
    self,
    from: address,
    action: uint8,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: LockerBillTextCommand { action },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun LockerBill.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun LockerBill.getLockerBillData(self): GetLockerBillDataReply {
    return net.runGetMethod(self.address, "get_locker_bill_data");
}
