/// Auto-generated wrapper for contract 'TacJetton'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/tac_jetton.types"

/// Storage `TacJettonStorage` is defined in `../types/tac_jetton.types`
struct TacJetton {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TacJetton.fromStorage(
    storage: TacJettonStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TacJetton {
    val stateInit = ContractState {
        code: build("TacJetton"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TacJetton { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TacJetton.fromAddress(address: address): TacJetton {
    return TacJetton { address };
}

/// Deploys the contract to the blockchain
fun TacJetton.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonErrorNotification(
    self,
    from: address,
    queryId: uint64,
    operation: uint32,
    ownerAddress: address,
    jettonAmount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonErrorNotification { queryId, operation, ownerAddress, jettonAmount },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonMint(
    self,
    from: address,
    queryId: uint64,
    recipientAddress: address,
    jettonAmount: coins,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: cell?,
    newJettonContent: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonMint {
            queryId,
            recipientAddress,
            jettonAmount,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
            newJettonContent,
        },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonBurnNotification(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    ownerAddress: address,
    responseAddress: address?,
    crosschainPayload: Cell<TacJettonBurnCrosschainPayload>?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonBurnNotification {
            queryId,
            jettonAmount,
            ownerAddress,
            responseAddress,
            crosschainPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonChangeAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonChangeAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonCancelAdminChange(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonCancelAdminChange { queryId },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonConfirmAdminChange(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonConfirmAdminChange { queryId },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonChangeContent(
    self,
    from: address,
    queryId: uint64,
    jettonContent: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonChangeContent { queryId, jettonContent },
    });
    return net.send(from, genericMsg);
}

fun TacJetton.sendTacJettonWithdrawExtraTon(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TacJettonWithdrawExtraTon { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TacJetton.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TacJetton.getJettonData(self): TacJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun TacJetton.getWalletAddress(self, ownerAddress: any_address): any_address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun TacJetton.getEvmTokenAddress(self): slice {
    return net.runGetMethod(self.address, "get_evm_token_address");
}

fun TacJetton.getFullData(self): TacJettonFullData {
    return net.runGetMethod(self.address, "get_full_data");
}
