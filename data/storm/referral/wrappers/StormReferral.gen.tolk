/// Auto-generated wrapper for contract 'StormReferral'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_referral.types"

/// Storage `StormReferralDeploymentStorage` is defined in `../types/storm_referral.types`
struct StormReferral {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormReferral.fromStorage(
    storage: StormReferralDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormReferral {
    val stateInit = ContractState {
        code: build("StormReferral"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormReferral { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormReferral.fromAddress(address: address): StormReferral {
    return StormReferral { address };
}

/// Deploys the contract to the blockchain
fun StormReferral.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormMintReferral(
    self,
    from: address,
    nftOwner: address,
    refType: uint2,
    redirectAddress: address,
    parameters: StormReferralParameters,
    balancesDict: map<uint256, coins>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormMintReferral {
            nftOwner,
            refType,
            redirectAddress,
            parameters: parameters.toCell(),
            balancesDict,
        },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormAddReferralAmount(
    self,
    from: address,
    referralAmount: coins,
    originAddr: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormAddReferralAmount { referralAmount, originAddr },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormReferralProvideFees(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormReferralProvideFees {},
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormReferralVaultsReply(
    self,
    from: address,
    vaultsWhitelist: map<uint256, coins>,
    requestTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormReferralVaultsReply { vaultsWhitelist, requestTail },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormReferralVaultsBootstrap(
    self,
    from: address,
    vaultsWhitelist: map<uint256, coins>,
    collectionAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormReferralVaultsBootstrap { vaultsWhitelist, collectionAddress },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormClaimReferralAmount(
    self,
    from: address,
    vaultAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormClaimReferralAmount { vaultAddress },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormReferralTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormReferralTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StormReferral.sendStormReferralGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormReferralGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormReferral.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormReferral.getNftData(self): StormReferralNftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun StormReferral.getReferralData(self): GetReferralDataReply {
    return net.runGetMethod(self.address, "get_referral_data");
}
