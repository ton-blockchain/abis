/// Auto-generated wrapper for contract 'StormLpWalletCode'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_lp_wallet.types"

/// Storage `StormLpWalletStorage` is defined in `../types/storm_lp_wallet.types`
struct StormLpWalletCode {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormLpWalletCode.fromStorage(
    storage: StormLpWalletStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormLpWalletCode {
    val stateInit = ContractState {
        code: build("StormLpWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormLpWalletCode { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormLpWalletCode.fromAddress(address: address): StormLpWalletCode {
    return StormLpWalletCode { address };
}

/// Deploys the contract to the blockchain
fun StormLpWalletCode.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormLpWalletCode.sendStormLpWalletTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    destination: address,
    responseAddress: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpWalletTransfer {
            queryId,
            amount,
            destination,
            responseAddress,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StormLpWalletCode.sendStormLpWalletInternalTransfer(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    fromAddress: address,
    responseAddress: address?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpWalletInternalTransfer {
            queryId,
            amount,
            fromAddress,
            responseAddress,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StormLpWalletCode.sendStormLpWalletBurn(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    responseAddress: address?,
    tail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpWalletBurn { queryId, amount, responseAddress, tail },
    });
    return net.send(from, genericMsg);
}

fun StormLpWalletCode.sendStormLpWalletRequestExcesses(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpWalletRequestExcesses { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormLpWalletCode.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormLpWalletCode.getWalletData(self): GetStormLpWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}
