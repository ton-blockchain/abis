/// Auto-generated wrapper for contract 'StormExecutor'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_executor.types"

/// Storage `StormExecutorDeploymentStorage` is defined in `../types/storm_executor.types`
struct StormExecutor {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormExecutor.fromStorage(
    storage: StormExecutorDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormExecutor {
    val stateInit = ContractState {
        code: build("StormExecutor"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormExecutor { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormExecutor.fromAddress(address: address): StormExecutor {
    return StormExecutor { address };
}

/// Deploys the contract to the blockchain
fun StormExecutor.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormMintExecutor(
    self,
    from: address,
    nftOwner: address,
    redirectAddress: address,
    balancesDict: map<uint256, coins>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormMintExecutor { nftOwner, redirectAddress, balancesDict },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormAddExecutorAmount(
    self,
    from: address,
    referralAmount: coins,
    originAddr: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormAddExecutorAmount { referralAmount, originAddr },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormExecutorVaultsReply(
    self,
    from: address,
    vaultsWhitelist: map<uint256, coins>,
    requestTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorVaultsReply { vaultsWhitelist, requestTail },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormExecutorVaultsBootstrap(
    self,
    from: address,
    vaultsWhitelist: map<uint256, coins>,
    collectionAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorVaultsBootstrap { vaultsWhitelist, collectionAddress },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormClaimExecutorAmount(
    self,
    from: address,
    vaultAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormClaimExecutorAmount { vaultAddress },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormExecutorTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.sendStormExecutorGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormExecutor.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormExecutor.getNftData(self): StormExecutorNftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun StormExecutor.getExecutorBalances(self): map<uint256, coins> {
    return net.runGetMethod(self.address, "get_executor_balances");
}
