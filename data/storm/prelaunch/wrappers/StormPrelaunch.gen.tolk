/// Auto-generated wrapper for contract 'StormPrelaunch'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_prelaunch.types"

/// Storage `StormPrelaunchStorage` is defined in `../types/storm_prelaunch.types`
struct StormPrelaunch {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormPrelaunch.fromStorage(
    storage: StormPrelaunchStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormPrelaunch {
    val stateInit = ContractState {
        code: build("StormPrelaunch"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormPrelaunch { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormPrelaunch.fromAddress(address: address): StormPrelaunch {
    return StormPrelaunch { address };
}

/// Deploys the contract to the blockchain
fun StormPrelaunch.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchTakeWalletAddress(
    self,
    from: address,
    queryId: uint64,
    walletAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchTakeWalletAddress { queryId, walletAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchJettonNotify(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderAddress: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchJettonNotify { queryId, amount, senderAddress, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchSetParticipants(
    self,
    from: address,
    queryId: uint64,
    participants: map<uint256, StormPrelaunchParticipant>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchSetParticipants { queryId, participants },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchSetRewardNotifier(
    self,
    from: address,
    queryId: uint64,
    rewardNotifierAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchSetRewardNotifier { queryId, rewardNotifierAddress },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchChangeCode(
    self,
    from: address,
    queryId: uint64,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchChangeCode { queryId, code },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchChangeData(
    self,
    from: address,
    queryId: uint64,
    data: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchChangeData { queryId, data },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchSetEndTime(
    self,
    from: address,
    queryId: uint64,
    endTime: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchSetEndTime { queryId, endTime },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchWithdrawJettons(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchWithdrawJettons { queryId, amount },
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.sendStormPrelaunchNotifyRewardsComplete(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPrelaunchNotifyRewardsComplete { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormPrelaunch.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormPrelaunch.getStorageData(self): StormPrelaunchStorageDataReply {
    return net.runGetMethod(self.address, "get_storage_data");
}

fun StormPrelaunch.getMethod104966Jsxb(
    self,
    userAddress: address,
): StormPrelaunchUserRewardCountsReply {
    return net.runGetMethod(self.address, "get_method_104966_jsxb", [userAddress]);
}
