/// Auto-generated wrapper for contract 'SmartAccountFactory'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/smart_account_factory.types"

/// Storage `SmartAccountFactoryStorage` is defined in `../types/smart_account_factory.types`
struct SmartAccountFactory {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun SmartAccountFactory.fromStorage(
    storage: SmartAccountFactoryStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): SmartAccountFactory {
    val stateInit = ContractState {
        code: build("SmartAccountFactory"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return SmartAccountFactory { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun SmartAccountFactory.fromAddress(address: address): SmartAccountFactory {
    return SmartAccountFactory { address };
}

/// Deploys the contract to the blockchain
fun SmartAccountFactory.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendStormNotifyWithDeploy(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    userPublicKeys: map<uint256, cell>,
    senderAddress: address,
    ownerAddress: address,
    jettonMinterAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormNotifyWithDeploy {
            queryId,
            amount,
            userPublicKeys,
            senderAddress,
            ownerAddress,
            jettonMinterAddress,
        },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendStormDeployOrdinarySa(
    self,
    from: address,
    queryId: uint64,
    userPublicKeys: map<uint256, cell>,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormDeployOrdinarySa { queryId, userPublicKeys },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactoryUpgradeCode(
    self,
    from: address,
    queryId: uint64,
    newCode: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactoryUpgradeCode { queryId, newCode },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactorySetHotPublicKey(
    self,
    from: address,
    queryId: uint64,
    hotPublicKey: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactorySetHotPublicKey { queryId, hotPublicKey },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactoryRequestColdPublicKey(
    self,
    from: address,
    queryId: uint64,
    replyAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactoryRequestColdPublicKey { queryId, replyAddress },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactorySetColdPublicKey(
    self,
    from: address,
    queryId: uint64,
    coldPublicKey: uint256,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactorySetColdPublicKey { queryId, coldPublicKey },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactorySetOrdinarySmartAccountCode(
    self,
    from: address,
    queryId: uint64,
    version: uint8,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactorySetOrdinarySmartAccountCode { queryId, version, code },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactorySetContent(
    self,
    from: address,
    queryId: uint64,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactorySetContent { queryId, content },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.sendSmartAccountFactorySetAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SmartAccountFactorySetAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun SmartAccountFactory.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun SmartAccountFactory.getFactoryData(self): SmartAccountFactoryDataReply {
    return net.runGetMethod(self.address, "get_factory_data");
}

fun SmartAccountFactory.getMinFees(self): SmartAccountFactoryMinFeesReply {
    return net.runGetMethod(self.address, "get_min_fees");
}

fun SmartAccountFactory.royaltyParams(self): SmartAccountFactoryRoyaltyParams {
    return net.runGetMethod(self.address, "royalty_params");
}

fun SmartAccountFactory.getNftAddressByIndex(self, itemIndex: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [itemIndex]);
}

fun SmartAccountFactory.getCollectionData(self): SmartAccountFactoryCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun SmartAccountFactory.getNftContent(self, itemIndex: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [itemIndex, individualContent]);
}
