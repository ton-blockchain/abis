/// Auto-generated wrapper for contract 'SmartAccountBlank'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/smart_account_blank.types"

/// Storage `SmartAccountBlankStorage` is defined in `../types/smart_account_blank.types`
struct SmartAccountBlank {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun SmartAccountBlank.fromStorage(
    storage: SmartAccountBlankStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): SmartAccountBlank {
    val stateInit = ContractState {
        code: build("SmartAccountBlank"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return SmartAccountBlank { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun SmartAccountBlank.fromAddress(address: address): SmartAccountBlank {
    return SmartAccountBlank { address };
}

/// Deploys the contract to the blockchain
fun SmartAccountBlank.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountBlank.sendStormDepositNotify(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    senderAddress: address,
    jettonMinterAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormDepositNotify { queryId, amount, senderAddress, jettonMinterAddress },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountBlank.sendStormReferralFees(
    self,
    from: address,
    queryId: uint64,
    referralSaAddress: address,
    amount: coins,
    jettonMinterAddress: address,
    notificationPayload: NotificationPayloadV2,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormReferralFees {
            queryId,
            referralSaAddress,
            amount,
            jettonMinterAddress,
            notificationPayload: notificationPayload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountBlank.sendStormInitSa(
    self,
    from: address,
    queryId: uint64,
    initData: InitializationData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormInitSa { queryId, initData },
    });
    return net.send(from, genericMsg);
}

fun SmartAccountBlank.sendStormDepositNotifyWithDeploy(
    self,
    from: address,
    queryId: uint64,
    vaultAddress: address,
    amount: coins,
    senderAddress: address,
    initData: InitializationData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormDepositNotifyWithDeploy {
            queryId,
            vaultAddress,
            amount,
            senderAddress,
            initData,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun SmartAccountBlank.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun SmartAccountBlank.getBlankStorageData(self): GetBlankStorageDataReply {
    return net.runGetMethod(self.address, "get_blank_storage_data");
}

fun SmartAccountBlank.getNftData(self): SmartAccountBlankNftDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}
