/// Auto-generated wrapper for contract 'StormLpMinter'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_lp_minter.types"

/// Storage `StormLpMinterStorage` is defined in `../types/storm_lp_minter.types`
struct StormLpMinter {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormLpMinter.fromStorage(
    storage: StormLpMinterStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormLpMinter {
    val stateInit = ContractState {
        code: build("StormLpMinter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormLpMinter { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormLpMinter.fromAddress(address: address): StormLpMinter {
    return StormLpMinter { address };
}

/// Deploys the contract to the blockchain
fun StormLpMinter.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormLpMinter.sendStormLpMintRequest(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    ownerAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpMintRequest { queryId, amount, ownerAddress },
    });
    return net.send(from, genericMsg);
}

fun StormLpMinter.sendStormLpBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    ownerAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpBurnNotification { queryId, amount, ownerAddress },
    });
    return net.send(from, genericMsg);
}

fun StormLpMinter.sendStormLpProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormLpProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormLpMinter.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormLpMinter.getVaultAddress(self): address {
    return net.runGetMethod(self.address, "get_vault_address");
}

fun StormLpMinter.getWalletAddress(self, ownerAddress: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun StormLpMinter.getJettonData(self): StormLpMinterJettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}
