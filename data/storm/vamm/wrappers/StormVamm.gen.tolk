/// Auto-generated wrapper for contract 'StormVamm'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_vamm.types"

/// Storage `StormVammStorage` is defined in `../types/storm_vamm.types`
struct StormVamm {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormVamm.fromStorage(
    storage: StormVammStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormVamm {
    val stateInit = ContractState {
        code: build("StormVamm"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormVamm { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormVamm.fromAddress(address: address): StormVamm {
    return StormVamm { address };
}

/// Deploys the contract to the blockchain
fun StormVamm.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormVamm.sendStormChangeSettings(
    self,
    from: address,
    redirectAddr: address,
    settings: AmmSettings,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormChangeSettings {
            redirectAddr,
            settings: settings.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StormVamm.sendStormPayFunding(
    self,
    from: address,
    oracleRef: OracleData,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormPayFunding { oracleRef: oracleRef.toCell() },
    });
    return net.send(from, genericMsg);
}

fun StormVamm.sendStormVammInit(
    self,
    from: address,
    redirectAddr: address,
    code: cell,
    data: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormVammInit { redirectAddr, code, data },
    });
    return net.send(from, genericMsg);
}

fun StormVamm.sendStormExecuteOrder(
    self,
    from: address,
    direction: uint1,
    orderIndex: uint3,
    traderAddr: address,
    prevAddr: address,
    refAddr: address?,
    executorIndex: uint32,
    order: cell,
    position: PositionData,
    oraclePayload: OraclePayload,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecuteOrder {
            direction,
            orderIndex,
            traderAddr,
            prevAddr,
            refAddr,
            executorIndex,
            order,
            position: position.toCell(),
            oraclePayload: oraclePayload.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun StormVamm.sendTakePositionV2(
    self,
    from: address,
    queryId: uint64,
    bundleSenderAddress: address?,
    ammAddress: address,
    direction: uint1,
    spentAmount: coins,
    addresses: AddressesData,
    ref: RefData,
    position: PositionData,
    intent: Intent,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TakePositionV2 {
            queryId,
            bundleSenderAddress,
            ammAddress,
            direction,
            spentAmount,
            addresses: addresses.toCell(),
            ref,
            position: position.toCell(),
            intent: intent.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormVamm.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormVamm.getPauseTime(self): uint32 {
    return net.runGetMethod(self.address, "get_pause_time");
}

fun StormVamm.getAmmContractData(self): cell {
    return net.runGetMethod(self.address, "get_amm_contract_data");
}

fun StormVamm.getAmmName(self): GetAmmNameReply {
    return net.runGetMethod(self.address, "get_amm_name");
}

fun StormVamm.getAmmStatus(self): GetAmmStatusReply {
    return net.runGetMethod(self.address, "get_amm_status");
}

fun StormVamm.getAmmState(self): GetAmmStateReply {
    return net.runGetMethod(self.address, "get_amm_state");
}

fun StormVamm.getExchangeSettings(self): GetExchangeSettingsReply {
    return net.runGetMethod(self.address, "get_exchange_settings");
}

fun StormVamm.getPositionManagerAddress(self): address {
    return net.runGetMethod(self.address, "get_position_manager_address");
}

fun StormVamm.getCanLiquidate(
    self,
    newPrice: int,
    positionRef: cell,
    newSettlementPrice: int,
    vpiSpread: int,
    vpiMarketDepthLong: int,
    vpiMarketDepthShort: int,
    vpiCoefficient: int,
    now: int,
): GetCanLiquidateReply {
    return net.runGetMethod(
        self.address,
        "get_can_liquidate",
        [
            newPrice,
            positionRef,
            newSettlementPrice,
            vpiSpread,
            vpiMarketDepthLong,
            vpiMarketDepthShort,
            vpiCoefficient,
            now,
        ],
    );
}

fun StormVamm.getCanForceClose(
    self,
    newPrice: int,
    positionRef: cell,
    newSettlementPrice: int,
    vpiSpread: int,
    vpiMarketDepthLong: int,
    vpiMarketDepthShort: int,
    vpiCoefficient: int,
    now: int,
): GetCanForceCloseReply {
    return net.runGetMethod(
        self.address,
        "get_can_force_close",
        [
            newPrice,
            positionRef,
            newSettlementPrice,
            vpiSpread,
            vpiMarketDepthLong,
            vpiMarketDepthShort,
            vpiCoefficient,
            now,
        ],
    );
}

fun StormVamm.getCanExecuteOrder(
    self,
    newPrice: int,
    positionRef: cell,
    newSettlementPrice: int,
    vpiSpread: int,
    vpiMarketDepthLong: int,
    vpiMarketDepthShort: int,
    vpiCoefficient: int,
    now: int,
): GetCanExecuteOrderReply {
    return net.runGetMethod(
        self.address,
        "get_can_execute_order",
        [
            newPrice,
            positionRef,
            newSettlementPrice,
            vpiSpread,
            vpiMarketDepthLong,
            vpiMarketDepthShort,
            vpiCoefficient,
            now,
        ],
    );
}

fun StormVamm.getSpotPrice(self): coins {
    return net.runGetMethod(self.address, "get_spot_price");
}

fun StormVamm.getLiquidationPriceVpi(
    self,
    openNotional: int,
    marginRatio: int,
    positionMargin: int,
    positionSize: int,
    isAdd: int,
    remainMargin: int,
    settlementOraclePrice: int,
    isSettleBySpotPrice: int,
): coins {
    return net.runGetMethod(
        self.address,
        "get_liquidation_price_vpi",
        [
            openNotional,
            marginRatio,
            positionMargin,
            positionSize,
            isAdd,
            remainMargin,
            settlementOraclePrice,
            isSettleBySpotPrice,
        ],
    );
}

fun StormVamm.getRemainMarginWithFundingPayment(
    self,
    newPrice: int,
    positionRef: cell,
    newSettlementPrice: int,
): GetRemainMarginWithFundingPaymentReply {
    return net.runGetMethod(
        self.address,
        "get_remain_margin_with_funding_payment",
        [newPrice, positionRef, newSettlementPrice],
    );
}

fun StormVamm.getTerminalAmmPrice(self): coins {
    return net.runGetMethod(self.address, "get_terminal_amm_price");
}

fun StormVamm.getPegAdjustCost(self, price: int): coins {
    return net.runGetMethod(self.address, "get_peg_adjust_cost", [price]);
}

fun StormVamm.getAdjustedSpotPrice(self, price: int, settlementPrice: int): coins {
    return net.runGetMethod(self.address, "get_adjusted_spot_price", [price, settlementPrice]);
}

fun StormVamm.getValidateSignatures(self, oraclePayload: cell): GetValidateSignaturesReply {
    return net.runGetMethod(self.address, "get_validate_signatures", [oraclePayload]);
}

fun StormVamm.getOracleData(self): GetOracleDataReply {
    return net.runGetMethod(self.address, "get_oracle_data");
}

fun StormVamm.getFunding(self, newPrice: int, newSettlementPrice: int): GetFundingReply {
    return net.runGetMethod(self.address, "get_funding", [newPrice, newSettlementPrice]);
}

fun StormVamm.getComputeFundingData(
    self,
    newPrice: int,
    pauseTime: int,
): GetComputeFundingDataReply {
    return net.runGetMethod(self.address, "get_compute_funding_data", [newPrice, pauseTime]);
}

fun StormVamm.getPremium(self, newPrice: int): coins {
    return net.runGetMethod(self.address, "get_premium", [newPrice]);
}

fun StormVamm.getFundingSettings(self): GetFundingSettingsReply {
    return net.runGetMethod(self.address, "get_funding_settings");
}

fun StormVamm.getDayTradingData(self): GetDayTradingDataReply {
    return net.runGetMethod(self.address, "get_day_trading_data");
}

fun StormVamm.getLazerData(self): GetLazerDataReply {
    return net.runGetMethod(self.address, "get_lazer_data");
}

fun StormVamm.getVammType(self): uint32 {
    return net.runGetMethod(self.address, "get_vamm_type");
}
