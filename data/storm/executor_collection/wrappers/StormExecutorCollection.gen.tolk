/// Auto-generated wrapper for contract 'StormExecutorCollection'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/storm_executor_collection.types"

/// Storage `StormExecutorCollectionStorage` is defined in `../types/storm_executor_collection.types`
struct StormExecutorCollection {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun StormExecutorCollection.fromStorage(
    storage: StormExecutorCollectionStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): StormExecutorCollection {
    val stateInit = ContractState {
        code: build("StormExecutorCollection"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return StormExecutorCollection { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun StormExecutorCollection.fromAddress(address: address): StormExecutorCollection {
    return StormExecutorCollection { address };
}

/// Deploys the contract to the blockchain
fun StormExecutorCollection.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionProvideVaults(
    self,
    from: address,
    requestTail: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionProvideVaults { requestTail },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionMintBatchContinuation(
    self,
    from: address,
    nextExecutorIndex: uint32,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionMintBatchContinuation { nextExecutorIndex },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionAddVault(
    self,
    from: address,
    vaultAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionAddVault { vaultAddress },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionGetRoyaltyParams(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionGetRoyaltyParams { queryId },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionMint(
    self,
    from: address,
    queryId: uint64,
    nftOwner: address,
    redirectAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionMint { queryId, nftOwner, redirectAddress },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionChangeAdmin(
    self,
    from: address,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionChangeAdmin { newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.sendStormExecutorCollectionChangeContent(
    self,
    from: address,
    content: StormExecutorCollectionContent,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: StormExecutorCollectionChangeContent { content: content.toCell() },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun StormExecutorCollection.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun StormExecutorCollection.getCollectionData(self): StormExecutorCollectionDataReply {
    return net.runGetMethod(self.address, "get_collection_data");
}

fun StormExecutorCollection.getNftAddressByIndex(self, itemIndex: int): address {
    return net.runGetMethod(self.address, "get_nft_address_by_index", [itemIndex]);
}

fun StormExecutorCollection.royaltyParams(self): StormRoyaltyParams {
    return net.runGetMethod(self.address, "royalty_params");
}

fun StormExecutorCollection.getNftContent(self, itemIndex: int, individualContent: cell): cell {
    return net.runGetMethod(self.address, "get_nft_content", [itemIndex, individualContent]);
}

fun StormExecutorCollection.getExecutorVaultsWhitelist(self): map<uint256, coins> {
    return net.runGetMethod(self.address, "get_executor_vaults_whitelist");
}
