/// Auto-generated wrapper for contract 'TelemintNftItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/telemint_nft_item.types"

/// Storage `TelemintNftItemStorage` is defined in `../types/telemint_nft_item.types`
struct TelemintNftItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun TelemintNftItem.fromStorage(
    storage: TelemintNftItemStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): TelemintNftItem {
    val stateInit = ContractState {
        code: build("TelemintNftItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return TelemintNftItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun TelemintNftItem.fromAddress(address: address): TelemintNftItem {
    return TelemintNftItem { address };
}

/// Deploys the contract to the blockchain
fun TelemintNftItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemDeploy(
    self,
    from: address,
    senderAddress: address,
    bid: coins,
    tokenInfo: cell,
    nftContent: cell,
    auctionConfig: TelemintAuctionConfig,
    royaltyParams: TelemintNftRoyaltyParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemDeploy {
            senderAddress,
            bid,
            tokenInfo,
            nftContent,
            auctionConfig: auctionConfig.toCell(),
            royaltyParams: royaltyParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemStartAuction(
    self,
    from: address,
    queryId: uint64,
    auctionConfig: TelemintAuctionConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemStartAuction {
            queryId,
            auctionConfig: auctionConfig.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemCancelAuction(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemCancelAuction { queryId },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemGetRoyaltyParams(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemGetRoyaltyParams { queryId },
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.sendTelemintNftItemChangeDnsRecord(
    self,
    from: address,
    queryId: uint64,
    key: uint256,
    value: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TelemintNftItemChangeDnsRecord { queryId, key, value },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun TelemintNftItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun TelemintNftItem.getNftData(self): TelemintNftItemDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun TelemintNftItem.getFullDomain(self): slice {
    return net.runGetMethod(self.address, "get_full_domain");
}

fun TelemintNftItem.getTelemintTokenName(self): string {
    return net.runGetMethod(self.address, "get_telemint_token_name");
}

fun TelemintNftItem.getTelemintAuctionState(self): TelemintAuctionStateReply {
    return net.runGetMethod(self.address, "get_telemint_auction_state");
}

fun TelemintNftItem.getTelemintAuctionConfig(self): TelemintAuctionConfigReply {
    return net.runGetMethod(self.address, "get_telemint_auction_config");
}

fun TelemintNftItem.royaltyParams(self): TelemintRoyaltyParamsReply {
    return net.runGetMethod(self.address, "royalty_params");
}

fun TelemintNftItem.dnsresolve(self, subdomain: slice, category: int): TelemintDnsResolveReply {
    return net.runGetMethod(self.address, "dnsresolve", [subdomain, category]);
}
