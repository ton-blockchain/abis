/// Auto-generated wrapper for contract 'GasPumpMasterV4'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/gaspump_master_common.types"
import "../types/gaspump_master_v4.types"

/// Storage `GasPumpMasterDeploymentStorage` is defined in `../types/gaspump_master_common.types`
struct GasPumpMasterV4 {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun GasPumpMasterV4.fromStorage(
    storage: GasPumpMasterDeploymentStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): GasPumpMasterV4 {
    val stateInit = ContractState {
        code: build("GasPumpMasterV4"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return GasPumpMasterV4 { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun GasPumpMasterV4.fromAddress(address: address): GasPumpMasterV4 {
    return GasPumpMasterV4 { address };
}

/// Deploys the contract to the blockchain
fun GasPumpMasterV4.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage00000400(
    self,
    from: address,
    payload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage00000400 { payload },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage1dce79ba(
    self,
    from: address,
    amount: coins,
    destination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage1dce79ba { amount, destination },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage268800bd(
    self,
    from: address,
    newTradeState: int257,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage268800bd { newTradeState },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpProvideWalletAddress(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    includeAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpProvideWalletAddress { queryId, ownerAddress, includeAddress },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage5b22c66d(
    self,
    from: address,
    amount: coins,
    destination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage5b22c66d { amount, destination },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMasterV4Buy(
    self,
    from: address,
    execute: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMasterV4Buy { execute },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage7362d09c(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    sender: address,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage7362d09c { queryId, amount, sender, forwardPayload },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage76b8a294(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage76b8a294 {},
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpBurnNotification(
    self,
    from: address,
    queryId: uint64,
    amount: coins,
    owner: address,
    responseDestination: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpBurnNotification { queryId, amount, owner, responseDestination },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage819dbe99(
    self,
    from: address,
    queryId: uint64,
    minterAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage819dbe99 { queryId, minterAddress },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessage9b618389(
    self,
    from: address,
    bounce: bool,
    destination: address,
    value: int257,
    sendMode: int257,
    body: cell?,
    stateInitCode: cell?,
    stateInitData: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessage9b618389 {
            bounce,
            destination,
            value,
            sendMode,
            body,
            stateInitCode,
            stateInitData,
        },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessageA7e7be8a(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessageA7e7be8a {},
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpUpdateContent(
    self,
    from: address,
    content: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpUpdateContent { content },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessageB70df392(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessageB70df392 {},
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessageB8462adf(
    self,
    from: address,
    address0: address,
    address1: address,
    config0: cell,
    config1: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessageB8462adf { address0, address1, config0, config1 },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessageCa153636(
    self,
    from: address,
    amount: coins,
    destination: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessageCa153636 { amount, destination },
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.sendGasPumpMessageD53276db(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: GasPumpMessageD53276db { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun GasPumpMasterV4.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun GasPumpMasterV4.owner(self): any_address {
    return net.runGetMethod(self.address, "owner");
}

fun GasPumpMasterV4.getFullJettonData(self): (
    int,
    int,
    any_address,
    cell,
    cell,
    int,
    int,
    int,
    int,
    tuple,
    int,
    int,
    int,
    int,
    int,
) {
    return net.runGetMethod(self.address, "get_full_jetton_data");
}

fun GasPumpMasterV4.getJettonData(self): GasPumpJettonData {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun GasPumpMasterV4.getWalletAddress(self, ownerAddress: any_address): any_address {
    return net.runGetMethod(self.address, "get_wallet_address", [ownerAddress]);
}

fun GasPumpMasterV4.anotherMinterAddress(self): any_address {
    return net.runGetMethod(self.address, "anotherMinterAddress");
}

fun GasPumpMasterV4.getBuyAmount(self, tonAmount: int): int {
    return net.runGetMethod(self.address, "getBuyAmount", [tonAmount]);
}

fun GasPumpMasterV4.getSellAmount(self, jettonAmount: int): int {
    return net.runGetMethod(self.address, "getSellAmount", [jettonAmount]);
}

fun GasPumpMasterV4.getAbiIpfs(self): slice {
    return net.runGetMethod(self.address, "get_abi_ipfs");
}

fun GasPumpMasterV4.supportedInterfaces(self): GasPumpSupportedInterfaces {
    return net.runGetMethod(self.address, "supported_interfaces");
}
