/// Auto-generated wrapper for contract 'FragmentNumbersItem'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/fragment_numbers_item.types"

/// Storage `FragmentNumbersItemStorage` is defined in `../types/fragment_numbers_item.types`
struct FragmentNumbersItem {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun FragmentNumbersItem.fromStorage(
    storage: FragmentNumbersItemStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): FragmentNumbersItem {
    val stateInit = ContractState {
        code: build("FragmentNumbersItem"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return FragmentNumbersItem { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun FragmentNumbersItem.fromAddress(address: address): FragmentNumbersItem {
    return FragmentNumbersItem { address };
}

/// Deploys the contract to the blockchain
fun FragmentNumbersItem.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.sendFragmentNumbersItemDeploy(
    self,
    from: address,
    senderAddress: address,
    bid: coins,
    tokenInfo: cell,
    nftContent: cell,
    auctionConfig: FragmentNumbersItemAuctionConfig,
    royaltyParams: FragmentNumbersItemRoyaltyParams,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentNumbersItemDeploy {
            senderAddress,
            bid,
            tokenInfo,
            nftContent,
            auctionConfig: auctionConfig.toCell(),
            royaltyParams: royaltyParams.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.sendFragmentNumbersItemStartAuction(
    self,
    from: address,
    queryId: uint64,
    auctionConfig: FragmentNumbersItemAuctionConfig,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentNumbersItemStartAuction {
            queryId,
            auctionConfig: auctionConfig.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.sendFragmentNumbersItemCancelAuction(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentNumbersItemCancelAuction { queryId },
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.sendFragmentNumbersItemTransfer(
    self,
    from: address,
    queryId: uint64,
    newOwnerAddress: address,
    responseDestination: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentNumbersItemTransfer {
            queryId,
            newOwnerAddress,
            responseDestination,
            customPayload,
            forwardTonAmount,
            forwardPayload,
        },
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.sendFragmentNumbersItemGetStaticData(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentNumbersItemGetStaticData { queryId },
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.sendFragmentNumbersItemGetRoyaltyParams(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentNumbersItemGetRoyaltyParams { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FragmentNumbersItem.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun FragmentNumbersItem.getNftData(self): FragmentNumbersItemDataReply {
    return net.runGetMethod(self.address, "get_nft_data");
}

fun FragmentNumbersItem.getTelemintTokenName(self): string {
    return net.runGetMethod(self.address, "get_telemint_token_name");
}

fun FragmentNumbersItem.getTelemintAuctionState(self): FragmentNumbersItemAuctionStateReply {
    return net.runGetMethod(self.address, "get_telemint_auction_state");
}

fun FragmentNumbersItem.getTelemintAuctionConfig(self): FragmentNumbersItemAuctionConfigReply {
    return net.runGetMethod(self.address, "get_telemint_auction_config");
}

fun FragmentNumbersItem.royaltyParams(self): FragmentNumbersItemRoyaltyParamsReply {
    return net.runGetMethod(self.address, "royalty_params");
}
