/// Auto-generated wrapper for contract 'FragmentMarketappProxyKnown'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "../types/fragment_marketapp_proxy_known.types"

/// Storage `FragmentMarketappProxyKnownStorage` is defined in `../types/fragment_marketapp_proxy_known.types`
struct FragmentMarketappProxyKnown {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun FragmentMarketappProxyKnown.fromStorage(
    storage: FragmentMarketappProxyKnownStorage,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): FragmentMarketappProxyKnown {
    val stateInit = ContractState {
        code: build("FragmentMarketappProxyKnown"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return FragmentMarketappProxyKnown { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun FragmentMarketappProxyKnown.fromAddress(address: address): FragmentMarketappProxyKnown {
    return FragmentMarketappProxyKnown { address };
}

/// Deploys the contract to the blockchain
fun FragmentMarketappProxyKnown.deploy(
    self,
    from: address,
    config: SendParams = {},
): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun FragmentMarketappProxyKnown.sendFragmentMarketappProxyKnownBuy(
    self,
    from: address,
    signature: bits512,
    request: FragmentMarketappProxyKnownSignedBuyRequest,
    extra: RemainingBitsAndRefs,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentMarketappProxyKnownBuy {
            signature,
            request: request.toCell(),
            extra,
        },
    });
    return net.send(from, genericMsg);
}

fun FragmentMarketappProxyKnown.sendFragmentMarketappProxyKnownAdminRelay(
    self,
    from: address,
    command: FragmentMarketappProxyKnownAdminRelayCommand,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FragmentMarketappProxyKnownAdminRelay { command: command.toCell() },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun FragmentMarketappProxyKnown.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
